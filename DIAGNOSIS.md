# ANNP 诊断记录（截至 2026-08-14）

给组里看的一份状态汇总，来源是 `DESIGN.md` §28–§40。分三块：**已确立**、**已排除**、**未测**。
每条都给出处，细节和原始数字都在 DESIGN.md 对应节里，这里不重复推导。

> 写这份东西的直接原因：8/14 拿到了第一个**外部基线**。在此之前所有对照都是我们自己的消融或计数表，
> 也就是说，我们从来没测过"这个架构相对于一个平凡的学习模型是好是坏"。测了，结论不好。

---

## 0. 一句话结论

**当前架构是一个有效窗口 2–3 token 的上下文模型。窗口有值，但宽度不随数据增长；
而在同参数量、同一条数据流、同协议下，一个三层窗口感知机领先 1.49 比特。**

所以现在能诚实主张的是**机制**（纯局部规则、网络内无反向传播，也能越过无上下文天花板），
不是**性能**。§0 里的长上下文主张需要改写。

---

## 1. 已确立

按证据强度排序。"强"= 跨数量级或多种子配对且符号一致。

| # | 结论 | 数字 | 强度 | 出处 |
|---|---|---|---|---|
| 1 | **有效窗口 2–3 token，且不随数据变宽** | $\lambda$：真实 1.40→1.53（10×），合成 1.25→1.39（10×） | **强**（2 信源 × 1 数量级） | §36.3, §40.6 |
| 2 | **两根承重墙：节点阶梯 + 查表读出** | +0.6324 / +0.5063，其余三项合计 <0.16 | **强** | §40.2 |
| 3 | **真实文本上的贡献随规模翻转** | 40k −0.1560 ($t{=}-9.21$) → 1M **+0.0459 ± 0.0226** ($t{=}+4.07$, 4/4 种子) | 中（1M 只有 4 种子） | §40.1 |
| 4 | **持续学习上有真实保留** | 配对差 **+0.3504 ± 0.0417**；代价：学新域贵 0.17 比特 | 中 | §28 |
| 5 | **节点数不是承重自由度** | 256/1024/4096 三档在噪声内 | 中 | §35.1, §40.3 |
| 6 | 路由不需要任何参数 | 用 $W_jq_j$ 当边打分即可，零路由参数、无反向通路 | 强（结构性） | §10.1③ |
| 7 | 停机无需阈值或 max_hops | 固定参考 logit = 0 即可；质量守恒 $<10^{-12}$ | 强 | §10.3 |
| 8 | 任意线程数位级一致 | 1 vs 8 线程逐位相同 | 强 | §10.3 |
| 9 | tokenizer 逐 token 对齐参考实现 | 4000 文档 / 284,909 token 零失配 | 强 | §33.1 |

**关于 #3 的一句要紧的话**：这条当时被写成正面结果（"跌破无上下文天花板 0.559 比特"）。
参照系错了——`bypass` 是我们自己的消融，order-1 计数编码器是平凡计数表。**赢过自己的消融不是成就。**
放进正确参照系后见第 4 节。

---

## 2. 已排除

**这一栏几乎全是我自己提的假说被自己的测量推翻。**列出来是因为它们代表了已经花掉的搜索预算，
组里不必再走一遍。

| 假说 | 怎么死的 | 出处 |
|---|---|---|
| 上下文进不了记忆的键 | 直接测，进得去 | §31.1 |
| 键的上下文宽度写死且取值在坏的一侧 | 扫了，不是 | §31.2 |
| **读出容量是瓶颈** | 我明确说过是，测出来不是 | §33.6 |
| 维度挤压 | 解释是错的；损伤来自"在大量访问上做平均" | §34.4–34.5 |
| 键不重复所以没东西可检索 | 键会回来 | §34.6–34.7 |
| **几何自由度（拓扑）是承重的** | 环拓扑图论层面成立，**端到端零改善** | §35.1, §38 |
| 读更慢的 rung 能延长触达 | 低通不是延迟 | §37 |
| **查询 + 燃料**（唯一一次给粒子加字段） | 在它被设计来解决的问题上同样不如基线，设计作废 | §40.4 |
| 更小的 $M$ 单调更好 | 不单调 | §40.3 |
| 迷失在中间现象存在 | 换成曝光匹配的对照后消失 | §29.5 |
| 触达窄是欠训练伪影 | 10× 数据，$\lambda$ 动 9–11% | §40.6 |

一个反面例子（值得单独记）：**$b_t$ 门初值取 0** 等于 $e^{-2\ln M}\approx 4\times10^{-6}$ 的 softmax 质量，
是**结构性排除**而不是"起步弱"。旧的"用途"测试在这个 bug 下照样通过；新写的竞争测试在 bug 下会失败。
→ 测试通过不等于机制在工作。（§31.11）

---

## 3. 未测

| 项 | 为什么重要 | 状态 |
|---|---|---|
| **Transformer 臂** | 用户明确要求；MLP 只是垫脚石 | 未开工 |
| **1.49 比特差的拆解** | 基线赢在隐层 / 梯度 / 无损窗口三者之一或全部。**每一样都是我们的设计前提**，所以这个拆解直接回答"缺的东西可不可补" | 未开工，很便宜（去隐层 + 窗口改 1，各一次） |
| 沿 surprise 下降的路由是否同样可导航 | §9 的全部结论能否迁移到实际系统，**关键未验证假设** | 未测（§9.4） |
| 生长 / 修剪 | §1.9 的动态部分，从未实现 | 未写 |
| 分开沉积 / 置信度加权是否转正 | 方向为正但四种子下在噪声边缘 | 未采纳 |

---

## 4. 外部基线（8/14，本记录的触发原因）

同参数量、同数据流、同 prequential 协议，调参优势**给基线**（4 个学习率并行取最好）。

同一条流：2827 文档、200,000 token、7214 个词片截到 4095、1.72% 落进 catch-all——两边逐项一致。

| | 参数 | 末十分位 |
|---|---|---|
| order-1 计数编码器 | — | 7.6475 &nbsp;&nbsp;<- 任何无上下文模型的天花板 |
| **本架构** | ≈1,740,000 | **6.9601** |
| 可加 order-2 天花板 | — | 6.4142 &nbsp;&nbsp;<- 任何不能表达交互的模型的天花板 |
| **窗口 MLP（窗口 3）** | 1,675,520 | **5.4683** |

**差距 1.4918 比特。**窗口取 3 是因为 §40.6 测出**我们自己**的有效窗口就是 2–3。

同一批的 `--bypass` 配对臂（种子 1：−0.1043，种子 2：+0.0937，均值 **−0.005**，符号相反）说明：
**在基线所在的这个规模上，架构里"新的那部分"贡献不可测。**两个种子分辨不了这个量
（噪声 ±0.10，是 1M 上效应量的 4 倍），所以它不推翻也不确立 §40.1，
只是与"交叉点在 200k 与 1M 之间"一致。

基线越过了可加 order-2 天花板，这自洽：那个天花板只对不能表达交互的模型成立，MLP 有隐层。
我们落在两个天花板之间——即"比任何无上下文模型好，但没有越过不能表达交互这条线"。

**算力不持平，方向对基线不利于我们**：基线单学习率约 6.9M flops/token（`vocab×hidden` 输出层
加反传占大头），我们约 2.2M。**参数量匹配（用户要求的口径），算力是基线的 3 倍。**
如实记下来；但 3 倍算力不足以解释 1.49 比特，而且这个开销来自 4096 类 softmax，
是任何要输出同一个分布的模型都要付的。

**一个跨规模可比性的坑**：我们 200k 得 6.9601，比 1M 的 7.1437 还低。不是 200k 更容易学，
是 200k 只见到 7214 个词片（unk 1.72%），1M 见到更多（unk 3.37%），**任务本身不同**。
跨规模的绝对损失不可比——这正是必须补这一跑而不是直接拿 1M 去比的原因。

### 为了让基线可信做的事

手写反向传播如果有细微错误，基线就只是"学得差"，而学得差的基线会让被测系统显得好。
所以先写了**有限差分梯度检验**（探 `w2`、`w1` 和窗口确实读到的一个 embedding 项），通过后才跑。

---

## 5. 方法学教训

这些比上面任何一条实验结论更容易在下一轮重犯。

1. **零假设选错，两次。** 稀有针符号用了均匀分布当机会水平（§29.4）；真实文本的 lag 谱又用了均匀，
   而探针学到的是边缘分布（§36.2）。修法：曝光匹配的 foil、对**平台**读数。
2. **共线性会污染合并后的结论，不只是分层结论。** 针的教学位置 ∝ 序号，使得"波段"与"该键的最后一次写入"
   变成同一件事。修法：随机置换 + 位置奇偶切分。（§30）
3. **坏掉的是量具，看起来像被测物坏了。** 门禁 grep 错了字符串、`sed` 在块内空行处提前终止——
   两次都报告"代码坏了"，实际代码是对的。
4. **批处理的完成标记在撒谎。** `echo DONE >> log` 无条件执行，被杀掉的运行被标成完成，
   续跑还会跳过它。改成 `if not errorlevel 1 echo DONE`。（§40.5）
5. **一个启动开销让所有真实文本计时读数失效。** `entropy_rate` 在 `--corpus` 下照跑，
   vocab 1024 时 6e10 次操作。**更该记的是误诊过程**：我把 vocab 砍了 16 倍、零改善，
   然后继续砍，而不是停下来问为什么砍了没用。（§39）
6. **一次实验只动一个变量。** `lag-1m` 同时改了 vocab 和 token 数，在得出结论前发现了。
7. **远程作业生命周期**（4 次失败）：SSH 会话结束会杀进程；进程活着但写进死掉的管道 = 输出必然丢失；
   `start /b` 断连即死。可用配方：**远端重定向到文件 + `schtasks /create /sc once` + `/run`**，
   配合续跑标记。（§34.3）

---

## 6. 我建议的下一步

**先拆那 1.49 比特，再决定架构去留。** 理由：这是唯一一个既便宜又能改变决策的测量。

- 若差距主要来自**隐层**（表达交互的能力）→ 我们的节点是线性联想，这可补，但要动核心。
- 若主要来自**梯度**（端到端优化）→ 与"网络内无反向传播"直接冲突，这是立项前提，不可补。
- 若主要来自**无损窗口** → 与"窗口靠共访涌现"冲突，同样是前提。

拆解结果如果落在后两者，诚实的产出形态是**一个机制性/负面结果**：
纯局部规则也能越过无上下文天花板——这是真的、可复现的、且有对照的观察，
只是它不是"一个更好的架构"。

**这份判断需要组里拍板，我不替这个决定做主。**
