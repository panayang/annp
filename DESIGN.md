# ANNP PoC — 设计共识快照 v0

> **这不是总设计方案。** 上一代的教训是"开头定死一个方案 → 思维固化 → 迭代 13 代还是不行"。
> 本文只记录**当前讨论到哪儿了**，每一节都标了置信度和对应的证伪实验。
> 标「分叉」的地方是明确留给大家吵的，标「待验」的是我们自己也不知道对不对的。
> 任何一节被实验推翻，改这份文档，不要迁就它写代码。

---

## 0. 一句话图像

> 前向传播 = 一群携带质量的准粒子，在一张自组织的图上做**带吸收态的守恒扩散**；
> 学习 = 所有权重矩阵内部的**多时间尺度扩散阶梯**在吸收更新流。
> 没有 train/inference 之分，没有 epoch，没有 fast/slow 模块之分。

三个量贯穿全文，先定义清楚：

| 量 | 定义 | 角色 |
|---|---|---|
| **payload** `[f64; d_head]` | 单位范数向量 | 内容 / 方向 |
| **mass** `f64` | L1 测度，全局守恒 | 路由测度 / 剪枝 / 算力预算 |
| **surprise** `f64` | 局部预测残差范数 $\lVert v - Wq\rVert$ | 可塑性 / 路由 / 结构生长的**唯一**信用信号 |

**设计红线：particle 上不允许出现第四个语义字段。** 任何"再加一个标记位就能解决"的提案，先检查能不能由这三个量推出来。
（`token: u32` / `slot: u16` 是重组用的路由标签，不是语义信息，节点看不到它们。）

---

## 1. 数据流逐段设计

### 1.1 嵌入 · 稳

token id → 查表得 $e \in \mathbb{R}^{d_{model}}$，且 $d_{model} = P \cdot d_{head}$。

**嵌入表与输出头权重绑定（weight tying）。** 这不是为了省参数，是为了让嵌入表**只从输出头的梯度学到**——这样即使我们最后选了"不跨图反传"的路线（见 1.5），嵌入表依然有明确的学习信号，不需要梯度穿过整张图。

- 待验：绑定后的嵌入向量作为**输入表示**是否够好。标准做法说够，但我们的用法不标准（要被打碎成正交片）。
- 对应实验：**A6**。

### 1.2 打碎 · 稳

$e$ 先过一个**固定的结构化正交变换**（Hadamard / butterfly，零参数零超参），再切成 $P$ 段，每段 $d_{head}$ 维。

正交的三个理由，都是第一性的：
1. Parseval ⇒ 能量守恒，每片能量均衡，不会出现"某一片全是零"；
2. 梯度等距，打碎/重组这一层不引入任何梯度缩放病态；
3. 零参数，不上超参表。

**碎片 → particle 的映射（方向与测度分离）：**

$$\texttt{payload}_s = \frac{c_s}{\lVert c_s\rVert}, \qquad \texttt{mass}_s = \frac{\lVert c_s\rVert}{\sum_t \lVert c_t\rVert}$$

即：**方向进 payload，模长进 mass**。两个好处——
- payload 永远单位范数 ⇒ 所有 $d_{head}$ 级矩阵乘法在固定数值量程里跑，几十跳之后也不会下溢/上溢。这是个纯工程但很关键的决定。
- 弱碎片天然 mass 小 ⇒ 被 1.6 的质量地板早早吸收 ⇒ **自适应算力是免费的**，不需要额外机制。

- 分叉：`mass ∝ 模长` vs `mass = 1/P 均匀`。前者更省算力，后者更无偏。→ **A6**

### 1.3 入射（内容寻址，非均匀）· 稳（这是上一代最硬的血泪教训）

节点住在 $G \times G$ 的二维环面上，$N = G^2$。**网格大小 $G$ 是我们承认的超参数之一。**

token 的入射坐标取**固定随机平面上的相位**：

$$\theta_1 = \operatorname{atan2}(\langle e, a_1\rangle, \langle e, a_2\rangle), \quad \theta_2 = \operatorname{atan2}(\langle e, b_1\rangle, \langle e, b_2\rangle)$$

$a_*, b_*$ 是初始化时固定的随机正交向量，**不训练**。取相位而非投影值，好处是**天然尺度不变**，不需要任何归一化常数。

第 $s$ 个 particle 落在 $(\theta_1,\theta_2) + \delta_s$，位移 $\delta_s$ 由 slot id 哈希得到，**分布在半径 $\sim\sqrt{P/\pi}$ 的圆盘内**——这个半径不是调的，是由"平均每个 slot 占一个节点"反推的。

为什么必须非均匀（这条要写进论文）：
> 均匀入射 = 最大熵输入分布 ⇒ 网络无法形成拓扑图谱 ⇒ 需要互相关联的记忆是**异址的** ⇒ particle 必须长途跋涉才能相遇 ⇒ 跳数暴涨。
> **局部性不是优化手段，它是联想记忆能工作的前提。**

- 对应实验：**A4**（内容寻址 vs 均匀随机入射），这条如果没拉开差距，说明我们对上一代的归因错了。

### 1.4 节点内部（FFN 与注意力已合并）· 稳（结构）/ 分叉（输出什么）

**一个节点 = 一个矩阵 $W \in \mathbb{R}^{d_{head}\times d_{head}}$ + 一个"上次输入"寄存器。** 没有别的。

- **读**：$u = \varphi(W q)$，$q$ = 入射 payload。
- **写**（delta rule，$q_{prev}$ = 该节点上次见到的 payload，$v$ = 本次）：
  $$W \mathrel{+}= \eta\,(v - W q_{prev})\,q_{prev}^\top$$
- **surprise**：$\sigma = \lVert v - W q_{prev}\rVert$。

这个形式的含义：**每个节点在线学习"预测自己下一个会看到什么"**，$W$ 是一个转移算子，$u$ 是它的预测。合并 FFN 与 attention 的具体形式就是这个——浅层时间尺度上 $W$ 是刚写入的键值对（=attention），深层沉淀下来的是稳定键值对（=FFN as key-value memory）。

**因果性从这里涌现**：$W$ 由到达序构建，读取只反映此前写入。它是**局部偏序**，不是全局下三角掩码。（这一点值得单开一节写进论文：单向注意力是全局同步假设的产物，不是因果性的必要形式。）

为什么用 delta rule 而不是 Hebb + $\lambda$：
- 只写"意外"，已能预测的内容不改写 ⇒ 抗干扰（**噪声免疫的机制解释**）；
- 不需要遗忘率 $\lambda$（少一个超参），也因此**结构上没有 recency bias ⇒ lost-in-the-middle 在原理上被消掉**；
- 免费产出 surprise 这个标量，后面三个机制全靠它。

- **分叉（重要）**：往下游路由的是**预测 $u$** 还是**残差 $v - Wq$**？
  - 路由预测：网络是分布式的下一状态预测器，语义连贯。（我倾向这个）
  - 路由残差：纯预测编码，"只传我解释不了的"，稀疏性更强，但**噪声是不可预测的，会被优先传播**——和噪声免疫的目标反向。
  - → **A8**
- 待验：$\varphi$ 取什么。倾向 GELU 或 tanh，先不纠结，但要记住 tanh 有天然的范数约束（对停机保证友好）。

### 1.5 路由 · 分叉（这是组内最大的分歧，做成可插拔）

节点对 `{邻居} ∪ {吸收}` 出一组 logits，softmax 后 **确定性 top-k 分裂**，mass 按权重分配（严格守恒，$\sum = 1$）。

$$\ell_e = \underbrace{\text{content}(u, e)}_{\text{两种实现}} + \underbrace{b_e}_{\text{内稳态电导}}$$

**$b_e$（两种实现共用）**：慢变标量，随该边使用率**下降**（intrinsic plasticity 式负反馈）。
这是用来防路由塌缩的，**替代 MoE 的 load-balance 辅助损失**——那是一个损失项加一个权重超参，这个是纯局部负反馈，零超参。

**content 项的两种实现（enum 切换，接口相同）：**

| | (a) 全局梯度路线 | (b) 局部预期路线 **（已实现，见 §10）** |
|---|---|---|
| 形式 | $\langle u, K_e\rangle$，$K_e$ 由任务损失经门控直通梯度训练 | $\langle u,\ \widehat{W_j q_j}\rangle$ —— **没有 $K_e$，没有任何路由参数** |
| 信用来源 | 误差沿 routing trace 反传 | 无需信用分配：邻居 $j$ 本来就在算 $W_j q_j$（它预期的下一个输入），直接拿来当 key |
| 变分解释 | — | 自由能下降流：particle 流向"已经认识它"的节点 |
| 工程量 | 一个反向核。**不需要 trace 存储**——§3 的位级确定性意味着重放前向即可精确重建，确定性是为可复现性买的，顺手把这里的内存成本降到零 | 不需要反向核，代码量更小 |
| 风险 | 门控梯度随 mass 一起衰减，深跳处可能学不动（§6 风险 1）。**这个量可以在没有网络的情况下单独测**——搭一条 $k$ 跳门控链，看 $\partial L/\partial K_e$ 的范数随跳深的标度。见 A1-pre | 纯局部预测编码在难任务上历史战绩一般 |

**A1-pre（在写反向核之前做）**：门控梯度衰减测量。指数衰减 ⇒ 带着数据否掉 (a)；不衰减 ⇒ (a) 值得那个反向核。这是 E0-d 式的做法：不靠意见决定，也不先付全部工程成本，只测决定生死的那一个量。

**这一条不站队，用数据结束争论。**(b) 的副作用恰好是我们要的：particle 自动流向已经认识它的节点 ⇒ 内容寻址和拓扑图谱**自组织**，不用额外造机制。而且 (b) 工程量更小，**建议先写 (b)**。

- 对应实验：**A1**，这是整个项目最重要的一次对照。

### 1.6 停机与算力预算 · 稳

**没有 max_hops。** 质量守恒 + 吸收伪边概率非零 ⇒ 吸收 Markov 链 ⇒ 几乎必然停机，可证明。

**相对质量地板 $\epsilon$**：mass < $\epsilon$ 的 particle 直接吸收。于是每 token 的 particle 数上界 $\approx 1/\epsilon$。

$\epsilon$ **不是训练超参，是推理期的 anytime 算力旋钮**——调大就更快更糙。这直接给出长上下文的算力答案：

$$\text{每 token 算力} = O(P \cdot H \cdot d_{head}^2), \quad \textbf{与序列长度无关}$$

- 默认**确定性 top-k**，不采样。可复现性是硬约束（见 §3）。采样版留作 ablation。

### 1.7 重组与输出头 · 稳

被吸收的 particle 按 `(token, slot)` 累加：$\hat c_s = \sum_i m_i \cdot \texttt{payload}_i \cdot Z$（$Z$ = 打碎时的模长和）。
拼成 $\hat e$ → 逆正交变换 → 与嵌入表相乘得 logits → 与下一个 token 算交叉熵。

**这是整个系统里唯一存在反向传播的地方**（路线 (b) 下），而且它只穿过一个矩阵乘法。

### 1.8 可塑性阶梯（记忆巩固）· 待验 —— 全设计里风险最高的一块

每个可训练矩阵不是一个矩阵，而是一列同形状变量 $U_1,\dots,U_m$：

1. **所有更新只写进 $U_1$**（不管来自 delta rule 还是梯度——见下面的注入深度）；
2. **相邻层扩散**：$C_k \dot U_k = g_{k-1}(U_{k-1}-U_k) + g_k(U_{k+1}-U_k)$；
3. **前向只读 $U_1$** ⇒ **前向算力完全不变，只多内存**。

**为什么没有"沉淀机制"这个模块**：沉淀就是扩散的自发行为。一次性写入被 $U_2$ 拉回、痕迹消失；反复写入逐层深入、存下来。**重复即沉淀**，筛选器是免费且正确的。不需要 replay buffer、不需要 EWC、不需要触发判据。

**为什么幂律**：均匀链 = 一维扩散的离散化，原点 Green 函数 $\sim(4\pi Dt)^{-1/2}$ ⇒ 单次痕迹按 $t^{-1/2}$ 衰减。
指数衰减承诺了一个特征时间尺度（= 一个必然错的超参），幂律是无标度的。**这是我们对"长上下文遗忘"的正面回答**：不是不遗忘，是以无特征尺度的方式遗忘。

**为什么不该有 fast/slow 两个模块**：时间尺度分离发生在**突触内部**，不在模块之间。$U_1 \approx$ 沉淀 + 近期，读一个矩阵就同时拿到长期知识和情节记忆。没有边界可跨，所以没有"迁移机制"要设计。
（这是对"不该每个节点都有海马"的直接回应：**没有任何节点有海马**，海马-皮层只是这个谱的一种空间实现，不是它的定义。）

#### 1.8.1 常数怎么来（不抄任何人）

- **约束一（尺度不变）** $\tau_k/\tau_{k-1} = r$ ⇒ $C_k \propto r^k,\ g_k \propto r^{-k},\ \tau_k \propto r^{2(k-1)}$
- **约束二（覆盖）** $\tau_m \ge T$（$T$ = 要求的最长保持跨度，token 数）

$$\boxed{\,m = 1 + \frac{\ln T}{2\ln r}\,}$$

**$m$ 不是自由参数**，由记忆跨度算出。真正自由的只有 $r$（内存 ↔ 衰减曲线光滑度的分辨率旋钮）。$T=10^6$ 时：$r=4 \Rightarrow m\approx6$；$r=2 \Rightarrow m\approx11$。

> 文献锚点：Fusi–Drew–Abbott cascade model；Benna–Fusi 2016。
> **我们不抄它们的系数**，只借用"记忆寿命随突触数线性增长"这个定性结论做 sanity check。上面两条约束是自洽推导的。

#### 1.8.2 $r$ 的对照实验：一个预测被证实，一个被证伪

**~~原预测（已被 E0-d 证伪）~~**：几何阶梯是离散尺度不变系统 ⇒ 逼近 $t^{-1/2}$ 时应带周期为 $\ln r$ 的**对数周期修正** $m(t)\sim t^{-1/2}[1+A\cos(2\pi\ln t/\ln r+\phi)]$。

> **E0-d 判决：没有。** 裸脉冲响应（无采样噪声，六个数量级），在 $r = 2,4,8,16,32$ 的**每一个** $r$ 上，诱饵周期 $\sqrt2\ln r$ 的振幅都 ≥ 预测周期的振幅。$r=32$ 时诱饵 0.443±0.037（12σ）而预测周期 0.028±0.044（0.6σ）。方法的灵敏度是被证明了的，涟漪不在那里。
> 随 $r$ 增大确实出现残差结构，但它**在 $\ln t$ 上是非周期的**——是粗离散化对幂律的平滑畸变，不是振荡。

**实际成立的定量预测（E0-d 证实）**：指数就是 $-1/2$，且**不含任何自由参数**。

| schedule | 拟合斜率（$t\in[20,10^6]$） |
|---|---|
| uniform / m=1581 | **−0.4993** |
| geo / r=2 / m=12 | **−0.4991** |
| geo / r=4 / m=7 | **−0.5019** |
| geo / r=8 / m=5 | **−0.4993** |
| geo / r=16 / m=4 | −0.5203 |
| geo / r=32 / m=3 | −0.4597 |

**六个数量级内与 $-1/2$ 差 0.5%**，而且 uniform 用 1581 级、geo/r=8 只用 **5 级**，拿到同一个指数。这才是该写进论文的理论检验：一个零参数预测被确认到 0.5%。

**并且给出一条真实的设计约束**：$r \ge 16$ 时指数开始漂移（−0.520 / −0.460），阶梯不再是扩散的好逼近。**$r$ 的可用范围是 $2\!\sim\!8$**，这不是调出来的，是测出来的边界。

#### 1.8.3 注入深度 —— 对"$U_1$ 信噪比会不会炸"的回答

先澄清：$U_1$ **不是**原始更新流。它 = 输入在 $\tau_1$ 窗口内的低通 + $U_2$ 携带的全部沉淀。读它从来不是"只读噪声"。

但两股写入流的噪声性质根本不同：

| 写入流 | 性质 | 相干时间 | 注入位置 |
|---|---|---|---|
| delta-rule 联想写入 | **不是任何东西的估计量，它就是要存的内容** | ~1 个事件 | $U_1$，全强度 |
| 任务梯度 (batch=1) | $\nabla L$ 的高方差估计，单次大部分是噪声 | ~有效 batch size | 更深的 $U_j$ |

> **所以"梯度和 delta 写入的相对幅度"是个伪自由度。** 它们不共享幅度比，各自有一个**注入深度**；幅度被该级的 $\tau_j$ 吸收了。

**而且注入深度是测出来的，不是调出来的。** 阶梯本身就是一组二进滤波器组，$\lVert U_k - U_{k+1}\rVert^2$ 是更新流在 $[\tau_k,\tau_{k+1}]$ 带内的涨落功率。零模型：

> 白噪声驱动下，到达第 $k$ 级的电荷是在 $\tau_k$ 窗口内的随机游走（方差 $\propto \tau_k$），摊在容量 $C_k$ 上：
> $$\mathrm{Var}(U_k)\ \sim\ \sigma^2\,\tau_k\,/\,C_k^2$$
> 几何阶梯取 $\tau_k = r^{2k}/g_1$、$C_k = r^k$，两者恰好相消 ⇒ **谱是平的**。

画 $\log\lVert U_k-U_{k+1}\rVert^2$ vs $\log\tau_k$：**平坦段是纯噪声，鼓包处就是信号进入的尺度**，注入深度取那里。阶梯是自己的测量仪器，不用额外代码。

平基线比斜基线是更好的仪器——信号表现为鼓包而非斜率变化，肉眼即可定位。而且"每个时间尺度十倍频程分到等量涨落功率"正是离散尺度不变性的实证含义。

> **E0 实测（2026-08-06）**：geo/r=2/m=8 的 7 级，$\tau$ 从 2 跨到 8192（三个数量级），功率 9.9e-5 → 4.3e-5，拟合斜率 **+0.02**，平。均匀链 −1.16（它没有分离的时间尺度，那个斜率读的是梯度剖面而非频带功率，符合预期）。
> **未解释**：几何阶梯的绝对功率量级随 $r$ 上升（r=2 约 5e-5，r=8 约 1.9e-4），而 $\tau_k/C_k^2$ 预测它与 $r$ 无关。诊断只比较同一条阶梯内部各级，故不受影响，但这个偏差挂账。
>
> 早先本节写的是"斜率 $-1$"，那是错的：漏了 $C_k^2$ 项。E0-c 测出来了。

若实测 $U_1$ 的 SNR 仍不行，修法**不是读 $U_2$**（那会毁掉一次性记忆能力，而那是整个方案的立身之本），而是**调大 $\tau_1$**——同一张谱图直接告诉你调到哪。

- 对应实验：**E0 / A3**。

### 1.9 拓扑演化（突触生长与修剪）· 待验

上一代的 node splitting / subnode splitting / UCB→D-UCB→高斯TS→学生t TS 全部废弃。

**归因**：那一串在解探索-利用问题，而真实瓶颈是**秩饱和**——节点 $W$ 的容量 $\approx d_{head}$ 个模式，超了就干扰。这不需要探索去发现，它**可直接测量**：写入后 surprise 持续偏高 = 饱和。

- **生长**：持续高 surprise + 高流量的两个节点之间连边（网络级 Hebb：co-active 且残差相关）。候选按**距离衰减采样**（多数近邻 + 少量长程）。
- **修剪**：时间平均通过质量低于分位数的边剪掉。
- **总边数固定预算** ⇒ 算力恒定，拓扑自组织。剪掉多少就长多少，用分位数 ⇒ 尺度无关，无阈值超参。

小世界拓扑给短路径 —— 这是"不做任何全局操作却能全局整合"的唯一便宜办法。**长程边正是 lost-in-the-middle 的直接补丁。**

长程边按 $P(v)\propto \operatorname{dist}(u,v)^{-\alpha}$ 采样，$\alpha$ **默认取 2**（= 格点维数）。理由是 Kleinberg 2000：$\alpha=d$ 是**去中心化贪婪路由**唯一的多项对数点，而我们的路线 (b) 恰好就是去中心化贪婪（沿局部 surprise 下降，无全局视野）。
**但实测把这条主张收窄了，见 §8.6**：在 $N\le 2.6\times10^5$ 内多项对数区根本没到，$\alpha\in[1,2]$ 的跳数彼此相差 6% 以内。真正稳健的只有**上界 $\alpha\le2$**。所以 2 是对的默认值（渐近正确、有限尺寸下不吃亏），而**不是**被实测单独挑出来的。

- 待验：生长/修剪与路由学习会不会共振。→ **A5**（固定小世界 vs 动态演化）
- 注：如果最后涌现出一批高度数 hub 节点扮演快速整合器，很好，写进论文当发现。**但不要设计它。**

---

## 2. 为什么没有 all-reduce

不是工程 trick，是**参数本身就不共享，所以没有可 reduce 之物**。节点权重各自独立，梯度天然局部。

知识共享靠**局部拉普拉斯扩散**：$\dot W_i \propto \sum_{j \in N(i)}(W_j - W_i)$。纯局部，但实现了"系统性巩固"（知识从热点缓慢扩散到邻域）。物理上就是格点上的场扩散。

- 待验：这个扩散和 §1.8 的时间尺度阶梯是两个正交方向的扩散（空间 / 时间尺度）。两者叠加的稳定性没人验证过。

---

## 3. 工程约束（不可协商的三条）

1. **位级可复现。** rayon + 真异步 = 不可复现 = 研究地狱。做法：离散事件模拟 + 逻辑时钟分桶 → 桶内按目标节点分组 → 同节点内按 particle serial 排序（写入序确定）→ **不同节点之间并行**。确定性代价接近零，并行度仍是 $O(N_{active})$。
2. **手写 adjoint，不造 autodiff 框架。** 算子不超过 6 个，手写约 300 行，比玩具 autodiff 快一个数量级，且逼着每行想清楚。**有限差分梯度检查是一等测试**——这就是"每行精度极高"的可操作形式。
3. **诊断从第一天就埋。** 见 §5。上一代的痛苦有相当一部分来自"跑出来不知道为什么"。

**阶梯的惰性更新技巧**：只在节点被访问时推进它的阶梯，按经过的逻辑时间 $\Delta t$。线性链的传播算子可以对 $\Delta t = 2^j$ 预计算矩阵指数再复合 ⇒ **对数时间精确惰性更新**，不是近似。

### 代码布局（两个 crate，扁平，无 mod.rs）

```
annp-core/src/
  lib.rs
  linalg.rs     d_head 级小矩阵核，无泛型魔法
  particle.rs   Particle + 收集器
  ladder.rs     时间尺度阶梯（可独立测试，见 E0）
  graph.rs      环面网格、边、生长/修剪
  node.rs       W 的读/写/surprise/路由
  engine.rs     确定性事件调度 + rayon
  backward.rs   手写 adjoint（仅路线 (a) 需要）
  model.rs      embed / shatter / collect / unembed / loss
annp-cli/src/
  main.rs
  data.rs
```

---

## 4. 超参数清单（诚实版）

不宣称"零超参"，直接列表。

| 参数 | 性质 | 怎么定 |
|---|---|---|
| $d_{head}$ | **真超参** | 扫描。注意 $d_{model} = P \cdot d_{head}$ 是**运行时量**，$d_{head}$ 定了架构 |
| 网格边长 $G$ | **真超参** | 扫描。$N = G^2$ |
| 学习率 $\eta$ | **真超参** | 扫描 |
| 阶梯比 $r$ | **半超参** | 内存↔光滑度的分辨率旋钮，且有可证伪预测（§1.8.2） |
| 阶梯节数 $m$ | **导出** | $m = 1 + \ln T/(2\ln r)$ |
| 边预算 | **半超参** | 由算力预算反推 |
| top-k 的 $k$ | **推理期旋钮** | 非训练超参 |
| 质量地板 $\epsilon$ | **推理期旋钮** | anytime 算力预算 |
| 梯度注入深度 | **导出** | 由阶梯自身的功率谱测出（§1.8.3） |
| $P$（每 token 碎片数） | **推理期旋钮** | 节点对 slot 不可知 ⇒ 可训练后改变 |

**已被消灭的超参（不要再提回来）**：max_hops、遗忘率 $\lambda$、load-balance 损失权重、Adam 的 $\beta_1/\beta_2$（被阶梯吸收）、bandit 家族的全部参数、梯度/delta 的相对幅度（伪自由度）。

---

## 5. 实验计划

### E0 · 阶梯台架（**先做这个，两小时，可当场判死刑**）

完全脱离网络，阶梯是任何在线学习状态的**替换件**。
- 单个 $d\times d$ 联想矩阵，喂 key-value 流，重访间隔取无标度分布（Zipf）。
- 对比：无衰减单矩阵 / 指数衰减单矩阵（**扫遍 $\lambda$**）/ 阶梯。
- 测：固定容量下的保持曲线、最终可召回的不同 pattern 数。
- 顺带测：**对数周期涟漪**（§1.8.2）、**功率谱的 $-1$ 斜率零模型**（§1.8.3）。

> **判据：阶梯若赢不了扫过参的指数衰减，整个 1.8 节作废，成本两小时。**
> 若赢了且涟漪出现，后面所有工作都有理论底气，那张图可以当论文封面。

### E1 · byte-level 语言建模

评测协议用 **prequential（在线累积）log loss**，它逐字等于该语料的压缩码长（MDL）。
- 不可能数据泄漏，不需要留出测试集；
- **"持续学习"不再是附加实验，它就是唯一的实验**——因为架构本来就没有 train/test 之分。
- 对照：同 FLOPs 的小 Transformer（同样用 prequential 计分，对它公平）。

### E2 · 顺序多域持续学习

按域顺序喂（如 代码 → 散文 → 数学 → 代码），测 backward transfer / 旧域 loss 回升幅度。
对照：Transformer + 微调、Transformer + EWC/replay。

### E3 · needle-in-haystack（lost-in-the-middle）

检索准确率 vs. 目标在上下文中的位置。
**这是最能体现我们优势的实验，因为 §1.4 声称 recency bias 在结构上不存在。建议 E1 之后立刻做。**

### E4 · 尺度律验证（两条论文级主张）

- **每 token 算力与序列长度 $T$ 无关**（对 Transformer 的 $O(T^2)$）；
- **有效记忆容量 $\propto N$（节点数），而每 token 算力不变**——这是相对 linear attention（单一全局状态）的核心差异，也是最强的一条主张。

### 消融轴

| 编号 | 轴 | 对照 | 验证什么 |
|---|---|---|---|
| **A1** | 路由信用 | 全局梯度 vs 局部 surprise | **最重要**；结束组内梯度之争 |
| **A2** | 阶梯比 $r$ | $r=2,4,8$；$m$ 随之导出 | 对数周期涟漪；理论检验而非调参 |
| **A3** | 梯度注入深度 | 测出的深度 vs 固定 vs 全进 $U_1$ | §1.8.3 的 SNR 论证 |
| **A4** | 入射 | 内容寻址 vs 均匀随机 | 上一代最硬的教训是否归因正确 |
| **A5** | 拓扑 | 固定小世界 vs 生长/修剪 | 结构可塑性是否真的必要 |
| **A6** | 打碎 | 正交 vs 直接切片；mass∝模长 vs 均匀；tying vs 不 tying | 1.1 / 1.2 的三个假设 |
| **A7** | 可塑性规则 | delta rule vs Hebb+$\lambda$ | 噪声免疫与无 recency bias 的来源 |
| **A8** | 节点输出 | 路由预测 $u$ vs 路由残差 | §1.4 的分叉 |

### 从第一天就必须埋的诊断

- 每跳 mass 分布、跳数直方图 → 算力预算是否如预期
- **阶梯功率谱** $\lVert U_k-U_{k+1}\rVert^2$ vs $\tau_k$ → 注入深度、SNR
- 路由熵随时间、节点使用率 Gini → **塌缩检测**
- 每节点 surprise 分布 → 秩饱和检测（驱动 §1.9 的生长）
- 保持曲线 → 幂律 vs 指数
- （路线 (a) 才需要）**per-hop 梯度范数** → 见 §6 的头号风险

---

## 6. 已知风险（按严重度排）

1. **路由的梯度信号衰减**（路线 (a)）。质量守恒让前向很干净，但门控梯度会随 mass 一起衰减，深跳数处路由可能学不动。这是目前唯一没找到"第一性优雅解"的地方 → 从第一天埋 per-hop 梯度范数统计。路线 (b) 绕开了它，但换来别的风险。
2. **在线单遍学习的样本效率**。没有 epoch。阶梯深层的时间平均相当于"隐式 replay"，理论上补偿一部分，但这是**整个方案最大的实证风险**，E1 要早看。
3. ~~**强度换寿命是硬 trade-off**~~ —— **被 E0 推翻，见 §8.1。** 对单一时间尺度的系统成立（指数族内部确实走出一条 Pareto 前沿），但阶梯落在那条前沿之外：它同时拿到最快指数的初始 SNR 和数倍于最慢指数的寿命。机制是**噪声底与信号寿命被解耦**——干扰被持续刷新，故 $U_1$ 的噪声底由最快时间尺度决定；探针只写一次，其痕迹按阶梯的脉冲响应衰减，寿命由最慢时间尺度决定。单时间尺度系统没有这个自由度。
4. **路由 key 也挂阶梯的话会持续漂移**。可能被内稳态压住，也可能和 delta 写入共振。建议路由 key 的 $m$ 单独取小，并记录路由分布的时间自相关。
5. **空间扩散（§2）与时间尺度扩散（§1.8）叠加的稳定性**，无人验证过。

---

## 7. 建议的推进顺序

1. **E0 阶梯台架**（独立，两小时，可判死刑）
2. `linalg.rs` + `ladder.rs` + 有限差分梯度检查测试
3. `graph.rs` 固定小世界拓扑（先不做生长/修剪）
4. `node.rs` + `engine.rs`，路由先只实现**路线 (b)**（不需要 trace 和反向核，代码量小）
5. E1 跑通 prequential loss，与 Transformer 对齐 FLOPs
6. E3（lost-in-the-middle）—— 最能体现优势
7. 补路线 (a)，跑 **A1**
8. 打开拓扑演化，跑 A5

**不要在第 1 步之前写任何网络代码。**


---

## 8. E0 结果（2026-08-06）

复现：`cargo run --release -p annp-cli -- e0 --trials 256`
数据：`results/e0-n256/`，参数与 git 版本见同目录 `manifest.json`。

判据是"阶梯若赢不了扫过参的指数衰减，§1.8 作废"。**没作废，而且赢的方式比预期强。**

### 8.1 E0-a 保持曲线：阶梯落在指数族的 Pareto 前沿之外

$d=64$，探针写入后被新鲜随机模式持续掩埋，理想观察者 SNR，n=256。

| 方案 | SNR(age=1) | SNR>1 维持到 | 噪声基底 | 局部斜率 |
|---|---|---|---|---|
| plain（无衰减） | 5.63 | 129 | 300 | −0.693 |
| exp/τ=30 | 7.30 | 58 | 125 | −1.307 |
| exp/τ=1000 | 5.80 | 126 | 241 | −0.732 |
| exp/τ=30000 | 5.64 | 129 | 300 | −0.694 |
| **uniform/m=8** | **7.15** | **576** | 1392 | **−0.486** |
| **geo/r=4/m=4** | **7.19** | **409** | 4167 | −0.557 |
| **geo/r=2/m=8** | 7.26 | 319 | 3346 | −0.579 |
| **geo/r=8/m=3** | 7.20 | 260 | 6461 | −0.546 |

三条结论：

1. **指数族内部是一条干净的 Pareto 前沿**（τ 小 → 初始 SNR 高、寿命短），**阶梯落在这条前沿之外**：初始 SNR 追平最快的指数（7.2 vs 7.3），寿命是最好指数的 2.0–4.5 倍。这是论文里最该画的一张图（横轴寿命、纵轴初始 SNR，指数族连线，阶梯在线外）。
2. **均匀链测得 −0.486，与 $t^{-1/2}$ 差 3%**。均匀链就是一维扩散的直接离散化，所以这是对机制本身的确认，不是拟合。
3. **几何阶梯的尾更肥**：geo/r=8 的 SNR=1 穿越点只有 260，但噪声基底一直到 6461——弱痕迹保留得远比均匀链久（均匀链基底 1392）。均匀链是"高而短"，几何是"低而长"，与各自的时间尺度覆盖一致。

> **方法学警告**：8 trials 时同一批 geo/r=2 测出的寿命是 642，256 trials 下变成 319。8 trials 的穿越点落在噪声的向上涨落上。**噪声基底那一列必须看**，它是"此处之后全是采样噪声"的分界。

### 8.2 E0-b Zipf 容量：尾部可用秩约 2.1 倍

2000 个模式，Zipf(s=1)，30000 次写入后测全体召回。

| 方案 | 可用秩（桶均 SNR≥1） | rank 22–45 召回率 | 总召回率 |
|---|---|---|---|
| plain | 96 | 61% | 21.6% |
| exp/τ=1000 | 45 | 60% | 21.3% |
| geo/r=2/m=8 | **205** | **99%** | 27.2% |
| geo/r=8/m=3 | **205** | — | **28.7%** |

注意 geo/r=8 只有 **3 级**（内存 3×，不是 8×）就拿到了同样的可用秩和最好的总召回率。$m$ 由 §1.8.1 的公式导出，这个结果说明公式给的下界是够用的。

### 8.3 E0-c 白噪声功率谱：几何阶梯的谱是平的

见 §1.8.3 的更正。诊断仪器可用，零模型是"平"而不是 −1。

### 8.4 E0-d 裸脉冲响应：机制确认 + 一个预测被证伪

`--impulse-horizon 1000000`，数据 `results/e0-impulse/e0d_impulse.csv`。

E0-a 测的是**复合观测量**（delta-rule 记忆在持续刷新的干扰下的 SNR），干扰过程会抹掉阶梯本身可能有的结构。而 §1.8.2 预测的是**阶梯的响应**。所以直接测它：往 $U_1$ 打一个单位脉冲，看它怎么衰减。

动力学是线性且逐元素解耦的，所以 **1×1 矩阵就承载全部答案，且完全没有采样噪声**——唯一的不确定度是拟合残差。**这里看不到的东西，就不在阶梯里。**

**结论一（确认）**：斜率见 §1.8.2 的表，六个数量级内与 $-1/2$ 差 0.5%。这比 E0-a 的 −0.49 ~ −0.61 强得多，因为那里的偏差全部来自记忆任务而非阶梯。

**结论二（证伪）**：对数周期涟漪不存在。

| $r$ | amp @ $\ln r$ | amp @ 诱饵 $\sqrt2\ln r$ |
|---|---|---|
| 2 | 0.00018 ± 0.00025 | **0.00064** ± 0.00024 |
| 4 | 0.00510 ± 0.00471 | **0.01576** ± 0.00464 |
| 8 | 0.02259 ± 0.01407 | 0.01892 ± 0.01409 |
| 16 | 0.09912 ± 0.02810 | **0.13866** ± 0.02764 |
| 32 | 0.02804 ± 0.04382 | **0.44348** ± 0.03696 |

诱饵周期每一行都不输给预测周期，$r=32$ 时高 16 倍。**方法有灵敏度（它检出了 0.44），涟漪就是不在 $\ln r$ 上。**

> **误差棒的读法要收敛（CR 发现）**：`ripple_at` 的标准误 $\sqrt{2s^2/n}$ 假设去趋势列与正弦列近似正交，只在窗口跨越"若干个周期"时成立，代码注释自己写了"够做检出阈值，不够当发表用的误差棒"。$r=32$ 时拟合窗口 $[20,10^6]$ 只有 **3.1 个周期**，而这恰好是指数已经漂移（−0.46）、非周期曲率最强的那一档——曲率可能通过不完全正交混进正弦系数。
> 所以 **"12σ" 不该按标定过的显著性读**。"无涟漪"这个结论主要由小 $r$ 那几行（采样密、周期多）支撑，那部分是稳的。

我原来的推理是"离散尺度不变 ⇒ 复指数 ⇒ 对数周期修正"。对这条扩散阶梯不成立——粗离散化的效应表现为**指数偏移**（$r\ge16$ 时斜率漂到 −0.52 / −0.46），不是振荡。§1.8.2 已改写。

### 8.5 尚未验证

- **均匀 vs 几何谁更适合真实用途**。E0-a 的 horizon 分不开（两者寿命都受噪声基底截断而非覆盖范围截断）。E0-d 说明两者的**指数相同**，差别只在覆盖同样范围所需的级数（1581 vs 5）——从内存看几何完胜，但记忆任务上的形状差异（均匀"高而短"、几何"低而长"）还没有定论。
- **E0-c 中几何阶梯绝对功率量级随 $r$ 上升**，$\tau_k/C_k^2$ 预测与 $r$ 无关。挂账。

---

## 9. 拓扑结果（2026-08-07）

复现：`cargo run --release -p annp-cli -- topology --sides 64,128,256,512 --pairs 3000 --contacts 1,4`
数据：`results/topology-large/topology.csv`（另有 `results/topology/` 的小尺寸扫描）。

### 9.1 想验证什么

§1.9 声称长程边指数 $\alpha=2$ 是**由格点维数导出**而非调出来的。Kleinberg 2000 给了可检验形式（**注意原文以格点边长 $n$ 表述，$N=n^2$，换算到 $N$ 时每个指数要除以 2**；且 $\alpha\ne2$ 的是**任意**去中心化算法的**下界**，贪婪允许高于它）：

$$\alpha<2:\ \Omega(n^{(2-\alpha)/3}),\qquad \alpha=2:\ O(\log^2 n),\qquad \alpha>2:\ \Omega(n^{(\alpha-2)/(\alpha-1)})$$

### 9.2 实测（每节点 1 条长程边，拟合 hops $\sim N^\beta$）

| $\alpha$ | N=4096 | N=16384 | N=65536 | N=262144 | $\beta$ 实测 | $\beta$ 下界 |
|---|---|---|---|---|---|---|
| 0.0 | 15.51 | 25.30 | 39.97 | 64.89 | **0.343** | **0.333** |
| 1.0 | 14.50 | 22.12 | 32.93 | 47.62 | 0.286 | 0.167 |
| 1.5 | 14.61 | 21.76 | 30.85 | 42.38 | **0.256** | 0.083 |
| 2.0 | 15.27 | 22.73 | 33.38 | 44.98 | 0.261 | polylog |
| 2.5 | 17.24 | 27.56 | 43.27 | 65.46 | 0.321 | 0.167 |
| 3.0 | 19.06 | 33.60 | 60.43 | 106.04 | 0.414 | 0.250 |
| 4.0 | 22.31 | 43.82 | 85.98 | 167.96 | 0.485 | 0.333 |

**结论一（实现被验证）**：$\alpha=0$ 端实测 0.343 vs 下界 0.333，差 3%。贪婪在 $\alpha=0$ 已知接近最优，所以这一端是紧的，能当实现正确性的检验。其余各行实测 ≥ 下界，符合"下界"的含义。

**结论二（主张要收窄）**：$\beta$ 的极小值落在 $\alpha=1.5$（0.256），$\alpha=2$ 紧随（0.261），两者无法区分。而且 $\alpha=2$ 的多项对数区**没到**：

| N | 4096 | 16384 | 65536 | 262144 |
|---|---|---|---|---|
| hops / $(\ln N)^2$ | 0.2208 | 0.2414 | 0.2714 | 0.2890 |

1.8 个数量级内该比值上升 31%，仍在前渐近区。

**结论三（稳健的部分）**：$\alpha>2$ 明确变差且是单调的（$\beta$：0.26 → 0.32 → 0.41 → 0.49）。**真正被实测确立的是上界 $\alpha\le2$**，不是 $\alpha=2$ 这一点。

> 对"极少超参数"这个目标，这其实是好消息：$\alpha$ 在 $[1,2]$ 内**没有可调收益**（跳数差 6% 以内），所以它不是一个需要扫的超参，取 2 即可。

### 9.3 每节点长程边数（$\alpha=2$）

| 长程边数 | N=4096 | N=16384 | N=65536 | N=262144 |
|---|---|---|---|---|
| 1 | 15.27 | 22.73 | 33.38 | 44.98 |
| 4（默认） | **8.49** | 11.47 | 14.99 | **19.24** |

常数因子 2.3×，不改变标度区。**PoC 规模（$N=4096$，度数 4+4）下平均约 8.5 跳**——这就是 §1.6 算力公式里 $H$ 的量级，可以直接用来估每 token 的算力。

### 9.4 尚未做

- 生长/修剪（§1.9 的动态部分）还没写，当前只有固定小世界。按 §7 这是第 8 步。
- 贪婪路由用的是**格点距离**作为目标函数。真实系统里路由是沿 surprise 下降的，两者是否有同样的可导航性，只有接上 `node.rs` 之后才能测。这是 §9 结论能否迁移到实际系统的**关键未验证假设**。

---

## 10. 前向通路（2026-08-07）

`node.rs` + `engine.rs`。46 个测试全过，clippy 零警告。

### 10.1 为了让前向能跑，必须定下来的四件事

原 §1.4/§1.5 没写，实现时被迫决定，四条都记在 `node.rs` 的文件头注释里。

**① 全程 f64，payload 不用 f32。** 原 §0 的表写的是 `payload: [f32; D_HEAD]`。阶梯已被证明必须 f64，而在最热的循环里放一个混精度边界，正是静默 bug 最爱待的地方。窄化 payload 是**将来实测后的优化**，不是默认。§0 的表要改。

**② 节点输出必须带残差：$u = q + \varphi(Wq)$。** 原 §1.4 写的是 $u=\varphi(Wq)$。那样跑不起来——$W$ 从零开始，$\varphi(0)=0$，**每个 payload 一进节点就变成零向量，网络把自己的输入湮灭掉了，永远学不到东西**。加残差后，什么都不懂的节点是透明的，网络的初始状态是纯扩散。

**③ 路由没有任何参数。**（这条最值得组里看）
原 §1.5 的路线 (b) 给每条边一个 key $K_e$，由邻居回传的 surprise 标量训练——那需要一条反向通路，还要给 particle 加一个"来源边"字段，撞 §0 的红线。

**根本不需要。** 节点 $j$ 本来就在算 $W_j q_j$，也就是**它预期下一个会收到什么**。边 $(i\to j)$ 的打分直接用它：

$$\ell_{i\to j} = \langle \hat u_i,\ \widehat{W_j q_j}\rangle + b_{i\to j}$$

于是"particle 流向正在期待它的节点"就是路由本身，而不是要学出来的东西。**零路由参数、无反向通路、particle 不加字段。** 路线 (b) 想表达的变分图像，实现出来比设计时更简单。

**④ 吸收是固定参考点，logit 恒为 0。** 边的 logit 是单位向量内积，天然落在 $[-1,1]$，以 0 为中心。**没有任何地方在期待这个内容时，所有边都打不过参考点，粒子被吸收。** 停机不需要阈值，也不需要 max_hops。

内稳态保留，且是**对一个节点的所有出边严格零和**的再分配：它能在邻居之间挪质量，但**永远无法改变吸收率**。这条性质让"防塌缩"和"算力预算"彻底解耦——后者只由固定参考 logit 和质量地板决定。

### 10.2 确定性怎么做到的

§3 要求位级可复现，所以不能真异步。每个 tick：

1. 按 `(node, serial)` 排序活粒子；
2. **同一节点内串行**处理（delta rule 的写入不对易）；
3. **不同节点并行**（状态不相交，且路由读的是 tick 开始前冻结的预期快照）；
4. 按节点序合并子粒子，重新分配 serial。

节点并行的实现是"扫过全部 N 个节点、跳过空闲的"——这样 rayon 拿到的 `&mut Node` 天然不相交，跳过的分支代价相对一个 tick 的真实工作量可以忽略。

### 10.3 已验证

| 主张 | 出处 | 结果 |
|---|---|---|
| 质量严格守恒 | §1.6 | 每 token 吸收质量与注入相差 $<10^{-12}$ |
| 无 max_hops 也必然停机 | §1.6 | 单 token 全部吸收所需 tick < 200 |
| 活粒子数 $\le 1/\epsilon$ | §1.6 | 通过 |
| **任意线程数结果位级一致** | §3 | 1 线程 vs 8 线程，输出、质量、访问直方图逐位相同 |
| 重复运行一致 | §3 | 通过 |
| 路由确实流向"期待它的"节点 | §10.1 ③ | 零参数下通过 |

### 10.4 一个被证伪的前提

原打算测"关掉内稳态会塌缩、打开就不塌缩"。**没有塌缩可测**：未训练 + 均匀入射下，最忙的十分之一节点占 16% 的访问（完全均匀是 10%），开关内稳态只差不到半个百分点。

塌缩风险是**训练后**才可能出现的现象，现在测不了。所以：
- 内稳态的**机制**在节点级被钉住了（`homeostasis_forces_turn_taking`：单节点重复刺激下，不开内稳态时一条边 200/200 全赢，开了就必须轮流）；
- 它的**价值**仍然未验证，等接上真实数据再说。

### 10.5 下一步的已知缺口

- **入射还是均匀随机的**，§1.3 的内容寻址入射需要嵌入表，属于 `model.rs`。在那之前 §10.3 的负载数字不能当真。
- **每 token 算力与序列长度无关**（§1.6 的核心主张）还没测，需要真实入射才有意义。
- **路由用的是"预期匹配"，而 §9 的可导航性是用格点距离测的。** §9.4 标的那个关键假设现在有了可测的形式：把 §9 的贪婪路由换成本节的预期匹配路由，看跳数是否还是同量级。

---

## 11. 输入输出层（2026-08-07）

`model.rs`。59 个测试全过，clippy 零警告。§0 的精度已按 §10.1 ① 改成 f64。

### 11.1 建成的

- **打碎 = 固定正交旋转**：随机符号翻转 + Walsh-Hadamard，$O(n\log n)$，零参数。符号翻转不是装饰——纯 Hadamard 的首行全 1，会把嵌入的**均值**整块塞给 chunk 0。测试 `rotation_spreads_a_spike_evenly` 钉住了这一点。
- **方向进 payload、模长进 mass**（§1.2）。
- **内容寻址入射**：两个固定随机平面上的**相位**决定环面坐标（相位而非投影 ⇒ 天然尺度不变，无归一化常数）；slot 位移在半径 $\sqrt{P/\pi}$ 的圆盘内。测试确认：同一 token 永远同一入口、微扰不改变入口、8 个 slot 落在局部патch 而非堆在一点或撒满全图。
- **重组**：`assemble ∘ shatter` 在透明网络下**逐位精确恢复嵌入**（$<10^{-12}$），把分裂/重接的算术和网络本身隔离验证了。
- **输出头与嵌入表绑定**，损失梯度 $(p-y)\,e^\top$ **恰好是秩一外积**——正是阶梯的 `inject` 已经接受的形状，所以输出头**不需要任何自己的机构**。这是全系统唯一的反向传播。
- $P$（slots）确实是**运行时宽度**：`width_can_change_after_construction_without_touching_node_weights` 通过。

### 11.2 两个测出来的事实

**① 绑定权重的表，天生偏向预测"当前 token"。**
行是单位范数且近似正交，所以喂 token $X$ 时 $X$ 的 logit ≈ 1，其余 ≈ $\pm 1/\sqrt{d_{model}}$。**透明网络的预测就是"你刚看到的那个"**，损失 3.05 而均匀是 ln 64 = 4.16。这不是缺陷，是绑定的必然后果，但它是**语言模型必须先超越的基线**——写实验时不能把"比均匀好"当成学到了东西。

**② 阶梯把输出头的学习速度压到 18.5%。**
同样 200 步、同样学习率，$m=4,r=4$ 的阶梯只走完普通矩阵 **18.5%** 的损失下降（5.4× 慢）。原因是设计使然：梯度打进 rung 1 后向外泄漏，$\sum C_k = 85$，而最深一级的弛豫时间是 $r^6/g_1 = 8192$ 步——200 步远在暂态内。渐近学习率不变，被压的是暂态。

> **这和 §1.8.3 形成一个必须解决的张力。** §1.8.3 主张梯度应该注入**更深**的级以平均掉噪声。但现在测出来，**即使注入最浅一级，学习已经慢了 5.4 倍**；注入更深只会更慢。
> 所以"注入深度"不是一个单调的选择，而是**噪声平均 vs 学习速度**的权衡，需要一个实验定夺，不能靠推导。嵌入表的级数因此做成了独立参数（`embed_rungs`，取 1 即退化为普通矩阵），而不是照抄节点的 schedule。

### 11.3 还没有的

- **`data.rs` / 分词**。工作目录里的 `tokenizer.model` 是 SentencePiece protobuf，解析要么加依赖要么手写；暂时把 vocab 做成参数，用合成 token 流即可跑通全链路。
- **端到端驱动**（shatter → engine → assemble → learn 的循环）还没接起来，接上之后才能测 §1.6 的"每 token 算力与序列长度无关"和 §10.5 的三个缺口。

---

## 12. 端到端（2026-08-07）

`runtime.rs` + `annp run`。65 个测试全过，clippy 零警告。
复现：`cargo run --release -p annp-cli -- run --tokens 20000`（加 `--bypass` 跑对照）。

> ## ⚠ §12 的全部损失数字暂时失效，见 §12.7
>
> CR 发现 §12.1 只修了必要条件（结算顺序），没修充分条件（表示的独立性）。**bypass 对照不读节点状态、没有泄漏，网络跑读节点状态、有泄漏——所以 §12.3 的对比是偏向网络的。** 在 §12.7 那个实验做完之前，不要引用 §12.3/12.5 的任何数字。

### 12.1 一个协议级 bug，被测试抓到（只修对了一半）

**位置不是按顺序结算的**——跳数多的 token 落得晚。最初的实现是"谁先结算就先算分并更新"，后果是：位置 $p$ 的损失是在模型**已经吸收了位置 $p+1$ 的更新**之后测的，而那个更新依赖位置 $p+2$ 的 token。

**这会让 prequential 损失不再是压缩上界，§5 的整个评测协议静默失效。** 现在改成结算严格按流序：已结算的输出在 `ready` 里排队，直到它之前的位置全部算完。

### 12.2 §1.6 的核心主张成立

每 token 的节点访问次数，20000 token 的流：

| 窗口 | visits | 平均跳数 |
|---|---|---|
| 前 1/10 | 1392.7 | 7.38 |
| 中间 1/10 | 1358.0 | 7.21 |
| 后 1/10 | 1338.2 | 7.13 |

**平的（略降）。每 token 算力与序列位置无关**，这是直接测量不是论证。

> **单位更正（CR 发现）**：这一列是**节点访问次数**，不是边穿越次数。一个在入射节点就被吸收、一条边都没走的碎片计为 1。所以要和 §9 的 `greedy_hops`（数边，起终点相同时为 0）比较，必须减 1：**边等价值约 6.2**，对 §9 的 8.5。**§9.4 那个关键假设仍然初步过关**（同量级），但两个数字在原始形式下不是同一个量。

负载（576 节点，内容寻址入射）：**无一节点从未被访问**，最忙的十分之一占 15.5%（均匀是 10%），最忙的单节点占 0.33%（均匀是 0.17%）。没有热点。

### 12.3 学习效果：目前网络没有证明自己有价值

2×2 对照，20000 token，比特/token（越低越好）。`--bypass` 表示丢弃网络输出、直接用当前 token 的嵌入喂输出头，**其余一切（路由、可塑性、更新时序）完全相同**。

| | 阶梯输出头 (m=3) | 普通输出头 (m=1) |
|---|---|---|
| **网络** | 5.3515 | **4.3208** |
| **bypass 对照** | 5.1436 | 4.4182 |

参照：均匀 6.000，信源熵率 1.442。

**两条必须直说的结论：**

**① 用阶梯输出头时，网络比 bypass 更差**（5.352 vs 5.144，差 0.21 比特）。也就是说在那个配置下，把 token 送进网络绕一圈，**破坏的信息比增加的多**。

**② 用普通输出头时，网络仅在最后一个十分位险胜**（4.3208 vs 4.4182），但**整段 prequential 总量基本打平**（89674 vs 89643 比特，网络反而多 31 比特）。

**④ 但 2 万 token 是不够的。** 10 万 token 的长跑（普通输出头）：

| | 末十分位 | prequential 总量 |
|---|---|---|
| **网络** | **4.3150** | **433124** 比特 |
| **bypass** | 4.4618 | 443940 比特 |

**网络在两个指标上都赢了**：末十分位领先 0.147 比特，全程总量少 10816 比特（2.4%）。**12.3 ② 那个"打平"是暂态**——网络需要足够的数据才能盖过自己的初始化噪声，一旦盖过就持续拉开。

所以修正后的表述：**网络确实在加信息，但幅度不大。** 以"从均匀 6.000 降到信源熵 1.442"这 4.558 比特为可用空间：网络吃下 37%，bypass 吃下 34%——**净贡献 3 个百分点**。

（注意：跨长度比较必须用**同长度的末十分位**，因为十分位是相对的。10 万跑的首十分位是 1 万 token，2 万跑的首十分位是 2 千 token，两者不可比。）

**③ 阶梯压在输出头上代价约 0.8–1.0 比特**（4.32 vs 5.35）。这和 §11.2 ② 测到的 18.5% 暂态阻尼一致，是暂态而非渐近代价，但在 2 万 token 的尺度上它主导了一切。**`embed_rungs` 的默认值应该是 1**，除非能证明阶梯输出头买到了别的东西（比如持续学习下的稳健性）。

### 12.4 网络为什么可能在减分——待查的三个方向

不改设计，先记下来：

1. **每跳都在加噪声。** $u=q+\tanh(Wq)$，$W$ 未训练时 $\tanh(Wq)$ 接近噪声；7 跳之后 payload 已经离 $q$ 很远。
2. **质量分裂 + 同一 payload。** top-k 分裂出的子粒子携带**相同**的 emitted payload，最后重组是"同一碎片被不同路径变换后的加权平均"——平均本身就是模糊。
3. **节点学的转移可能大部分是噪声。**（我认为这条最可疑）一个节点看到的是**多个 token、多个 slot 交错**的碎片流，delta rule 学的是"这个节点上，什么碎片跟在什么碎片后面"。若交错顺序本身没有结构，这就是在拟合噪声。
   这直接关系到 §1.4 的"因果性由到达序涌现"——**到达序是一个被打散的混合流时，这个机制可能太弱**。

### 12.5 下一步

- 长跑结果（100k）判断 12.3 ② 的反超是否真实。
- **上真实语料前先做一遍 code review**（数理逻辑向）。
- 分词接 `sentencepiece` crate。

### 12.6 关于 bypass 对照本身的一个观察

bypass 的末十分位从 2 万跑的 4.4182 变成 10 万跑的 4.4618，**没有随数据变好**。原因大概是输出头绑定造成的双线性问题：同一张表既当输入嵌入又当输出映射，$E$ 出现在两处，梯度下降在这种非凸结构上很慢。这意味着 **bypass 是个会饱和的对照**，长跑下网络的领先幅度里有一部分来自对照饱和而非网络变强——报数据时要区分这两者。

### 12.7 泄漏还没修完：§12.1 只是必要条件

CR 指出 §12.1 的修法只序列化了**输出头**的算分与更新，没有触及产生 `output.accumulated` 的**节点表示**本身。

机制：token 是**设计上重叠在途**的（一 tick 注入一个，平均在途约 7 个）。节点的 `W` 是一份共享可变状态，谁的粒子落上去就更新谁的——**不区分 token**。于是位置 $p$ 的粒子在第 5 跳落到某节点时，那个节点的 `W` 可能已经被位置 $p+3$ 的粒子写过了。而 $p$ 的输出正是由这些 `W` 变换出来的，$p$ 的损失又是用来预测 $p+1$ 的。

**结果：预测 $p+1$ 时用到了 $p+3$ 的内容。** 真实的流式解码器在解 $p+1$ 时不可能被 $p+3$ 影响，所以算术编码的论证断了，prequential 损失不再是压缩上界。

**而且这让 §12.3 的对比失去意义，方向还是偏向网络的**（这一条 CR 没点出来，是更严重的部分）：
- `--bypass` 用的是 `embedding_of(token)`，只依赖嵌入表，而表**严格按位置序更新** ⇒ **无泄漏**；
- 网络跑读节点状态 ⇒ **有泄漏**。

所以网络那 2.4% 的领先里，有多少是真本事、有多少是偷看了未来，**目前无法区分**。

**要做的实验**：加一个"不重叠"模式——注入一个 token，跑到静默，算分，再注入下一个。这样算分时网络只见过 $\le p$ 的 token，泄漏归零。然后：

| | 重叠（现状） | 不重叠（无泄漏） |
|---|---|---|
| 网络 | 已测 4.3150 | ? |
| bypass | 已测 4.4618 | 应与重叠一致 |

**两者之差就是泄漏的大小**，不重叠那一列才是能写进论文的数字。

代价是失去"异步重叠"这个设计特征。但要注意：**跨 token 的到达序机制并没有因此消失**——节点仍然会先看到 $p$ 的碎片、后看到 $p+1$ 的碎片，§1.4 的"因果性由到达序涌现"依然成立，丢的只是"同时在途"。这一点值得组里确认我的理解对不对。

### 12.8 内稳态与吸收率并未解耦（§10.1 ④ 作废）

零和成立，但**由此推不出"不改变吸收率"**。偏置进入的是一个**经过 top-k 截断**、与固定吸收参考 logit 竞争的 softmax：零和的偏置移动可以把某条边压到参考点以下，让吸收挤进保留集。

反例：三条边 logit $(0.5, 0.5, -1.0)$，吸收固定 0，`top_k=2` ⇒ 吸收权重 0。加零和偏置 $(-0.6, +0.6, 0)$ ⇒ logit 变 $(-0.1, 1.1, -1.0)$，top-2 变成 $\{1.1, \text{吸收}\}$，**吸收权重 25%**。偏置无界且每个落选边每 tick 都在涨，所以这是常态不是构造。

**所以"防塌缩"和"算力预算"没有解耦，而且目前没有任何东西约束这个耦合。** §10.1 ④ 的说法已在 `node.rs` 注释里改正。现有测试只测了偏置和为零（真命题），没测吸收率不变（假命题）。
