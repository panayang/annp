# ANNP PoC — 设计共识快照 v0

> **这不是总设计方案。** 上一代的教训是"开头定死一个方案 → 思维固化 → 迭代 13 代还是不行"。
> 本文只记录**当前讨论到哪儿了**，每一节都标了置信度和对应的证伪实验。
> 标「分叉」的地方是明确留给大家吵的，标「待验」的是我们自己也不知道对不对的。
> 任何一节被实验推翻，改这份文档，不要迁就它写代码。

---

## 0. 一句话图像

> 前向传播 = 一群携带质量的准粒子，在一张自组织的图上做**带吸收态的守恒扩散**；
> 学习 = 所有权重矩阵内部的**多时间尺度扩散阶梯**在吸收更新流。
> 没有 train/inference 之分，没有 epoch，没有 fast/slow 模块之分。

三个量贯穿全文，先定义清楚：

| 量 | 定义 | 角色 |
|---|---|---|
| **payload** `[f64; d_head]` | 单位范数向量 | 内容 / 方向 |
| **mass** `f64` | L1 测度，全局守恒 | 路由测度 / 剪枝 / 算力预算 |
| **surprise** `f64` | 局部预测残差范数 $\lVert v - Wq\rVert$ | 可塑性 / 路由 / 结构生长的**唯一**信用信号 |

**设计红线：particle 上不允许出现第四个语义字段。** 任何"再加一个标记位就能解决"的提案，先检查能不能由这三个量推出来。
（`token: u32` / `slot: u16` 是重组用的路由标签，不是语义信息，节点看不到它们。）

---

## 1. 数据流逐段设计

### 1.1 嵌入 · 稳

token id → 查表得 $e \in \mathbb{R}^{d_{model}}$，且 $d_{model} = P \cdot d_{head}$。

**嵌入表与输出头权重绑定（weight tying）。** 这不是为了省参数，是为了让嵌入表**只从输出头的梯度学到**——这样即使我们最后选了"不跨图反传"的路线（见 1.5），嵌入表依然有明确的学习信号，不需要梯度穿过整张图。

- 待验：绑定后的嵌入向量作为**输入表示**是否够好。标准做法说够，但我们的用法不标准（要被打碎成正交片）。
- 对应实验：**A6**。

### 1.2 打碎 · 稳

$e$ 先过一个**固定的结构化正交变换**（Hadamard / butterfly，零参数零超参），再切成 $P$ 段，每段 $d_{head}$ 维。

正交的三个理由，都是第一性的：
1. Parseval ⇒ 能量守恒，每片能量均衡，不会出现"某一片全是零"；
2. 梯度等距，打碎/重组这一层不引入任何梯度缩放病态；
3. 零参数，不上超参表。

**碎片 → particle 的映射（方向与测度分离）：**

$$\texttt{payload}_s = \frac{c_s}{\lVert c_s\rVert}, \qquad \texttt{mass}_s = \frac{\lVert c_s\rVert}{\sum_t \lVert c_t\rVert}$$

即：**方向进 payload，模长进 mass**。两个好处——
- payload 永远单位范数 ⇒ 所有 $d_{head}$ 级矩阵乘法在固定数值量程里跑，几十跳之后也不会下溢/上溢。这是个纯工程但很关键的决定。
- 弱碎片天然 mass 小 ⇒ 被 1.6 的质量地板早早吸收 ⇒ **自适应算力是免费的**，不需要额外机制。

- 分叉：`mass ∝ 模长` vs `mass = 1/P 均匀`。前者更省算力，后者更无偏。→ **A6**

### 1.3 入射（内容寻址，非均匀）· 稳（这是上一代最硬的血泪教训）

节点住在 $G \times G$ 的二维环面上，$N = G^2$。**网格大小 $G$ 是我们承认的超参数之一。**

token 的入射坐标取**固定随机平面上的相位**：

$$\theta_1 = \operatorname{atan2}(\langle e, a_1\rangle, \langle e, a_2\rangle), \quad \theta_2 = \operatorname{atan2}(\langle e, b_1\rangle, \langle e, b_2\rangle)$$

$a_*, b_*$ 是初始化时固定的随机正交向量，**不训练**。取相位而非投影值，好处是**天然尺度不变**，不需要任何归一化常数。

第 $s$ 个 particle 落在 $(\theta_1,\theta_2) + \delta_s$，位移 $\delta_s$ 由 slot id 哈希得到，**分布在半径 $\sim\sqrt{P/\pi}$ 的圆盘内**——这个半径不是调的，是由"平均每个 slot 占一个节点"反推的。

为什么必须非均匀（这条要写进论文）：
> 均匀入射 = 最大熵输入分布 ⇒ 网络无法形成拓扑图谱 ⇒ 需要互相关联的记忆是**异址的** ⇒ particle 必须长途跋涉才能相遇 ⇒ 跳数暴涨。
> **局部性不是优化手段，它是联想记忆能工作的前提。**

- 对应实验：**A4**（内容寻址 vs 均匀随机入射），这条如果没拉开差距，说明我们对上一代的归因错了。

### 1.4 节点内部（FFN 与注意力已合并）· 稳（结构）/ 分叉（输出什么）

**一个节点 = 一个矩阵 $W \in \mathbb{R}^{d_{head}\times d_{head}}$ + 一个"上次输入"寄存器。** 没有别的。

- **读**：$u = \varphi(W q)$，$q$ = 入射 payload。
- **写**（delta rule，$q_{prev}$ = 该节点上次见到的 payload，$v$ = 本次）：
  $$W \mathrel{+}= \eta\,(v - W q_{prev})\,q_{prev}^\top$$
- **surprise**：$\sigma = \lVert v - W q_{prev}\rVert$。

这个形式的含义：**每个节点在线学习"预测自己下一个会看到什么"**，$W$ 是一个转移算子，$u$ 是它的预测。合并 FFN 与 attention 的具体形式就是这个——浅层时间尺度上 $W$ 是刚写入的键值对（=attention），深层沉淀下来的是稳定键值对（=FFN as key-value memory）。

**因果性从这里涌现**：$W$ 由到达序构建，读取只反映此前写入。它是**局部偏序**，不是全局下三角掩码。（这一点值得单开一节写进论文：单向注意力是全局同步假设的产物，不是因果性的必要形式。）

为什么用 delta rule 而不是 Hebb + $\lambda$：
- 只写"意外"，已能预测的内容不改写 ⇒ 抗干扰（**噪声免疫的机制解释**）；
- 不需要遗忘率 $\lambda$（少一个超参），也因此**结构上没有 recency bias ⇒ lost-in-the-middle 在原理上被消掉**；
- 免费产出 surprise 这个标量，后面三个机制全靠它。

- **分叉（重要）**：往下游路由的是**预测 $u$** 还是**残差 $v - Wq$**？
  - 路由预测：网络是分布式的下一状态预测器，语义连贯。（我倾向这个）
  - 路由残差：纯预测编码，"只传我解释不了的"，稀疏性更强，但**噪声是不可预测的，会被优先传播**——和噪声免疫的目标反向。
  - → **A8**
- 待验：$\varphi$ 取什么。倾向 GELU 或 tanh，先不纠结，但要记住 tanh 有天然的范数约束（对停机保证友好）。

### 1.5 路由 · 分叉（这是组内最大的分歧，做成可插拔）

节点对 `{邻居} ∪ {吸收}` 出一组 logits，softmax 后 **确定性 top-k 分裂**，mass 按权重分配（严格守恒，$\sum = 1$）。

$$\ell_e = \underbrace{\text{content}(u, e)}_{\text{两种实现}} + \underbrace{b_e}_{\text{内稳态电导}}$$

**$b_e$（两种实现共用）**：慢变标量，随该边使用率**下降**（intrinsic plasticity 式负反馈）。
这是用来防路由塌缩的，**替代 MoE 的 load-balance 辅助损失**——那是一个损失项加一个权重超参，这个是纯局部负反馈，零超参。

**content 项的两种实现（enum 切换，接口相同）：**

| | (a) 全局梯度路线 | (b) 局部预期路线 **（已实现，见 §10）** |
|---|---|---|
| 形式 | $\langle u, K_e\rangle$，$K_e$ 由任务损失经门控直通梯度训练 | $\langle u,\ \widehat{W_j q_j}\rangle$ —— **没有 $K_e$，没有任何路由参数** |
| 信用来源 | 误差沿 routing trace 反传 | 无需信用分配：邻居 $j$ 本来就在算 $W_j q_j$（它预期的下一个输入），直接拿来当 key |
| 变分解释 | — | 自由能下降流：particle 流向"已经认识它"的节点 |
| 工程量 | 一个反向核。**不需要 trace 存储**——§3 的位级确定性意味着重放前向即可精确重建，确定性是为可复现性买的，顺手把这里的内存成本降到零 | 不需要反向核，代码量更小 |
| 风险 | 门控梯度随 mass 一起衰减，深跳处可能学不动（§6 风险 1）。**这个量可以在没有网络的情况下单独测**——搭一条 $k$ 跳门控链，看 $\partial L/\partial K_e$ 的范数随跳深的标度。见 A1-pre | 纯局部预测编码在难任务上历史战绩一般 |

**A1-pre（在写反向核之前做）**：门控梯度衰减测量。指数衰减 ⇒ 带着数据否掉 (a)；不衰减 ⇒ (a) 值得那个反向核。这是 E0-d 式的做法：不靠意见决定，也不先付全部工程成本，只测决定生死的那一个量。

**这一条不站队，用数据结束争论。**(b) 的副作用恰好是我们要的：particle 自动流向已经认识它的节点 ⇒ 内容寻址和拓扑图谱**自组织**，不用额外造机制。而且 (b) 工程量更小，**建议先写 (b)**。

- 对应实验：**A1**，这是整个项目最重要的一次对照。

### 1.6 停机与算力预算 · 稳

**没有 max_hops。** 质量守恒 + 吸收伪边概率非零 ⇒ 吸收 Markov 链 ⇒ 几乎必然停机，可证明。

**相对质量地板 $\epsilon$**：mass < $\epsilon$ 的 particle 直接吸收。于是每 token 的 particle 数上界 $\approx 1/\epsilon$。

$\epsilon$ **不是训练超参，是推理期的 anytime 算力旋钮**——调大就更快更糙。这直接给出长上下文的算力答案：

$$\text{每 token 算力} = O(P \cdot H \cdot d_{head}^2), \quad \textbf{与序列长度无关}$$

- 默认**确定性 top-k**，不采样。可复现性是硬约束（见 §3）。采样版留作 ablation。

### 1.7 重组与输出头 · 稳

被吸收的 particle 按 `(token, slot)` 累加：$\hat c_s = \sum_i m_i \cdot \texttt{payload}_i \cdot Z$（$Z$ = 打碎时的模长和）。
拼成 $\hat e$ → 逆正交变换 → 与嵌入表相乘得 logits → 与下一个 token 算交叉熵。

**这是整个系统里唯一存在反向传播的地方**（路线 (b) 下），而且它只穿过一个矩阵乘法。

### 1.8 可塑性阶梯（记忆巩固）· 待验 —— 全设计里风险最高的一块

每个可训练矩阵不是一个矩阵，而是一列同形状变量 $U_1,\dots,U_m$：

1. **所有更新只写进 $U_1$**（不管来自 delta rule 还是梯度——见下面的注入深度）；
2. **相邻层扩散**：$C_k \dot U_k = g_{k-1}(U_{k-1}-U_k) + g_k(U_{k+1}-U_k)$；
3. **前向只读 $U_1$** ⇒ **前向算力完全不变，只多内存**。

**为什么没有"沉淀机制"这个模块**：沉淀就是扩散的自发行为。一次性写入被 $U_2$ 拉回、痕迹消失；反复写入逐层深入、存下来。**重复即沉淀**，筛选器是免费且正确的。不需要 replay buffer、不需要 EWC、不需要触发判据。

**为什么幂律**：均匀链 = 一维扩散的离散化，原点 Green 函数 $\sim(4\pi Dt)^{-1/2}$ ⇒ 单次痕迹按 $t^{-1/2}$ 衰减。
指数衰减承诺了一个特征时间尺度（= 一个必然错的超参），幂律是无标度的。**这是我们对"长上下文遗忘"的正面回答**：不是不遗忘，是以无特征尺度的方式遗忘。

**为什么不该有 fast/slow 两个模块**：时间尺度分离发生在**突触内部**，不在模块之间。$U_1 \approx$ 沉淀 + 近期，读一个矩阵就同时拿到长期知识和情节记忆。没有边界可跨，所以没有"迁移机制"要设计。
（这是对"不该每个节点都有海马"的直接回应：**没有任何节点有海马**，海马-皮层只是这个谱的一种空间实现，不是它的定义。）

#### 1.8.1 常数怎么来（不抄任何人）

- **约束一（尺度不变）** $\tau_k/\tau_{k-1} = r$ ⇒ $C_k \propto r^k,\ g_k \propto r^{-k},\ \tau_k \propto r^{2(k-1)}$
- **约束二（覆盖）** $\tau_m \ge T$（$T$ = 要求的最长保持跨度，token 数）

$$\boxed{\,m = 1 + \frac{\ln T}{2\ln r}\,}$$

**$m$ 不是自由参数**，由记忆跨度算出。真正自由的只有 $r$（内存 ↔ 衰减曲线光滑度的分辨率旋钮）。$T=10^6$ 时：$r=4 \Rightarrow m\approx6$；$r=2 \Rightarrow m\approx11$。

> 文献锚点：Fusi–Drew–Abbott cascade model；Benna–Fusi 2016。
> **我们不抄它们的系数**，只借用"记忆寿命随突触数线性增长"这个定性结论做 sanity check。上面两条约束是自洽推导的。

#### 1.8.2 $r$ 的对照实验：一个预测被证实，一个被证伪

**~~原预测（已被 E0-d 证伪）~~**：几何阶梯是离散尺度不变系统 ⇒ 逼近 $t^{-1/2}$ 时应带周期为 $\ln r$ 的**对数周期修正** $m(t)\sim t^{-1/2}[1+A\cos(2\pi\ln t/\ln r+\phi)]$。

> **E0-d 判决：没有。** 裸脉冲响应（无采样噪声，六个数量级），在 $r = 2,4,8,16,32$ 的**每一个** $r$ 上，诱饵周期 $\sqrt2\ln r$ 的振幅都 ≥ 预测周期的振幅。$r=32$ 时诱饵 0.443±0.037（12σ）而预测周期 0.028±0.044（0.6σ）。方法的灵敏度是被证明了的，涟漪不在那里。
> 随 $r$ 增大确实出现残差结构，但它**在 $\ln t$ 上是非周期的**——是粗离散化对幂律的平滑畸变，不是振荡。

**实际成立的定量预测（E0-d 证实）**：指数就是 $-1/2$，且**不含任何自由参数**。

| schedule | 拟合斜率（$t\in[20,10^6]$） |
|---|---|
| uniform / m=1581 | **−0.4993** |
| geo / r=2 / m=12 | **−0.4991** |
| geo / r=4 / m=7 | **−0.5019** |
| geo / r=8 / m=5 | **−0.4993** |
| geo / r=16 / m=4 | −0.5203 |
| geo / r=32 / m=3 | −0.4597 |

**六个数量级内与 $-1/2$ 差 0.5%**，而且 uniform 用 1581 级、geo/r=8 只用 **5 级**，拿到同一个指数。这才是该写进论文的理论检验：一个零参数预测被确认到 0.5%。

**并且给出一条真实的设计约束**：$r \ge 16$ 时指数开始漂移（−0.520 / −0.460），阶梯不再是扩散的好逼近。**$r$ 的可用范围是 $2\!\sim\!8$**，这不是调出来的，是测出来的边界。

#### 1.8.3 注入深度 —— 对"$U_1$ 信噪比会不会炸"的回答

先澄清：$U_1$ **不是**原始更新流。它 = 输入在 $\tau_1$ 窗口内的低通 + $U_2$ 携带的全部沉淀。读它从来不是"只读噪声"。

但两股写入流的噪声性质根本不同：

| 写入流 | 性质 | 相干时间 | 注入位置 |
|---|---|---|---|
| delta-rule 联想写入 | **不是任何东西的估计量，它就是要存的内容** | ~1 个事件 | $U_1$，全强度 |
| 任务梯度 (batch=1) | $\nabla L$ 的高方差估计，单次大部分是噪声 | ~有效 batch size | 更深的 $U_j$ |

> **所以"梯度和 delta 写入的相对幅度"是个伪自由度。** 它们不共享幅度比，各自有一个**注入深度**；幅度被该级的 $\tau_j$ 吸收了。

**而且注入深度是测出来的，不是调出来的。** 阶梯本身就是一组二进滤波器组，$\lVert U_k - U_{k+1}\rVert^2$ 是更新流在 $[\tau_k,\tau_{k+1}]$ 带内的涨落功率。零模型：

> 白噪声驱动下，到达第 $k$ 级的电荷是在 $\tau_k$ 窗口内的随机游走（方差 $\propto \tau_k$），摊在容量 $C_k$ 上：
> $$\mathrm{Var}(U_k)\ \sim\ \sigma^2\,\tau_k\,/\,C_k^2$$
> 几何阶梯取 $\tau_k = r^{2k}/g_1$、$C_k = r^k$，两者恰好相消 ⇒ **谱是平的**。

画 $\log\lVert U_k-U_{k+1}\rVert^2$ vs $\log\tau_k$：**平坦段是纯噪声，鼓包处就是信号进入的尺度**，注入深度取那里。阶梯是自己的测量仪器，不用额外代码。

平基线比斜基线是更好的仪器——信号表现为鼓包而非斜率变化，肉眼即可定位。而且"每个时间尺度十倍频程分到等量涨落功率"正是离散尺度不变性的实证含义。

> **E0 实测（2026-08-06）**：geo/r=2/m=8 的 7 级，$\tau$ 从 2 跨到 8192（三个数量级），功率 9.9e-5 → 4.3e-5，拟合斜率 **+0.02**，平。均匀链 −1.16（它没有分离的时间尺度，那个斜率读的是梯度剖面而非频带功率，符合预期）。
> **未解释**：几何阶梯的绝对功率量级随 $r$ 上升（r=2 约 5e-5，r=8 约 1.9e-4），而 $\tau_k/C_k^2$ 预测它与 $r$ 无关。诊断只比较同一条阶梯内部各级，故不受影响，但这个偏差挂账。
>
> 早先本节写的是"斜率 $-1$"，那是错的：漏了 $C_k^2$ 项。E0-c 测出来了。

若实测 $U_1$ 的 SNR 仍不行，修法**不是读 $U_2$**（那会毁掉一次性记忆能力，而那是整个方案的立身之本），而是**调大 $\tau_1$**——同一张谱图直接告诉你调到哪。

- 对应实验：**E0 / A3**。

### 1.9 拓扑演化（突触生长与修剪）· 待验

上一代的 node splitting / subnode splitting / UCB→D-UCB→高斯TS→学生t TS 全部废弃。

**归因**：那一串在解探索-利用问题，而真实瓶颈是**秩饱和**——节点 $W$ 的容量 $\approx d_{head}$ 个模式，超了就干扰。这不需要探索去发现，它**可直接测量**：写入后 surprise 持续偏高 = 饱和。

- **生长**：持续高 surprise + 高流量的两个节点之间连边（网络级 Hebb：co-active 且残差相关）。候选按**距离衰减采样**（多数近邻 + 少量长程）。
- **修剪**：时间平均通过质量低于分位数的边剪掉。
- **总边数固定预算** ⇒ 算力恒定，拓扑自组织。剪掉多少就长多少，用分位数 ⇒ 尺度无关，无阈值超参。

小世界拓扑给短路径 —— 这是"不做任何全局操作却能全局整合"的唯一便宜办法。**长程边正是 lost-in-the-middle 的直接补丁。**

长程边按 $P(v)\propto \operatorname{dist}(u,v)^{-\alpha}$ 采样，$\alpha$ **默认取 2**（= 格点维数）。理由是 Kleinberg 2000：$\alpha=d$ 是**去中心化贪婪路由**唯一的多项对数点，而我们的路线 (b) 恰好就是去中心化贪婪（沿局部 surprise 下降，无全局视野）。
**但实测把这条主张收窄了，见 §8.6**：在 $N\le 2.6\times10^5$ 内多项对数区根本没到，$\alpha\in[1,2]$ 的跳数彼此相差 6% 以内。真正稳健的只有**上界 $\alpha\le2$**。所以 2 是对的默认值（渐近正确、有限尺寸下不吃亏），而**不是**被实测单独挑出来的。

- 待验：生长/修剪与路由学习会不会共振。→ **A5**（固定小世界 vs 动态演化）
- 注：如果最后涌现出一批高度数 hub 节点扮演快速整合器，很好，写进论文当发现。**但不要设计它。**

---

## 2. 为什么没有 all-reduce

不是工程 trick，是**参数本身就不共享，所以没有可 reduce 之物**。节点权重各自独立，梯度天然局部。

知识共享靠**局部拉普拉斯扩散**：$\dot W_i \propto \sum_{j \in N(i)}(W_j - W_i)$。纯局部，但实现了"系统性巩固"（知识从热点缓慢扩散到邻域）。物理上就是格点上的场扩散。

- 待验：这个扩散和 §1.8 的时间尺度阶梯是两个正交方向的扩散（空间 / 时间尺度）。两者叠加的稳定性没人验证过。

---

## 3. 工程约束（不可协商的三条）

1. **位级可复现。** rayon + 真异步 = 不可复现 = 研究地狱。做法：离散事件模拟 + 逻辑时钟分桶 → 桶内按目标节点分组 → 同节点内按 particle serial 排序（写入序确定）→ **不同节点之间并行**。确定性代价接近零，并行度仍是 $O(N_{active})$。
2. **手写 adjoint，不造 autodiff 框架。** 算子不超过 6 个，手写约 300 行，比玩具 autodiff 快一个数量级，且逼着每行想清楚。**有限差分梯度检查是一等测试**——这就是"每行精度极高"的可操作形式。
3. **诊断从第一天就埋。** 见 §5。上一代的痛苦有相当一部分来自"跑出来不知道为什么"。

**阶梯的惰性更新技巧**：只在节点被访问时推进它的阶梯，按经过的逻辑时间 $\Delta t$。线性链的传播算子可以对 $\Delta t = 2^j$ 预计算矩阵指数再复合 ⇒ **对数时间精确惰性更新**，不是近似。

### 代码布局（两个 crate，扁平，无 mod.rs）

```
annp-core/src/
  lib.rs
  linalg.rs     d_head 级小矩阵核，无泛型魔法
  particle.rs   Particle + 收集器
  ladder.rs     时间尺度阶梯（可独立测试，见 E0）
  graph.rs      环面网格、边、生长/修剪
  node.rs       W 的读/写/surprise/路由
  engine.rs     确定性事件调度 + rayon
  backward.rs   手写 adjoint（仅路线 (a) 需要）
  model.rs      embed / shatter / collect / unembed / loss
annp-cli/src/
  main.rs
  data.rs
```

---

## 4. 超参数清单（诚实版）

不宣称"零超参"，直接列表。

| 参数 | 性质 | 怎么定 |
|---|---|---|
| $d_{head}$ | **真超参** | 扫描。注意 $d_{model} = P \cdot d_{head}$ 是**运行时量**，$d_{head}$ 定了架构 |
| 网格边长 $G$ | **真超参** | 扫描。$N = G^2$ |
| 学习率 $\eta$ | **真超参** | 扫描 |
| 阶梯比 $r$ | **半超参** | 内存↔光滑度的分辨率旋钮，且有可证伪预测（§1.8.2） |
| 阶梯节数 $m$ | **导出** | $m = 1 + \ln T/(2\ln r)$ |
| 边预算 | **半超参** | 由算力预算反推 |
| top-k 的 $k$ | **推理期旋钮** | 非训练超参 |
| 质量地板 $\epsilon$ | **推理期旋钮** | anytime 算力预算 |
| 梯度注入深度 | **导出** | 由阶梯自身的功率谱测出（§1.8.3） |
| $P$（每 token 碎片数） | **推理期旋钮** | 节点对 slot 不可知 ⇒ 可训练后改变 |

**已被消灭的超参（不要再提回来）**：max_hops、遗忘率 $\lambda$、load-balance 损失权重、Adam 的 $\beta_1/\beta_2$（被阶梯吸收）、bandit 家族的全部参数、梯度/delta 的相对幅度（伪自由度）。

---

## 5. 实验计划

### E0 · 阶梯台架（**先做这个，两小时，可当场判死刑**）

完全脱离网络，阶梯是任何在线学习状态的**替换件**。
- 单个 $d\times d$ 联想矩阵，喂 key-value 流，重访间隔取无标度分布（Zipf）。
- 对比：无衰减单矩阵 / 指数衰减单矩阵（**扫遍 $\lambda$**）/ 阶梯。
- 测：固定容量下的保持曲线、最终可召回的不同 pattern 数。
- 顺带测：**对数周期涟漪**（§1.8.2）、**功率谱的 $-1$ 斜率零模型**（§1.8.3）。

> **判据：阶梯若赢不了扫过参的指数衰减，整个 1.8 节作废，成本两小时。**
> 若赢了且涟漪出现，后面所有工作都有理论底气，那张图可以当论文封面。

### E1 · byte-level 语言建模

评测协议用 **prequential（在线累积）log loss**，它逐字等于该语料的压缩码长（MDL）。
- 不可能数据泄漏，不需要留出测试集；
- **"持续学习"不再是附加实验，它就是唯一的实验**——因为架构本来就没有 train/test 之分。
- 对照：同 FLOPs 的小 Transformer（同样用 prequential 计分，对它公平）。

### E2 · 顺序多域持续学习

按域顺序喂（如 代码 → 散文 → 数学 → 代码），测 backward transfer / 旧域 loss 回升幅度。
对照：Transformer + 微调、Transformer + EWC/replay。

### E3 · needle-in-haystack（lost-in-the-middle）

检索准确率 vs. 目标在上下文中的位置。
**这是最能体现我们优势的实验，因为 §1.4 声称 recency bias 在结构上不存在。建议 E1 之后立刻做。**

### E4 · 尺度律验证（两条论文级主张）

- **每 token 算力与序列长度 $T$ 无关**（对 Transformer 的 $O(T^2)$）；
- **有效记忆容量 $\propto N$（节点数），而每 token 算力不变**——这是相对 linear attention（单一全局状态）的核心差异，也是最强的一条主张。

### 消融轴

| 编号 | 轴 | 对照 | 验证什么 |
|---|---|---|---|
| **A1** | 路由信用 | 全局梯度 vs 局部 surprise | **最重要**；结束组内梯度之争 |
| **A2** | 阶梯比 $r$ | $r=2,4,8$；$m$ 随之导出 | 对数周期涟漪；理论检验而非调参 |
| **A3** | 梯度注入深度 | 测出的深度 vs 固定 vs 全进 $U_1$ | §1.8.3 的 SNR 论证 |
| **A4** | 入射 | 内容寻址 vs 均匀随机 | 上一代最硬的教训是否归因正确 |
| **A5** | 拓扑 | 固定小世界 vs 生长/修剪 | 结构可塑性是否真的必要 |
| **A6** | 打碎 | 正交 vs 直接切片；mass∝模长 vs 均匀；tying vs 不 tying | 1.1 / 1.2 的三个假设 |
| **A7** | 可塑性规则 | delta rule vs Hebb+$\lambda$ | 噪声免疫与无 recency bias 的来源 |
| **A8** | 节点输出 | 路由预测 $u$ vs 路由残差 | §1.4 的分叉 |

### 从第一天就必须埋的诊断

- 每跳 mass 分布、跳数直方图 → 算力预算是否如预期
- **阶梯功率谱** $\lVert U_k-U_{k+1}\rVert^2$ vs $\tau_k$ → 注入深度、SNR
- 路由熵随时间、节点使用率 Gini → **塌缩检测**
- 每节点 surprise 分布 → 秩饱和检测（驱动 §1.9 的生长）
- 保持曲线 → 幂律 vs 指数
- （路线 (a) 才需要）**per-hop 梯度范数** → 见 §6 的头号风险

---

## 6. 已知风险（按严重度排）

1. **路由的梯度信号衰减**（路线 (a)）。质量守恒让前向很干净，但门控梯度会随 mass 一起衰减，深跳数处路由可能学不动。这是目前唯一没找到"第一性优雅解"的地方 → 从第一天埋 per-hop 梯度范数统计。路线 (b) 绕开了它，但换来别的风险。
2. **在线单遍学习的样本效率**。没有 epoch。阶梯深层的时间平均相当于"隐式 replay"，理论上补偿一部分，但这是**整个方案最大的实证风险**，E1 要早看。
3. ~~**强度换寿命是硬 trade-off**~~ —— **被 E0 推翻，见 §8.1。** 对单一时间尺度的系统成立（指数族内部确实走出一条 Pareto 前沿），但阶梯落在那条前沿之外：它同时拿到最快指数的初始 SNR 和数倍于最慢指数的寿命。机制是**噪声底与信号寿命被解耦**——干扰被持续刷新，故 $U_1$ 的噪声底由最快时间尺度决定；探针只写一次，其痕迹按阶梯的脉冲响应衰减，寿命由最慢时间尺度决定。单时间尺度系统没有这个自由度。
4. **路由 key 也挂阶梯的话会持续漂移**。可能被内稳态压住，也可能和 delta 写入共振。建议路由 key 的 $m$ 单独取小，并记录路由分布的时间自相关。
5. **空间扩散（§2）与时间尺度扩散（§1.8）叠加的稳定性**，无人验证过。

---

## 7. 建议的推进顺序

1. **E0 阶梯台架**（独立，两小时，可判死刑）
2. `linalg.rs` + `ladder.rs` + 有限差分梯度检查测试
3. `graph.rs` 固定小世界拓扑（先不做生长/修剪）
4. `node.rs` + `engine.rs`，路由先只实现**路线 (b)**（不需要 trace 和反向核，代码量小）
5. E1 跑通 prequential loss，与 Transformer 对齐 FLOPs
6. E3（lost-in-the-middle）—— 最能体现优势
7. 补路线 (a)，跑 **A1**
8. 打开拓扑演化，跑 A5

**不要在第 1 步之前写任何网络代码。**


---

## 8. E0 结果（2026-08-06）

复现：`cargo run --release -p annp-cli -- e0 --trials 256`
数据：`results/e0-n256/`，参数与 git 版本见同目录 `manifest.json`。

判据是"阶梯若赢不了扫过参的指数衰减，§1.8 作废"。**没作废，而且赢的方式比预期强。**

### 8.1 E0-a 保持曲线：阶梯落在指数族的 Pareto 前沿之外

$d=64$，探针写入后被新鲜随机模式持续掩埋，理想观察者 SNR，n=256。

| 方案 | SNR(age=1) | SNR>1 维持到 | 噪声基底 | 局部斜率 |
|---|---|---|---|---|
| plain（无衰减） | 5.63 | 129 | 300 | −0.693 |
| exp/τ=30 | 7.30 | 58 | 125 | −1.307 |
| exp/τ=1000 | 5.80 | 126 | 241 | −0.732 |
| exp/τ=30000 | 5.64 | 129 | 300 | −0.694 |
| **uniform/m=8** | **7.15** | **576** | 1392 | **−0.486** |
| **geo/r=4/m=4** | **7.19** | **409** | 4167 | −0.557 |
| **geo/r=2/m=8** | 7.26 | 319 | 3346 | −0.579 |
| **geo/r=8/m=3** | 7.20 | 260 | 6461 | −0.546 |

三条结论：

1. **指数族内部是一条干净的 Pareto 前沿**（τ 小 → 初始 SNR 高、寿命短），**阶梯落在这条前沿之外**：初始 SNR 追平最快的指数（7.2 vs 7.3），寿命是最好指数的 2.0–4.5 倍。这是论文里最该画的一张图（横轴寿命、纵轴初始 SNR，指数族连线，阶梯在线外）。
2. **均匀链测得 −0.486，与 $t^{-1/2}$ 差 3%**。均匀链就是一维扩散的直接离散化，所以这是对机制本身的确认，不是拟合。
3. **几何阶梯的尾更肥**：geo/r=8 的 SNR=1 穿越点只有 260，但噪声基底一直到 6461——弱痕迹保留得远比均匀链久（均匀链基底 1392）。均匀链是"高而短"，几何是"低而长"，与各自的时间尺度覆盖一致。

> **方法学警告**：8 trials 时同一批 geo/r=2 测出的寿命是 642，256 trials 下变成 319。8 trials 的穿越点落在噪声的向上涨落上。**噪声基底那一列必须看**，它是"此处之后全是采样噪声"的分界。

### 8.2 E0-b Zipf 容量：尾部可用秩约 2.1 倍

2000 个模式，Zipf(s=1)，30000 次写入后测全体召回。

| 方案 | 可用秩（桶均 SNR≥1） | rank 22–45 召回率 | 总召回率 |
|---|---|---|---|
| plain | 96 | 61% | 21.6% |
| exp/τ=1000 | 45 | 60% | 21.3% |
| geo/r=2/m=8 | **205** | **99%** | 27.2% |
| geo/r=8/m=3 | **205** | — | **28.7%** |

注意 geo/r=8 只有 **3 级**（内存 3×，不是 8×）就拿到了同样的可用秩和最好的总召回率。$m$ 由 §1.8.1 的公式导出，这个结果说明公式给的下界是够用的。

### 8.3 E0-c 白噪声功率谱：几何阶梯的谱是平的

见 §1.8.3 的更正。诊断仪器可用，零模型是"平"而不是 −1。

### 8.4 E0-d 裸脉冲响应：机制确认 + 一个预测被证伪

`--impulse-horizon 1000000`，数据 `results/e0-impulse/e0d_impulse.csv`。

E0-a 测的是**复合观测量**（delta-rule 记忆在持续刷新的干扰下的 SNR），干扰过程会抹掉阶梯本身可能有的结构。而 §1.8.2 预测的是**阶梯的响应**。所以直接测它：往 $U_1$ 打一个单位脉冲，看它怎么衰减。

动力学是线性且逐元素解耦的，所以 **1×1 矩阵就承载全部答案，且完全没有采样噪声**——唯一的不确定度是拟合残差。**这里看不到的东西，就不在阶梯里。**

**结论一（确认）**：斜率见 §1.8.2 的表，六个数量级内与 $-1/2$ 差 0.5%。这比 E0-a 的 −0.49 ~ −0.61 强得多，因为那里的偏差全部来自记忆任务而非阶梯。

**结论二（证伪）**：对数周期涟漪不存在。

| $r$ | amp @ $\ln r$ | amp @ 诱饵 $\sqrt2\ln r$ |
|---|---|---|
| 2 | 0.00018 ± 0.00025 | **0.00064** ± 0.00024 |
| 4 | 0.00510 ± 0.00471 | **0.01576** ± 0.00464 |
| 8 | 0.02259 ± 0.01407 | 0.01892 ± 0.01409 |
| 16 | 0.09912 ± 0.02810 | **0.13866** ± 0.02764 |
| 32 | 0.02804 ± 0.04382 | **0.44348** ± 0.03696 |

诱饵周期每一行都不输给预测周期，$r=32$ 时高 16 倍。**方法有灵敏度（它检出了 0.44），涟漪就是不在 $\ln r$ 上。**

> **误差棒的读法要收敛（CR 发现）**：`ripple_at` 的标准误 $\sqrt{2s^2/n}$ 假设去趋势列与正弦列近似正交，只在窗口跨越"若干个周期"时成立，代码注释自己写了"够做检出阈值，不够当发表用的误差棒"。$r=32$ 时拟合窗口 $[20,10^6]$ 只有 **3.1 个周期**，而这恰好是指数已经漂移（−0.46）、非周期曲率最强的那一档——曲率可能通过不完全正交混进正弦系数。
> 所以 **"12σ" 不该按标定过的显著性读**。"无涟漪"这个结论主要由小 $r$ 那几行（采样密、周期多）支撑，那部分是稳的。

我原来的推理是"离散尺度不变 ⇒ 复指数 ⇒ 对数周期修正"。对这条扩散阶梯不成立——粗离散化的效应表现为**指数偏移**（$r\ge16$ 时斜率漂到 −0.52 / −0.46），不是振荡。§1.8.2 已改写。

### 8.5 尚未验证

- **均匀 vs 几何谁更适合真实用途**。E0-a 的 horizon 分不开（两者寿命都受噪声基底截断而非覆盖范围截断）。E0-d 说明两者的**指数相同**，差别只在覆盖同样范围所需的级数（1581 vs 5）——从内存看几何完胜，但记忆任务上的形状差异（均匀"高而短"、几何"低而长"）还没有定论。
- **E0-c 中几何阶梯绝对功率量级随 $r$ 上升**，$\tau_k/C_k^2$ 预测与 $r$ 无关。挂账。

---

## 9. 拓扑结果（2026-08-07）

复现：`cargo run --release -p annp-cli -- topology --sides 64,128,256,512 --pairs 3000 --contacts 1,4`
数据：`results/topology-large/topology.csv`（另有 `results/topology/` 的小尺寸扫描）。

### 9.1 想验证什么

§1.9 声称长程边指数 $\alpha=2$ 是**由格点维数导出**而非调出来的。Kleinberg 2000 给了可检验形式（**注意原文以格点边长 $n$ 表述，$N=n^2$，换算到 $N$ 时每个指数要除以 2**；且 $\alpha\ne2$ 的是**任意**去中心化算法的**下界**，贪婪允许高于它）：

$$\alpha<2:\ \Omega(n^{(2-\alpha)/3}),\qquad \alpha=2:\ O(\log^2 n),\qquad \alpha>2:\ \Omega(n^{(\alpha-2)/(\alpha-1)})$$

### 9.2 实测（每节点 1 条长程边，拟合 hops $\sim N^\beta$）

| $\alpha$ | N=4096 | N=16384 | N=65536 | N=262144 | $\beta$ 实测 | $\beta$ 下界 |
|---|---|---|---|---|---|---|
| 0.0 | 15.51 | 25.30 | 39.97 | 64.89 | **0.343** | **0.333** |
| 1.0 | 14.50 | 22.12 | 32.93 | 47.62 | 0.286 | 0.167 |
| 1.5 | 14.61 | 21.76 | 30.85 | 42.38 | **0.256** | 0.083 |
| 2.0 | 15.27 | 22.73 | 33.38 | 44.98 | 0.261 | polylog |
| 2.5 | 17.24 | 27.56 | 43.27 | 65.46 | 0.321 | 0.167 |
| 3.0 | 19.06 | 33.60 | 60.43 | 106.04 | 0.414 | 0.250 |
| 4.0 | 22.31 | 43.82 | 85.98 | 167.96 | 0.485 | 0.333 |

**结论一（实现被验证）**：$\alpha=0$ 端实测 0.343 vs 下界 0.333，差 3%。贪婪在 $\alpha=0$ 已知接近最优，所以这一端是紧的，能当实现正确性的检验。其余各行实测 ≥ 下界，符合"下界"的含义。

**结论二（主张要收窄）**：$\beta$ 的极小值落在 $\alpha=1.5$（0.256），$\alpha=2$ 紧随（0.261），两者无法区分。而且 $\alpha=2$ 的多项对数区**没到**：

| N | 4096 | 16384 | 65536 | 262144 |
|---|---|---|---|---|
| hops / $(\ln N)^2$ | 0.2208 | 0.2414 | 0.2714 | 0.2890 |

1.8 个数量级内该比值上升 31%，仍在前渐近区。

**结论三（稳健的部分）**：$\alpha>2$ 明确变差且是单调的（$\beta$：0.26 → 0.32 → 0.41 → 0.49）。**真正被实测确立的是上界 $\alpha\le2$**，不是 $\alpha=2$ 这一点。

> 对"极少超参数"这个目标，这其实是好消息：$\alpha$ 在 $[1,2]$ 内**没有可调收益**（跳数差 6% 以内），所以它不是一个需要扫的超参，取 2 即可。

### 9.3 每节点长程边数（$\alpha=2$）

| 长程边数 | N=4096 | N=16384 | N=65536 | N=262144 |
|---|---|---|---|---|
| 1 | 15.27 | 22.73 | 33.38 | 44.98 |
| 4（默认） | **8.49** | 11.47 | 14.99 | **19.24** |

常数因子 2.3×，不改变标度区。**PoC 规模（$N=4096$，度数 4+4）下平均约 8.5 跳**——这就是 §1.6 算力公式里 $H$ 的量级，可以直接用来估每 token 的算力。

### 9.4 尚未做

- 生长/修剪（§1.9 的动态部分）还没写，当前只有固定小世界。按 §7 这是第 8 步。
- 贪婪路由用的是**格点距离**作为目标函数。真实系统里路由是沿 surprise 下降的，两者是否有同样的可导航性，只有接上 `node.rs` 之后才能测。这是 §9 结论能否迁移到实际系统的**关键未验证假设**。

---

## 10. 前向通路（2026-08-07）

`node.rs` + `engine.rs`。46 个测试全过，clippy 零警告。

### 10.1 为了让前向能跑，必须定下来的四件事

原 §1.4/§1.5 没写，实现时被迫决定，四条都记在 `node.rs` 的文件头注释里。

**① 全程 f64，payload 不用 f32。** 原 §0 的表写的是 `payload: [f32; D_HEAD]`。阶梯已被证明必须 f64，而在最热的循环里放一个混精度边界，正是静默 bug 最爱待的地方。窄化 payload 是**将来实测后的优化**，不是默认。§0 的表要改。

**② 节点输出必须带残差：$u = q + \varphi(Wq)$。** 原 §1.4 写的是 $u=\varphi(Wq)$。那样跑不起来——$W$ 从零开始，$\varphi(0)=0$，**每个 payload 一进节点就变成零向量，网络把自己的输入湮灭掉了，永远学不到东西**。加残差后，什么都不懂的节点是透明的，网络的初始状态是纯扩散。

**③ 路由没有任何参数。**（这条最值得组里看）
原 §1.5 的路线 (b) 给每条边一个 key $K_e$，由邻居回传的 surprise 标量训练——那需要一条反向通路，还要给 particle 加一个"来源边"字段，撞 §0 的红线。

**根本不需要。** 节点 $j$ 本来就在算 $W_j q_j$，也就是**它预期下一个会收到什么**。边 $(i\to j)$ 的打分直接用它：

$$\ell_{i\to j} = \langle \hat u_i,\ \widehat{W_j q_j}\rangle + b_{i\to j}$$

于是"particle 流向正在期待它的节点"就是路由本身，而不是要学出来的东西。**零路由参数、无反向通路、particle 不加字段。** 路线 (b) 想表达的变分图像，实现出来比设计时更简单。

**④ 吸收是固定参考点，logit 恒为 0。** 边的 logit 是单位向量内积，天然落在 $[-1,1]$，以 0 为中心。**没有任何地方在期待这个内容时，所有边都打不过参考点，粒子被吸收。** 停机不需要阈值，也不需要 max_hops。

内稳态保留，且是**对一个节点的所有出边严格零和**的再分配：它能在邻居之间挪质量，但**永远无法改变吸收率**。这条性质让"防塌缩"和"算力预算"彻底解耦——后者只由固定参考 logit 和质量地板决定。

### 10.2 确定性怎么做到的

§3 要求位级可复现，所以不能真异步。每个 tick：

1. 按 `(node, serial)` 排序活粒子；
2. **同一节点内串行**处理（delta rule 的写入不对易）；
3. **不同节点并行**（状态不相交，且路由读的是 tick 开始前冻结的预期快照）；
4. 按节点序合并子粒子，重新分配 serial。

节点并行的实现是"扫过全部 N 个节点、跳过空闲的"——这样 rayon 拿到的 `&mut Node` 天然不相交，跳过的分支代价相对一个 tick 的真实工作量可以忽略。

### 10.3 已验证

| 主张 | 出处 | 结果 |
|---|---|---|
| 质量严格守恒 | §1.6 | 每 token 吸收质量与注入相差 $<10^{-12}$ |
| 无 max_hops 也必然停机 | §1.6 | 单 token 全部吸收所需 tick < 200 |
| 活粒子数 $\le 1/\epsilon$ | §1.6 | 通过 |
| **任意线程数结果位级一致** | §3 | 1 线程 vs 8 线程，输出、质量、访问直方图逐位相同 |
| 重复运行一致 | §3 | 通过 |
| 路由确实流向"期待它的"节点 | §10.1 ③ | 零参数下通过 |

### 10.4 一个被证伪的前提

原打算测"关掉内稳态会塌缩、打开就不塌缩"。**没有塌缩可测**：未训练 + 均匀入射下，最忙的十分之一节点占 16% 的访问（完全均匀是 10%），开关内稳态只差不到半个百分点。

塌缩风险是**训练后**才可能出现的现象，现在测不了。所以：
- 内稳态的**机制**在节点级被钉住了（`homeostasis_forces_turn_taking`：单节点重复刺激下，不开内稳态时一条边 200/200 全赢，开了就必须轮流）；
- 它的**价值**仍然未验证，等接上真实数据再说。

### 10.5 下一步的已知缺口

- **入射还是均匀随机的**，§1.3 的内容寻址入射需要嵌入表，属于 `model.rs`。在那之前 §10.3 的负载数字不能当真。
- **每 token 算力与序列长度无关**（§1.6 的核心主张）还没测，需要真实入射才有意义。
- **路由用的是"预期匹配"，而 §9 的可导航性是用格点距离测的。** §9.4 标的那个关键假设现在有了可测的形式：把 §9 的贪婪路由换成本节的预期匹配路由，看跳数是否还是同量级。

---

## 11. 输入输出层（2026-08-07）

`model.rs`。59 个测试全过，clippy 零警告。§0 的精度已按 §10.1 ① 改成 f64。

### 11.1 建成的

- **打碎 = 固定正交旋转**：随机符号翻转 + Walsh-Hadamard，$O(n\log n)$，零参数。符号翻转不是装饰——纯 Hadamard 的首行全 1，会把嵌入的**均值**整块塞给 chunk 0。测试 `rotation_spreads_a_spike_evenly` 钉住了这一点。
- **方向进 payload、模长进 mass**（§1.2）。
- **内容寻址入射**：两个固定随机平面上的**相位**决定环面坐标（相位而非投影 ⇒ 天然尺度不变，无归一化常数）；slot 位移在半径 $\sqrt{P/\pi}$ 的圆盘内。测试确认：同一 token 永远同一入口、微扰不改变入口、8 个 slot 落在局部патch 而非堆在一点或撒满全图。
- **重组**：`assemble ∘ shatter` 在透明网络下**逐位精确恢复嵌入**（$<10^{-12}$），把分裂/重接的算术和网络本身隔离验证了。
- **输出头与嵌入表绑定**，损失梯度 $(p-y)\,e^\top$ **恰好是秩一外积**——正是阶梯的 `inject` 已经接受的形状，所以输出头**不需要任何自己的机构**。这是全系统唯一的反向传播。
- $P$（slots）确实是**运行时宽度**：`width_can_change_after_construction_without_touching_node_weights` 通过。

### 11.2 两个测出来的事实

**① 绑定权重的表，天生偏向预测"当前 token"。**
行是单位范数且近似正交，所以喂 token $X$ 时 $X$ 的 logit ≈ 1，其余 ≈ $\pm 1/\sqrt{d_{model}}$。**透明网络的预测就是"你刚看到的那个"**，损失 3.05 而均匀是 ln 64 = 4.16。这不是缺陷，是绑定的必然后果，但它是**语言模型必须先超越的基线**——写实验时不能把"比均匀好"当成学到了东西。

**② 阶梯把输出头的学习速度压到 18.5%。**
同样 200 步、同样学习率，$m=4,r=4$ 的阶梯只走完普通矩阵 **18.5%** 的损失下降（5.4× 慢）。原因是设计使然：梯度打进 rung 1 后向外泄漏，$\sum C_k = 85$，而最深一级的弛豫时间是 $r^6/g_1 = 8192$ 步——200 步远在暂态内。渐近学习率不变，被压的是暂态。

> **这和 §1.8.3 形成一个必须解决的张力。** §1.8.3 主张梯度应该注入**更深**的级以平均掉噪声。但现在测出来，**即使注入最浅一级，学习已经慢了 5.4 倍**；注入更深只会更慢。
> 所以"注入深度"不是一个单调的选择，而是**噪声平均 vs 学习速度**的权衡，需要一个实验定夺，不能靠推导。嵌入表的级数因此做成了独立参数（`embed_rungs`，取 1 即退化为普通矩阵），而不是照抄节点的 schedule。

### 11.3 还没有的

- **`data.rs` / 分词**。工作目录里的 `tokenizer.model` 是 SentencePiece protobuf，解析要么加依赖要么手写；暂时把 vocab 做成参数，用合成 token 流即可跑通全链路。
- **端到端驱动**（shatter → engine → assemble → learn 的循环）还没接起来，接上之后才能测 §1.6 的"每 token 算力与序列长度无关"和 §10.5 的三个缺口。

---

## 12. 端到端（2026-08-07）

`runtime.rs` + `annp run`。65 个测试全过，clippy 零警告。
复现：`cargo run --release -p annp-cli -- run --tokens 20000`（加 `--bypass` 跑对照）。

> ## §12.3 的数字已按 §12.7 更正
>
> CR 发现 §12.1 只修了必要条件（结算顺序）。泄漏已量化并修复，**序列化（`Mode::Serial`）现在是默认**。下面表里的"重叠"列不是合法码长，只作为泄漏大小的参照。

### 12.1 一个协议级 bug，被测试抓到（只修对了一半）

**位置不是按顺序结算的**——跳数多的 token 落得晚。最初的实现是"谁先结算就先算分并更新"，后果是：位置 $p$ 的损失是在模型**已经吸收了位置 $p+1$ 的更新**之后测的，而那个更新依赖位置 $p+2$ 的 token。

**这会让 prequential 损失不再是压缩上界，§5 的整个评测协议静默失效。** 现在改成结算严格按流序：已结算的输出在 `ready` 里排队，直到它之前的位置全部算完。

### 12.2 §1.6 的核心主张成立

每 token 的节点访问次数，20000 token 的流：

| 窗口 | visits | 平均跳数 |
|---|---|---|
| 前 1/10 | 1392.7 | 7.38 |
| 中间 1/10 | 1358.0 | 7.21 |
| 后 1/10 | 1338.2 | 7.13 |

**平的（略降）。每 token 算力与序列位置无关**，这是直接测量不是论证。

> **单位更正（CR 发现）**：这一列是**节点访问次数**，不是边穿越次数。一个在入射节点就被吸收、一条边都没走的碎片计为 1。所以要和 §9 的 `greedy_hops`（数边，起终点相同时为 0）比较，必须减 1：**边等价值约 6.2**，对 §9 的 8.5。**§9.4 那个关键假设仍然初步过关**（同量级），但两个数字在原始形式下不是同一个量。

负载（576 节点，内容寻址入射）：**无一节点从未被访问**，最忙的十分之一占 15.5%（均匀是 10%），最忙的单节点占 0.33%（均匀是 0.17%）。没有热点。

### 12.3 学习效果：目前网络没有证明自己有价值

2×2 对照，20000 token，比特/token（越低越好）。`--bypass` 表示丢弃网络输出、直接用当前 token 的嵌入喂输出头，**其余一切（路由、可塑性、更新时序）完全相同**。

| | 阶梯输出头 (m=3) | 普通输出头 (m=1) |
|---|---|---|
| **网络** | 5.3515 | **4.3208** |
| **bypass 对照** | 5.1436 | 4.4182 |

参照：均匀 6.000，信源熵率 1.442。

**两条必须直说的结论：**

**① 用阶梯输出头时，网络比 bypass 更差**（5.352 vs 5.144，差 0.21 比特）。也就是说在那个配置下，把 token 送进网络绕一圈，**破坏的信息比增加的多**。

**② 用普通输出头时，网络仅在最后一个十分位险胜**（4.3208 vs 4.4182），但**整段 prequential 总量基本打平**（89674 vs 89643 比特，网络反而多 31 比特）。

**④ 但 2 万 token 是不够的。** 10 万 token、普通输出头、**合法协议（序列化）**：

| | 末十分位 | prequential 总量 |
|---|---|---|
| **网络** | **4.3723** | **436696** 比特 |
| **bypass** | 4.4618 | 443940 比特 |

**网络在两个指标上都赢了**：末十分位领先 0.0895 比特，全程总量少 **7244 比特（1.63%）**。**12.3 ② 那个"打平"是暂态**——网络需要足够数据盖过自己的初始化噪声，盖过之后持续拉开。

以"从均匀 6.000 降到信源熵 1.442"这 4.558 比特为可用空间：网络吃下 35.7%，bypass 吃下 33.7%——**净贡献 2.0 个百分点**。

（注意：跨长度比较必须用**同长度的末十分位**，因为十分位是相对的。10 万跑的首十分位是 1 万 token，2 万跑的首十分位是 2 千 token，两者不可比。）

**③ 阶梯压在输出头上代价约 0.8–1.0 比特**（4.32 vs 5.35）。这和 §11.2 ② 测到的 18.5% 暂态阻尼一致，是暂态而非渐近代价，但在 2 万 token 的尺度上它主导了一切。**`embed_rungs` 的默认值应该是 1**，除非能证明阶梯输出头买到了别的东西（比如持续学习下的稳健性）。

### 12.4 网络为什么可能在减分——待查的三个方向

不改设计，先记下来：

1. **每跳都在加噪声。** $u=q+\tanh(Wq)$，$W$ 未训练时 $\tanh(Wq)$ 接近噪声；7 跳之后 payload 已经离 $q$ 很远。
2. **质量分裂 + 同一 payload。** top-k 分裂出的子粒子携带**相同**的 emitted payload，最后重组是"同一碎片被不同路径变换后的加权平均"——平均本身就是模糊。
3. **节点学的转移可能大部分是噪声。**（我认为这条最可疑）一个节点看到的是**多个 token、多个 slot 交错**的碎片流，delta rule 学的是"这个节点上，什么碎片跟在什么碎片后面"。若交错顺序本身没有结构，这就是在拟合噪声。
   这直接关系到 §1.4 的"因果性由到达序涌现"——**到达序是一个被打散的混合流时，这个机制可能太弱**。

### 12.5 下一步

- 长跑结果（100k）判断 12.3 ② 的反超是否真实。
- **上真实语料前先做一遍 code review**（数理逻辑向）。
- 分词接 `sentencepiece` crate。

### 12.6 关于 bypass 对照本身的一个观察

bypass 的末十分位从 2 万跑的 4.4182 变成 10 万跑的 4.4618，**没有随数据变好**。原因大概是输出头绑定造成的双线性问题：同一张表既当输入嵌入又当输出映射，$E$ 出现在两处，梯度下降在这种非凸结构上很慢。这意味着 **bypass 是个会饱和的对照**，长跑下网络的领先幅度里有一部分来自对照饱和而非网络变强——报数据时要区分这两者。

### 12.7 泄漏还没修完：§12.1 只是必要条件

CR 指出 §12.1 的修法只序列化了**输出头**的算分与更新，没有触及产生 `output.accumulated` 的**节点表示**本身。

机制：token 是**设计上重叠在途**的（一 tick 注入一个，平均在途约 7 个）。节点的 `W` 是一份共享可变状态，谁的粒子落上去就更新谁的——**不区分 token**。于是位置 $p$ 的粒子在第 5 跳落到某节点时，那个节点的 `W` 可能已经被位置 $p+3$ 的粒子写过了。而 $p$ 的输出正是由这些 `W` 变换出来的，$p$ 的损失又是用来预测 $p+1$ 的。

**结果：预测 $p+1$ 时用到了 $p+3$ 的内容。** 真实的流式解码器在解 $p+1$ 时不可能被 $p+3$ 影响，所以算术编码的论证断了，prequential 损失不再是压缩上界。

**而且这让 §12.3 的对比失去意义，方向还是偏向网络的**（这一条 CR 没点出来，是更严重的部分）：
- `--bypass` 用的是 `embedding_of(token)`，只依赖嵌入表，而表**严格按位置序更新** ⇒ **无泄漏**；
- 网络跑读节点状态 ⇒ **有泄漏**。

所以网络那 2.4% 的领先里，有多少是真本事、有多少是偷看了未来，**目前无法区分**。

**要做的实验**：加一个"不重叠"模式——注入一个 token，跑到静默，算分，再注入下一个。这样算分时网络只见过 $\le p$ 的 token，泄漏归零。然后：

| | 重叠（现状） | 不重叠（无泄漏） |
|---|---|---|
| 网络 | 已测 4.3150 | ? |
| bypass | 已测 4.4618 | 应与重叠一致 |

**两者之差就是泄漏的大小**，不重叠那一列才是能写进论文的数字。

代价是失去"异步重叠"这个设计特征。但要注意：**跨 token 的到达序机制并没有因此消失**——节点仍然会先看到 $p$ 的碎片、后看到 $p+1$ 的碎片，§1.4 的"因果性由到达序涌现"依然成立，丢的只是"同时在途"。这一点值得组里确认我的理解对不对。

### 12.8 内稳态与吸收率并未解耦（§10.1 ④ 作废）

零和成立，但**由此推不出"不改变吸收率"**。偏置进入的是一个**经过 top-k 截断**、与固定吸收参考 logit 竞争的 softmax：零和的偏置移动可以把某条边压到参考点以下，让吸收挤进保留集。

反例：三条边 logit $(0.5, 0.5, -1.0)$，吸收固定 0，`top_k=2` ⇒ 吸收权重 0。加零和偏置 $(-0.6, +0.6, 0)$ ⇒ logit 变 $(-0.1, 1.1, -1.0)$，top-2 变成 $\{1.1, \text{吸收}\}$，**吸收权重 25%**。偏置无界且每个落选边每 tick 都在涨，所以这是常态不是构造。

**所以"防塌缩"和"算力预算"没有解耦，而且目前没有任何东西约束这个耦合。** §10.1 ④ 的说法已在 `node.rs` 注释里改正。现有测试只测了偏置和为零（真命题），没测吸收率不变（假命题）。

### 12.9 泄漏已量化并修复

`Mode::Serial`（现在的默认）注入一个 token → 跑到静默 → 算分 → 再注入下一个。不变量由测试钉住：每次 `advance` 之后在途粒子必须为 0，即**给一个 token 算分时，还没有任何更晚的 token 碰过它走过的节点**。

10 万 token，普通输出头，prequential 总量（比特）：

| | 序列化（合法） | 重叠（泄漏） | 差 |
|---|---|---|---|
| **网络** | 436696 | 433124 | **3572** |
| **bypass** | 443940.4 | 443940.4 | **0** |

**两件事同时得到确认：**

**① bypass 两格逐位相同**（总量、首末十分位、passthrough 全部一致）。这是对泄漏路径诊断的独立验证：bypass 只读嵌入表、不读节点状态，所以协议对它**结构上**无影响。如果这两格不同，说明泄漏路径的理解有漏洞——它们相同。

**② 泄漏值 3572 比特 / 10 万 token = 0.036 比特/token**（末十分位 0.057）。原先报的"网络领先 2.4%"里，**约三分之一是偷看未来，三分之二是真的**。修正后的合法数字是 **1.63%**。

结论：**网络的贡献经得起协议修正，只是幅度要打折。** 净吃下可用空间的 2.0 个百分点（原报 3.0）。

### 12.10 排列是自由的，随机排列不是卖点

任何排列 $\pi$ 都给出合法码长——模型预测的是"**到达序里的下一个**"，而 $\pi$ 定义到达序。但：

- **左到右**：文本的可预测结构在这个序上，也是和因果 Transformer 唯一可比的序。基线用它。
- **填中间序** $\pi=(x_1,\dots,x_{q-1},\ x_T,\dots,x_{q+1})$ 然后预测 $x_q$：模型**两侧都看过**，而 $x_q$ 确实是到达序里的下一个 ⇒ 合法码长。**因果 Transformer 结构上做不了这件事。** 这才是"不受方向束缚"的证据所在。
- **均匀随机排列**：同样合法，但"下一个到达"变成随机的未揭示 token，**任何模型都只能预测边缘分布，我们也一起崩**。它不是对照实验，只是把任务变成不可能。早先把它当卖点是错的。

---

## 13. 读数式入射锚点：失败，且失败方式排除了阻尼这条路（2026-08-07）

`Ingress` 的读数模式 + `--fixed-ingress` 对照。10 万 token，普通输出头，序列化协议。

### 13.1 结果

| | prequential 总量 | 末十分位 |
|---|---|---|
| **固定（相位位置）** | **436696** | **4.3723** |
| 读数（上次停在哪儿） | 438600 | 4.3883 |

**读数更差**，多 1904 比特（0.44%）。

### 13.2 但不是我预判的失效方式

我担心的是**塌缩**（所有 token 迁到一处）。没有发生：60 个有记忆锚点的 token 占据 **56 个不同格子**。

真正的失效是**不收敛**。每次观测的锚点移动距离，全程：

| 首十分位 | 中间十分位 | 末十分位 |
|---|---|---|
| 3.48 | 3.42 | 3.39 |

**平的。** 锚点从头到尾以恒定幅度抖动，一次也没有安定下来。

（参照：24×24 环面上两个均匀随机点的期望 L1 距离约 12，实测 3.4 ⇒ 停留点确实在入射点附近，**有局部性，但没有收敛**。）

**内容寻址要求映射是稳定的。** 同一个 token 每次从不同地方进入，等于把内容寻址退化成随机寻址——比"任意但固定"的映射更差。这解释了 13.1。

### 13.3 阻尼救不了，这一条可以解析地证明

自然的下一步是给读数加阻尼（EMA）或者按 §1.8 的逻辑挂一条阶梯。**都没用**，因为：

设锚点 $a_t$，token 从 $a_t$ 入射，质量停在 $r_t = a_t + \xi_t$（$\xi$ 为净位移，路由在格点方向上无系统偏置 ⇒ 近似零均值）。

- 纯读数：$a_{t+1} = r_t = a_t + \xi_t$ ⇒ 随机游走。
- 加阻尼：$a_{t+1} = (1-\alpha)a_t + \alpha r_t = a_t + \alpha\xi_t$ ⇒ **仍然是随机游走**，只是步长小了 $\alpha$ 倍。

$$\boxed{\text{读数的目标以锚点自身为中心 ⇒ 不存在回复力 ⇒ 任何平滑只减慢扩散，不能阻止它}}$$

阶梯也一样——它是一族平滑，改变不了这个结构。

### 13.4 于是缺的是"竞争"，而这正是组里不想要的东西

SOM 能形成映射，靠的是**回复力来自输入分布而非自身**：token A 在格子 $c$ 获胜时，$c$ 的邻域一起朝 A 移动，于是**其他 token 被推开**。是 token 之间的竞争提供了结构。

单个 token 追着自己的停留点跑，没有竞争对手，就只能自由扩散。

**所以三条路，都有代价：**

| | 代价 |
|---|---|
| **(a) 入射保持固定**（现状） | §1.3 的拓扑图谱叙事只是期望，不是事实。**但目前数字最好。** |
| **(b) 真做 SOM 竞争** | 引入第二个自适应过程和它的速率——正是组里反对的那类东西，且与 §1.9 的共振问题重新出现 |
| **(c) 干脆去掉网格** | 入射退化成哈希，靠 §1.9 长出拓扑。放弃"局部性是联想记忆的前提"，但**少一个超参和一个维数选择** |

我倾向先维持 (a)（零成本、数字最好），把 (c) 当作认真的候选——网格现在买到的东西，也许没有它的代价大。这个我不该单方面决定。

代码保留两种模式，`--fixed-ingress` 是默认。

---

## 14. 消融与吸收规则更换（2026-08-07）

全部 10 万 token、序列化协议、普通输出头、$P=8, d_{head}=16$、度数 4+4=8。**bypass 对照 462414.6 比特**。

### 14.1 top-k 截断是纯亏损（旧的固定吸收规则下）

| top_k | prequential | 访问/token | 跳数 |
|---|---|---|---|
| 2 | 452589.4 | 1410 | 7.47 |
| 3 | 449049.5 | 870 | 4.82 |
| 4 | 445716.7 | 641 | 3.85 |
| 6 | 443063.6 | 409 | 2.94 |
| **8**（≈不截断） | **436715.8** | **438** | **2.73** |

**单调：k 越大损失越低且算力越少。** k=2→8 损失降 3.5%、访问降到 1/3.2，相对 bypass 从 +2.12% 到 **+5.55%**。

top-k 本来是"控制算力"的机制，实测它**增加**算力——因为截断把吸收挤出保留集（排序在最后，又打不过被内稳态顶高的边），质量排不掉、路径变长。

### 14.2 节点阶梯是承重的（唯一从未被消融过的部件）

| $k=2$ | prequential |
|---|---|
| rungs=4 | 452589.4 |
| **rungs=1（无阶梯）** | **485210.7** |
| bypass | 462414.6 |

**关掉节点阶梯，网络比不用网络还差 22796 比特。** 机制上说得通：$\eta=1$ 的 delta rule 是精确单次存储，**没有阶梯的节点只是"我上次看到什么"的寄存器**，每次到达都被覆写；深层阶梯留住的是重复出现的结构。**阶梯是把节点从寄存器变成记忆的那个东西。**

那 75% 的算力买到的是整个网络的价值。

### 14.3 相对吸收规则：三个预测全中，但代价一半的收益

$$\text{候选} = \{j : \langle u,\widehat{\text{expect}_j}\rangle > \langle u,\widehat{\text{expect}_{\text{当前}}}\rangle\}$$

两段式：**吸收份额只由内容算**（偏置不在这个计算里），转发余额才按 `content + bias` 在候选间分配。

| 规则 | prequential | vs bypass | 访问/token | 跳数 |
|---|---|---|---|---|
| 固定参考 $k=8$ | **436715.8** | **+5.55%** | 438 | 2.73 |
| 相对 $k=8$ | 451203.9 | +2.42% | **137** | 2.16 |
| 相对 $k=2$ | 454267.3 | +1.76% | **52.7** | 1.95 |
| 相对 $k=8$, rungs=1 | 460723.3 | +0.37% | 220 | 2.33 |

**三个预测全部兑现：**
1. 停机有保证（top_k=1 不再不停机）；
2. 内稳态**不能**改变吸收率（测试逐位断言）；
3. **top-k 不再重要**——相对规则下 $k=2$ 与 $k=8$ 只差 0.7%，固定规则下差 3.5%。

**但收益掉了一半**（+5.55% → +2.42%）。

原因看跳数：**相对规则下粒子约 2 跳就停，而且全程不变**（1.95→1.95），预热没有让路径变长。判据是严格的"有邻居比这里更好"，粒子很快爬到一个局部最优就停下——Lyapunov 性质在起作用，但**局部最优很浅**。

反过来说：**旧规则那多出来的收益，来自吸收被压制导致的"意外过度探索"**——更多低质量粒子游荡、访问更多节点、重组时是更大的集成平均。那是个 bug，但它在做有用的事。

单位算力的效率反而是相对规则更高（每次访问带来的比特数：相对 $k=2$ 是固定规则的 2.6 倍）。所以这是一条**质量-算力前沿**，不是简单的好坏。

### 14.4 待议：如何在不引入温度超参的前提下把探索找回来

我的提议（未实现）：**参考点减去节点自身的 surprise**

$$\text{reference} = \langle u,\widehat{\text{expect}_{\text{当前}}}\rangle - \sigma$$

含义：**在懂你的地方停下，在不懂你的地方继续走。** 节点预测得准（$\sigma$ 小）⇒ 内容已到家 ⇒ 吸收；预测得差（$\sigma$ 大）⇒ 这里不理解它 ⇒ 继续找。

- $\sigma$ 是 §0 已经定义的"唯一信用信号"，本来就在算；
- 与内积同量纲（都由单位向量导出），**不需要任何新常数**；
- 保持两段式结构，故 14.3 的三条性质不受影响。

---

## 15. surprise 参考点：收益全部拿回来了（2026-08-07）

10 万 token，$k=8$，其余同 §14。**bypass 对照 462414.6 比特。**

### 15.1 结果

| 吸收规则 | 内稳态 | prequential | vs bypass | 末十分位 | 访问/token |
|---|---|---|---|---|---|
| 固定参考（有缺陷） | 0.05 | 434709.1 | +6.00% | 4.2682 | 450–479 |
| 相对 | 0.05 | 451203.9 | +2.42% | 4.5009 | 125–137 |
| **相对 − surprise** | 0.05 | 433834.3 | +6.18% | **4.2105** | 478–485 |
| **相对 − surprise** | **0** | **431314.3** | **+6.72%** | 4.2342 | 475–488 |

**surprise 规则以相同算力全面超过有缺陷的固定规则**（433834 vs 434709，访问数 478 vs 450），同时保留 §14.3 的三条正确性：停机有保证、内稳态不能改吸收率、top-k 不再重要。

§14.3 丢掉的那一半收益**全部拿回来了**，而且拿回的方式是有依据的——探索由"这个节点懂不懂我"驱动，不是由被压制的吸收意外产生。

**一处必须更正我自己的预测**：我说过 surprise 规则会自行退火（网络学会后 $\sigma$ 下降 ⇒ 路径变短）。**没有发生**——访问数全程平坦（485→483→478）。原因是信源有 1.44 比特的不可约熵，$\sigma$ 不会趋于零。机制成立，但不是因为我给的理由。

### 15.2 内稳态应该去掉

| | prequential | 最忙十分位 | 最忙单节点 |
|---|---|---|---|
| 内稳态 0.05 | 433834.3 | 19.7% | 0.47% |
| **内稳态 0** | **431314.3** | 20.4% | 0.49% |

它**要价 2520 比特（0.58%），买到 0.7 个百分点的负载均衡**，而且两档都离塌缩很远（无节点未被访问）。

同时它在每一个已知缺陷里都有份：吸收被压制、跳数随 $d_{head}$ 漂移、`top_k=1` 不停机。

CLI 默认值已改为 **0**。代码暂时保留（§10.4 说过塌缩是训练后现象，现在这个规模没出现，不代表更大规模不出现），但**它现在是删除候选**，不是设计的一部分。

### 15.3 当前的默认配置

`absorb=surprise`，`homeostasis=0`，`top_k=8`（≈不截断），`rungs=4`，`embed_rungs=1`，序列化协议，固定入射。相对 bypass **+6.72%**。

可用空间（均匀 6.000 → 信源熵 1.418）是 4.582 比特；网络末十分位 4.234 ⇒ 吃下 **38.5%**，bypass 吃下 29.9%。**净贡献 8.6 个百分点**（§12.3 时是 2.0）。

---

## 16. 路由地形的假设被证伪；三处机制删除（2026-08-07）

### 16.1 路由读深层 rung：不成立

10 万 token，其余同 §15.3。**bypass 462414.6。**

| 路由读取 | prequential | 末十分位 | 访问/token |
|---|---|---|---|
| **rung 1（鲜活）** | **431314.3** | **4.2342** | 483 |
| 最深 rung（沉淀） | 432630.7 | 4.2456 | 488 |

**读深层反而差 0.30%。假设被证伪。**

我的诊断是"路由地形由单样本构成、每步都在抖，所以局部最优很浅"。数据说不是——**抖动不是限制因素**。

更可能的解释：**一个节点的最深 rung 是它见过的一切的平均**，而节点接收的碎片是多样的，所以那个平均趋近于零、没有方向，作为寻址 key 反而不如"我上次看到什么"（后者至少是个真实的、有方向的内容）。

**这条同时修正了 §1.4 的一个论断。** §1.4 说"浅层是刚写入的键值对（=attention），深层是沉淀下来的稳定键值对（=FFN as key-value memory）"。实测表明**深层 rung 不是一个可以独立读出的有用表示**。

那 §14.2 里"关掉节点阶梯网络就比不用还差"要怎么解释？**阶梯的价值在于它约束 rung 1**——扩散把 rung 1 往运行平均上拉，使它不再是纯粹的"我上次看到什么"寄存器。**阶梯是刹车，不是第二个读数。** 这个说法比 §1.4 原来那个二元论更简单，而且两次测量都支持它。

没有去扫中间的 rung：方向是"越深越差"，说明最优在 rung 1 这一端，为一个 0.3% 的效应加一个旋钮去搜，与"保持规则简洁"相悖。

### 16.2 删掉的三处机制

| 机制 | 删除依据 |
|---|---|
| **top-k 截断** | §14.1：截断越狠越差越贵；最好的配置本来就不截断。分支数由质量地板单独界定 |
| **内稳态边偏置** | §15.2：要价 0.58%，买到 0.7 点负载均衡，且在每个已知缺陷里都有份 |
| **路由源选择** | §16.1：深层读取更差，没有可调收益 |

连带删除：`EngineParams::top_k`、`Context::top_k`、`Node::edge_bias`、`apply_homeostasis`、`RoutingSource`、三个相关测试、三个 CLI 参数。

**副作用**：`route` 的第二段现在是纯 content softmax。§10.1 ④ 声称的解耦**现在是平凡成立的**——再没有别的东西能影响吸收份额。

### 16.3 当前默认配置

`absorb=surprise`、无 top-k、无内稳态、路由读 rung 1、`rungs=4`、`embed_rungs=1`、序列化协议、固定入射、$P=8,d_{head}=16$。

**431314.3 比特 vs bypass 462414.6 ⇒ +6.72%。** 可用空间 4.582 比特中网络吃下 38.5%、bypass 29.9%，**净贡献 8.6 个百分点**。

---

# 17. 盘点（2026-08-07）

写在这里是因为 §8–16 是按时间顺序记的实验流水，读它无法回答"我们现在到底在哪"。本节是横切面。

## 17.1 已经定了的（有数据，不要重开）

| # | 决定 | 依据 |
|---|---|---|
| 1 | **序列化协议**，一次一个 token 在途 | 泄漏 0.036 比特/token；bypass 两格逐位相同验证诊断（§12.9） |
| 2 | **prequential = 压缩上界**作为唯一指标 | 架构无 train/test 之分，别的都不诚实（§5） |
| 3 | **节点阶梯必需**，$r\in[2,8]$，$m$ 由跨度导出 | 关掉后网络比不用还差 22796 比特（§14.2）；指数 −0.499 跨六个数量级（§8.4） |
| 4 | **阶梯是刹车，不是第二个读数** | 深层 rung 单独读更差 0.3%（§16.1）——**这条推翻了 §1.4 的 attention/FFN 二元论** |
| 5 | **吸收 = 相对参考 − surprise** | +6.72% vs bypass，同时保住停机保证（§15.1） |
| 6 | **无 top-k 截断** | 截断越狠越差越贵（§14.1） |
| 7 | **无内稳态** | 要价 0.58%，买 0.7 点负载均衡（§15.2） |
| 8 | **入射固定**（相位位置） | 读数式自适应无回复力，阻尼在解析上也救不了（§13.3） |
| 9 | **输出头不挂阶梯**（`embed_rungs=1`） | 这个长度上代价 0.8–1.0 比特（§11.2 ②） |
| 10 | **打碎用固定正交旋转** | 零参数；`assemble∘shatter` 逐位恢复（§11.1） |
| 11 | **输出头与嵌入表绑定** | 梯度恰为秩一外积，阶梯原生接受；且是路线 (b) 下嵌入表唯一的学习信号 |
| 12 | **位级确定性 / 手写 adjoint / 诊断先埋** | §3 三条，全部兑现且有测试 |

**当前默认配置**：`absorb=surprise`、$P=8$、$d_{head}=16$、$24\times24$ 网格、度 4+4、`rungs=4`、`embed_rungs=1`。
**431314.3 比特 vs bypass 462414.6 ⇒ +6.72%；净贡献 8.6 个百分点。**

## 17.2 已废弃（连同原因，避免重新提出）

**上一代带来的**：fast/slow 模块二分、node/subnode 分裂、bandit 家族（UCB→D-UCB→高斯TS→学生t TS）、attention + 局部 KV cache、`max_hops`、遗忘率 $\lambda$、load-balance 辅助损失、RL 路由。

**这一代实测废弃的**：
| 废弃项 | 原因 |
|---|---|
| 对数周期涟漪预测 | 诱饵周期每档都不输给预测周期（§8.4） |
| 重叠在途 | 泄漏，破坏压缩上界（§12.9） |
| 随机序当卖点 | 任何模型都会一起崩；填中间序才是证据（§12.10） |
| 读数式入射 | 无回复力，纯随机游走（§13.3） |
| top-k / 内稳态 / 路由源选择 | §16.2 |
| §1.4 的 attention/FFN 二元论 | §16.1 |
| §10.1 ④ 的"解耦"论证 | 现在平凡成立，论证本身不需要了 |

## 17.3 尚未决定（**这是主要缺口**）

| # | 问题 | 状态 | 卡在哪 |
|---|---|---|---|
| **A** | $P$ 是**坐标划分**还是**路径重数** | 扫描因参数量差 64× 而欠功率，结论作废 | 需在 100k+ 重做；$P{=}1$ 那档很慢 |
| **B** | **网格是不是承重结构** | 未测 | 它只买到"最不重要的那个轴"（§12.4 ③）；若 §1.9 能长拓扑，它可能只是脚手架 |
| **C** | 要不要**有序的深度轴**（纤维丛） | 未测 | 节点无法按处理阶段分化；但离 Transformer 更近一步 |
| **D** | 路线 **(a) 全局梯度 vs (b) 局部** | **(a) 一行没写** | A1-pre（门控梯度衰减测量）**从未做过** |
| **E** | 节点记忆要不要**低秩** | 未测 | 与 §1.9 的秩饱和触发直接相关 |
| **F** | §1.4 的分叉：路由**预测 $u$** 还是**残差** | 未测（A8） | |

## 17.4 完全没有实现的部分

| 部件 | 说明 |
|---|---|
| ~~**§1.9 生长/修剪**~~ | **突触周转已实现，见 §18。** 但 §1.9 混淆了两件事：固定预算下的**重连**（已做）和**扩容**（加节点/加秩，仍未做，= 决策 E） |
| **`backward.rs` / 路线 (a)** | 不存在 |
| **`data.rs` / 分词 / 真实语料** | 不存在。全部结果都在合成 Markov 信源上 |
| **填中间序评测** | 不存在。这是"不受方向束缚"的唯一硬证据 |
| **E1–E4** | 全部未做：byte-level LM、持续学习、needle-in-haystack、尺度律 |
| **A1/A4/A6/A7** | 路由信用、入射内容寻址 vs 均匀、打碎方式、delta vs Hebb —— 全部未做 |

## 17.5 §1.9 为什么现在变成主线

三条独立的线索都指向它：

1. **它是唯一一条零证据的核心主张**（其余四条——算力与长度无关、持续学习、无 all-reduce、无 lost-in-the-middle——至少有部分测量）；
2. **决策 B（网格是否承重）无法在 §1.9 缺席时回答**——网格的价值恰恰是"若拓扑能自己长，它就不必要"；
3. **决策 E（低秩节点记忆）与 §1.9 的触发器是同一件事**——秩饱和既是低秩的自然度量，也是 §1.9 生长的判据。

换句话说：**A/B/C/E 四个未决问题里有三个的答案取决于 §1.9，而它一行没写。** 继续扫 $P$ 只会得到"在固定拓扑下 $P$ 该取多少"，那个答案在拓扑能生长之后可能完全不同。

## 17.6 建议的下一步顺序

1. **实现 §1.9 的生长/修剪**（固定边预算，秩饱和触发生长，低通过质量触发修剪）；
2. 测"固定拓扑 vs 演化拓扑"——这是可扩展性主张的第一个证据；
3. 在**演化拓扑上**重做 $P$ 扫描（决策 A），顺带回答 B；
4. A1-pre（门控梯度衰减），决定路线 (a) 的生死；
5. 接 sentencepiece + 真实语料，跑 E1/E3。

**开销最小、解锁最多的是第 1 步。**

---

## 18. 突触周转（§1.9 的重连部分，2026-08-07）

**先澄清 §1.9 自己的一个混淆**：它把"生长"和"扩容"当成一件事。度数固定时能做的是**在固定预算内重连**；**扩容**（加节点、加秩）是另一件事，仍未实现，即决策 E。本节只做前者。

### 18.1 四处顺着现有架构的设计选择

1. **格点边永久，只有长程边可塑。** 这是被 §9 逼出来的：贪婪路由不会卡住**正因为**四个轴邻居总能提供严格更近的一步。剪掉它们会同时毁掉那个保证和连通性。骨架固定、长程可塑。
2. **替换而非增删** ⇒ 度数恒定 ⇒ CSR 不重分配，**每跳算力严格不变**。这是重新分配一个固定预算，不是扩大它。
3. **新边不需要初始化**——路由零参数之后（§16.2），边只是指针，长出来立刻可用。
4. **目标选"访问最少"而非"surprise 最低"。** 低 surprise 是"已经满足"，不是有余量。而未访问节点的 expect 是零向量，在 surprise 规则下**恰好只在源节点困惑时才够格**——新边只为它的源放不下的内容而打开。两个机制自然咬合。

**节奏与候选数不是自由参数**：节点每被访问 $d_{head}$ 次重连一次（$d_{head}$ 正是它的矩阵能装的模式数），候选数 = 它自己的可塑度数。

### 18.2 结果，以及那个必须做的对照

10 万 token，其余同 §16.3。

| | prequential | 末十分位 | 访问/token | **最忙十分位** |
|---|---|---|---|---|
| 固定拓扑 | 431314.3 | 4.2342 | 474.7 | 20.4% |
| **周转（择优）** | **430755.5** | **4.2047** | 466.9 | **10.5%** |
| 周转（盲选，对照） | 434700.3 | 4.2738 | — | 19.3% |

**盲选对照是关键**：以相同的频率重连（250 万次）、但取第一个抽到的候选而非四个里访问最少的那个。它**比固定拓扑还差** 3386 比特，负载也毫无改善。

⇒ **起作用的是"择优"，不是"搅动"。** 随机重连反而有害——它毁掉 Kleinberg 采样出来的结构却没放进更好的东西。

### 18.3 真正的收获是负载，不是损失

损失只好 0.13%。但**最忙十分位从 20.4% 降到 10.5%，而完全均匀是 10%**——基本满载均衡。

这正是内稳态本该干却干砸了的活：§15.2 里它要价 0.58%，把最忙十分位从 20.4% 改善到 19.7%（即几乎为零）。结构性周转**倒赚 0.13%**，把它做到了。

> **负载均衡属于拓扑，不属于路由分数。** 我们删掉了一个在错误层面上做这件事的机制，它在正确的层面上免费实现了。

### 18.4 未解决

- **拓扑不收敛**：250 万次重连 / 576 节点 / 4 个可塑槽 ⇒ 每个节点重连约 5000 次，全程churn 不停。择优是有效的，但结构没有沉淀下来。更慢的节奏能否让结构固化？节奏是导出的（$d_{head}$ 次访问），改它就要改那条推导。
- **扩容仍未做**（决策 E）：秩饱和触发的加节点/加秩。这才是 §1.9 里"生长"那一半。

---

# 19. 绝对标尺：绑定输出头是全部问题，而这个基准测不出架构要测的东西（2026-08-07）

10 万 token，其余同 §18。加入两样以前没有的东西：**prequential bigram 编码器**（在线、单遍、KT 平滑，与其他一切同协议，因而码长同样合法）和**解绑输出头**。

## 19.1 结果

| 配置 | prequential 总量 | 末十分位 |
|---|---|---|
| 信源熵率 | — | 1.4181 |
| **bigram 编码器** | **154058** | **1.4331** |
| **解绑 bypass** | **193282** | **1.5242** |
| **解绑 + 网络** | 207248 | 1.5981 |
| 绑定 + 网络（§18 的"最好成绩"） | 430756 | 4.2047 |
| 绑定 bypass | 462415 | 4.6306 |

**两条结论，都必须直说：**

**① 绑定就是全部问题。** 解绑把损失从 4.63 降到 1.52 比特，省下 **269133 比特（58%）**。而网络有史以来最好的贡献是 31659 比特。**解绑一项的收益是网络全部贡献的 8.5 倍。**

**② 去掉这个残疾之后，网络是净负值。** 解绑 bypass 193282，解绑 + 网络 207248——**网络让结果差了 13966 比特（7.2%）**。

⇒ **§12 到 §18 里每一个 "+X% vs bypass"，测的都是"网络在多大程度上补偿了一个残废的输出头"。** 那些百分比是假象。

## 19.2 但真正的原因是基准，不是架构

信源是**一阶** Markov 链。最优预测器**只需要当前 token**——$P(\text{next}\mid\text{current})$ 就是全部真相，**没有任何上下文可用**。

所以"只看当前 token 的解绑 bypass"在这个任务上**按构造就是最优的**，任何额外处理只能添噪声。

> **这个基准无法测量这个架构存在的理由。** 我们造的是一个累积并使用上下文的系统，而信源里没有上下文。

这同时解释了 §11.2 ① 那个"绑定表天生偏向预测当前 token"为什么当时看起来只是个初始化性质——在一阶 Markov 源上，绑定的对称约束**同时**是主要瓶颈和几乎唯一被惩罚的东西。

## 19.3 哪些结论作废，哪些没有

**作废**：所有关于网络贡献幅度的绝对数字——§12.9 的 1.63%、§15 的 6.72%、§18 的改进量。分母是一个做不了这个任务的基线。

**不受影响**（协议性质或独立部件）：泄漏与序列化协议（§12.9）、停机保证、位级确定性、E0 全部（§8）、拓扑测量（§9）、绑定头的对称性/半正定限制本身（现在有测试）。

**状态未定**：网络内部的各项对照——阶梯必需（§14.2）、top-k 有害（§14.1）、surprise 规则（§15）、周转（§18）。它们比较的是网络的不同版本，作为"关于网络的陈述"仍然成立，**但在一个网络整体为净负值的任务上，其意义无法评估**。要在有上下文的信源上重做。

## 19.4 下一步：换一个有上下文的信源

一阶 Markov 必须换掉。最小改动是**高阶 Markov**：当前 token 不再充分，bypass 按构造次优，网络才有东西可挣。配套标尺升级为 **order-$k$ 计数编码器**，同样在线单遍。

**在换掉信源之前，不要再跑任何架构对照。** 本机的 $P$ 扫描已中止——它测的是"哪个 $P$ 最能补偿残废的输出头"。

---

# 20. 上下文能不能流过去（2026-08-07）

信源换成 order-2（$V=16$，256 个上下文），标尺换成两把：order-1 计数编码器（**无上下文模型的天花板**）和 order-k 计数编码器（**任何模型的天花板**）。两者之差就是上下文值多少。

## 20.1 order-2 上的第一轮：网络拿到零

| 配置 | prequential | 末十分位 |
|---|---|---|
| order-1 编码器 | — | **3.5813** |
| order-2 编码器 | — | 1.4404 |
| 解绑 bypass | 361400.3 | 3.6034 |
| 解绑 + 网络（内容寻址） | 362528.2 | **3.6035** |

**上下文值 2.1408 比特，网络拿到 0.0001。** 小数点后四位与 bypass 相同——**网络就是一个无上下文模型**。

order-1 回归对照重现 §19（网络 1.4913 vs bypass 1.4760，仍净负），所以基准是好的：它明确提供了 2.14 比特，而架构一比特没拿。

## 20.2 诊断：相邻 token 几乎从不经过同一个节点

2.8 跳在 576 个节点里触及约 10 个。入射是内容寻址的，而**相邻 token 内容上毫无关系**，所以两个约 10 节点的随机子集在 576 个节点里的**期望重叠是 0.17 个节点**。

token $p$ 写进去的状态，不在 token $p+1$ 的路径上。**架构有记忆，但没有按序重访记忆的机制。**

## 20.3 判决实验：固定锚点

所有 token 从同一处入射，共享强制且彻底。判据先于数据写下：**末十分位必须低于 3.5813**（跌破无上下文天花板是定性门槛，不是定量改进）。

| | prequential | 末十分位 | 最忙十分位 |
|---|---|---|---|
| 内容寻址 | 362528.2 | 3.6035 | 13.8% |
| **固定锚点** | **352103.8** | **3.4279** | 35.1% |

**3.4279 < 3.5813，跌破了。上下文机制是通的，堵点是寻址。**

但只拿到 2.1408 里的 0.1534（**7.2%**），而且负载垮了（最忙十分位 35.1%，均匀是 10%）。

> **固定锚点不是解法，是存在性证明。**
> 任何新方案的成功判据现在是明确的：**末十分位低于 3.5813，且负载不垮。**

## 20.4 一条被实测纠正的推断

我推断过"节点结构上就是一阶模型"：$W$ 训练在 $(\text{上次输入},\text{这次输入})$ 上，$Wq$ 给出 $\mathbb{E}[\text{next}\mid\text{current}]$，key 是单个碎片，无法区分不同前文。

**如果严格如此，它不可能跌破一阶天花板。** 漏掉的是 **payload 跨跳累积**：$u=q+\tanh(Wq)$ 使得粒子在第二跳时的 key 已经是"当前碎片 ⊕ 节点 1 的历史条件预测"。

**节点在自己的到达序上是一阶的；粒子在跳与跳之间不是。** 那 7.2% 就是从这条缝渗进来的——是副产品，不是机制，这也解释了为什么这么少。

**它不是上界。** 3.4279 是"共享最大、寻址最差"那个角落的值——一个同时做好共享和寻址的方案完全可以超过它。把它当天花板会低估后面的方案。

## 20.5 一个必须记下的怀疑：合成信源可能高估了"近因"

order-$k$ Markov 的全部依赖都是**近因**依赖：需要的上下文永远是最近 $k$ 个 token。任何把"流上相邻"变成"空间相邻"的方案（游标、桶）在这种信源上天然占便宜。

**真实语言不是这样。** 代词的先行词可能在很远之外，靠**内容**找到而不是靠位置。所以：

- **内容寻址给相似性，时间寻址给近因，真实语言两个都要。**
- 现在的设计只有相似性（并因缺少近因而失败）；纯游标只有近因。

即使游标在这个基准上成功，**它也只证明了近因那一半**。自然的合成方向是"游标定入射**区域**、内容定区域**内**的位置"——但这条只有在有长程依赖的信源（或真实语料）上才能被检验。**在此之前不要宣称寻址问题已解决。**

---

# 21. 游标入射：共享拿到了，几何不用动（2026-08-07）

order-2 信源，10 万 token，解绑输出头。天花板：无上下文 3.5813，任何模型 1.4404，**可用上下文 2.1408 比特**。

## 21.1 结果

| 入射模式 | prequential | 末十分位 | 吃下可用上下文 | 最忙十分位 |
|---|---|---|---|---|
| 内容寻址 | 362528.2 | 3.6035 | **0.0%** | 13.8% |
| 固定锚点 | 352103.8 | 3.4279 | 7.2% | **35.1%** |
| **游标** | 353303.4 | **3.4569** | **5.8%** | **10.1%** |

游标跌破无上下文天花板，拿到固定锚点 **81%** 的收益，而**负载 10.1% 基本就是均匀**（均匀为 10%）。

游标的定义没有可调量：token $t$ 从 $(t mod G,\ \lfloor t/G
floor mod G)$ 入射。步长来自"要在约 3 跳的路径长度下产生足迹重叠"，周期来自网格本身。

## 21.2 桶方案/球方案不需要建了

它们要解决的问题是"让相邻 token 共享节点而不堆积"。**一个只改入射的游标就解决了**，网格保持同质，于是：

- §18 的突触周转一行不用改；
- §1.9 的扩容不必面对"结构化几何长不了"（加环要重标时间坐标、球面细分一次乘 4）；
- 不用设计任何新几何。

**"§1.9 在新拓扑下怎么办"这个反对意见的答案，不是更聪明的扩容机制，而是不需要走到那一步。**

## 21.3 但只吃下 5.8%，瓶颈换地方了

固定锚点是"共享的极限情形"，游标已经拿到它 81%——**再优化共享也就这个量级**。所以剩下的 94% 不在"节点能看到什么"，在**"节点能表示什么"**。

这就是 §20.4 那个被纠正后剩下的一半：**节点的 delta rule key 是单个碎片**，所以 $W$ 给出的是 $\mathbb{E}[	ext{next}\mid	ext{current}]$。跨跳渗漏让它勉强跌破一阶天花板，但那是副产品。

下一步：**把 key 换成最近若干次输入的多时间尺度摘要**（挂阶梯 ⇒ 阶数是谱不是超参）。

## 21.4 未撤销的保留意见

§20.5 仍然成立：**游标只证明了近因那一半**。order-$k$ Markov 的依赖全是近因依赖，天然偏袒游标。真实语言里先行词靠内容找到。"游标定区域、内容定区域内位置"是自然的合成方向，但要有长程依赖的信源才能检验。**在那之前不宣称寻址问题已解决。**

---

# 22. 多时间尺度上下文 key（2026-08-07）

游标入射，order-2，10 万 token，解绑输出头。天花板同 §21。

## 22.1 结果：方向对，量很小

| `context_scales` | 末十分位 | prequential | 吃下可用上下文 |
|---|---|---|---|
| 1（= 原来的 last input） | 3.4511 | 353184.6 | 6.08% |
| 2 | 3.4345 | 352193.2 | 6.86% |
| 4 | 3.4220 | 351111.5 | 7.44% |
| 6 | 3.4156 | 350500.0 | **7.74%** |

**单调改善且饱和**（增量 +0.78 / +0.58 / +0.30 个百分点）。机制在做它被设计来做的事，但**它的贡献上限只有约 1.7 个百分点**。

## 22.2 两个诊断都对，但加起来只有 7.7%

- §20 的诊断（寻址：相邻 token 不共享节点）→ 游标 → **0% 变成 6.1%**；
- §21.3 的诊断（表示：key 是单个碎片）→ 多尺度 key → **6.1% 变成 7.7%**。

两个都被数据确认了方向，**但可用上下文的 92% 仍然没有拿到**。

**所以主要瓶颈不是这两个中的任何一个。** 继续猜下一个机制是没有依据的——需要一个能定位信息在哪一步丢失的诊断，而不是再改一处然后看数字。

秩不是显然的解释：单节点 $W$ 的秩 ≤ $d_{head}=16$，256 个上下文单节点装不下，但 576 个节点合起来是 9216，总容量绰绰有余。所以问题更可能在**信息能否穿过路径并在重组后保留**，而不是**能否被存下**。

## 22.3 一条必须记住的方法学约束

`scales=1` 在这一跑里是 3.4511，而 §21 那一跑的游标是 3.4569——**同一配置，不同二进制**。

原因是路由含离散分支（`logits[j] > reference` 的成员判定、排序），**$10^{-16}$ 的浮点差异可以翻转一个路由决策并宏观发散**。系统对给定二进制是位级确定的（§3 有测试），但**跨代码改动不是数值稳定的**。

> **只能比较同一次扫描内、同一个二进制产出的数字。** 跨 §之间的绝对值不可直接相减。

---

# 23. 瓶颈定位：上游一直在工作，读出把成果扔了（2026-08-07）

order-2 信源，游标入射，解绑头，10 万 token。天花板：无上下文 3.5813、可加 3.2089、任何模型 1.4404。

## 23.1 四个测量，逐步收紧

| 测量 | scales=6 | 说明 |
|---|---|---|
| 模型本身 | 3.4156 | |
| 线性探针 $\hat e\to$ next | 3.3371 | 现有形状的头**训到最优** |
| 单隐层探针 $\hat e\to$ next | 3.2655 | 某种非线性读出 |
| **解码后查表** | **2.1059** | 从 $\hat e$ 线性解出 prev，再用 $(\text{cur},\hat{\text{prev}})$ 查计数表 |
| prev 解码正确率 | **77.5%** | |

## 23.2 缺口分解（3.4156 → 1.4404，共 1.975 比特）

| 来源 | 比特 | 占比 |
|---|---|---|
| 优化（模型 → 线性探针） | 0.079 | 4.0% |
| 形状（线性 → 单隐层） | 0.072 | 3.6% |
| **查表能力（单隐层 → 解码查表）** | **1.160** | **58.7%** |
| 解码残错（22.5% 错误的代价） | 0.666 | 33.7% |

**66% 的缺口只靠换读出就能拿回来。** 而且需要的不是"某种非线性"——单隐层只拿到 3.6%，**查表拿到 62%**。

## 23.3 上游一直在正常工作

`context_scales` 1→6：

| | 解码正确率 | 查表损失 | **模型实际损失** |
|---|---|---|---|
| scales=1 | 38.7% | 3.1554 | 3.4511 |
| scales=6 | 77.5% | 2.1059 | 3.4156 |

**多尺度 key 的真实贡献是 1.05 比特，通过实际的头只体现了 0.036 比特——被扔掉了 97%。**

§22 判它"不是主要瓶颈"**撤回**。同理，§20/§21 里游标那些"方向对但量很小"的结果，也都是被同一个漏斗压住的。

## 23.4 我在这一段里两次把测量当成了它不保证的东西

**① 把单隐层探针当上限。** 它只是另一个弱学习器（18k 参数、单遍在线 SGD），给的是**下界**。证据：$\hat e$ 的 prev 可解码性从 2.97 改善到 1.02（1.95 比特），单隐层探针只动了 0.05 比特，而查表动了 1.05 比特。
**修法**：加入 oracle——无参数、无优化，不会因为学不动而低估。

**② 把 5000 token 的 38.5% 解码正确率当收敛值**，并据此推翻了自己前面正确的判断。10 万 token 下是 77.5%。
**修法**：探针类数字只在**末十分位**且**完整规模**下读。

两次是同一类错误，和 §22.3（跨二进制比较）、部署时那次（跑了旧二进制）也是同一类：**在比较之前，先确认这个数字真的度量了你以为的那个量。**

## 23.5 读出该做成什么

需要"查表"，而**网络里本来就全是查表**——路由就是拿 payload 匹配邻居的预期、按匹配度分配质量。读出是唯一纯线性的地方。

所以把同一个机制用在读出上：**一组 (key, 分布) 对，$\hat e$ 与 key 做内容匹配，输出是按匹配度加权的分布混合**。

- 是**软查表**，正是 oracle 证明有效的能力；
- 混合在**概率空间**，$p=\sum_m w_m p_m$ 在 log 空间天然非可加；
- 用的是路由已有的机制，不是从别处搬一个 MLP。

梯度是干净的：令 $r_m = w_m p_m[y]/p[y]$（成分 $m$ 的后验责任，$\sum_m r_m = 1$），则

$$\frac{\partial L}{\partial \text{value}_m} = r_m\,(p_m - e_y),\qquad \frac{\partial L}{\partial s_m} = w_m - r_m$$

即"解释得比自己权重更好的成分，提高它的分数"。key 不做归一化，**其模长充当学到的逆温度**，因此不需要温度超参。

**诚实记账**：成分数 $M$ 是一个**真的新超参**（读出的容量）。目前没有导出它的办法。

---

# 24. 查表读出：越过可加天花板（2026-08-07）

order-2 信源，游标入射，`context_scales=6`，10 万 token。

## 24.1 形式

$$p(\text{next}) = \sum_m w_m\,p_m(\text{next}),\qquad w=\mathrm{softmax}\big(\langle \hat x, \text{key}_m\rangle\big),\qquad p_m = \mathrm{softmax}(\text{value}_m)$$

**混合在概率空间**，因此在 log 空间非可加。梯度：令 $r_m = w_m p_m[y]/p[y]$（后验责任，$\sum_m r_m = 1$），

$$\frac{\partial L}{\partial\,\text{value}_m} = r_m(p_m - e_y),\qquad \frac{\partial L}{\partial\,\text{key}_m} = (w_m - r_m)\,\hat x$$

key 不归一化，模长即学到的逆温度 ⇒ 无温度超参。

## 24.2 第一次实现失败了，原因是初始化不是尺度自由的

| $M$（旧初始化，lr=0.05） | 末十分位 |
|---|---|
| 线性头对照 | 3.4156 |
| 64 | 3.9501 |
| 256 | 3.9609 |
| 1024 | 3.9812 |

比无上下文天花板（3.5813）还差，且**随容量单调恶化**。

原因是**混合模型的自锁**：key 初始模长按 $1/\sqrt{d_{model}}$ 取，分数散布只有 0.088，softmax 在 $M$ 个成分上几乎均匀 ⇒ $r_m\approx 1/M$ ⇒ key 和 value **都慢 $M$ 倍** ⇒ 门永远分化不了。$M$ 越大越锁死。

一个独立证据在单元测试里：`a_lookup_head_can_separate_what_a_linear_head_cannot` 用 $M=8$、lr=0.2 **通过**了。同一结构、换个尺度就能学出 XOR——那个测试当初正是为了隔离"结构 vs 优化"而写。

## 24.3 初始化的推导（不是调出来的）

分数 $s_m=\langle\hat x,\text{key}_m\rangle$。key 元素 iid $N(0,\sigma^2)$ 时，对任意单位 $\hat x$ 有 $s_m\sim N(0,\sigma^2)$——**与宽度无关**，否则 $d_{model}$ 也会耦合进来。

$M$ 个分数的最大者约 $\sigma\sqrt{2\ln M}$，归一化因子约 $M e^{\sigma^2/2}$。要求胜出者占 $O(1)$ 质量：

$$\sigma\sqrt{2\ln M}-\frac{\sigma^2}{2}=\ln M \;\Longrightarrow\; \boxed{\sigma=\sqrt{2\ln M}}$$

（重根，恰为临界点。）$M=64$ 时 $\sigma=2.88$，而原来是 0.088——**小了 33 倍**。

测试 `the_lookup_gate_starts_with_a_clear_winner_at_every_size` 钉的是**机制前提**（$M\in\{8,64,256,1024\}$ 的初始最大门权重 > 0.1）而非损失。

## 24.4 结果

| $M$ | lr=0.3 | lr=1.0 |
|---|---|---|
| 16 | 3.3892 | 3.3933 |
| 64 | 3.1078 | 2.8314 |
| 256 | 3.0617 | **2.6599** |

**最优 lr 不再随 $M$ 下沉**（原失效模式消失），且性能随容量单调改善。仅测了两个 lr，故"完全解耦"尚不能断言。

| | 比特 | 吃下可用上下文 |
|---|---|---|
| 线性头 | 3.4156 | 7.7% |
| 查表头（旧初始化，$M$=64） | 2.8931 | 32.1% |
| **查表头（推导初始化，$M$=256）** | **2.6599** | **43.0%** |
| 解码后查表 | 2.1059 | 68.9% |
| order-2 天花板 | 1.4404 | 100% |

**2.6599 低于可加天花板 3.2089。** 那条线是"在**完美知道 prev** 的前提下任何 log 空间可加的读出能到的最好水平"，任何线性读出都跨不过去；而本模型对 prev 只有 77.5% 可解码性，**在信息更差的情况下越过了它**——只能是因为表达了交互。§23 的诊断链到此闭合。

## 24.5 $M$ 的状态

$M$ 仍是**无依据的超参**。$M=256$ 恰等于信源上下文数 $16^2$，若收益在此饱和，$M$ 可解释为"读出要分辨多少种不同的条件分布"，是可从任务推出的量；若继续单调上升，它就是个真旋钮。容量扫描进行中，结论未定。

---

# 25. 第二次盘点（2026-08-07）

§17 是第一次横切面，之后架构变了很多。本节替代它。**最要紧的一条先说：**

> **§14–§18 的每一个机制决定，都是在那个后来被证明会扔掉 90% 以上信号的线性读出下做的**（§23）。所以那批"已定"实际上是"在已不成立的条件下定的"，必须重测才能保留。

## 25.1 目前的全流程

```
token
  ↓  嵌入表（随机固定；解绑后不再更新）
  ↓  固定正交旋转（随机符号 + Walsh-Hadamard，零参数）
  ↓  切成 P 片；方向→payload（单位范数），模长→mass（L1 守恒）
  ↓  游标入射：位置 (t mod G, ⌊t/G⌋ mod G)，每 token 走一格
  ↓
  ├─ 节点：多尺度上下文 key（τ_j = 2^j 的泄漏积分器之和，归一化）
  │        W: key → 当前输入（delta rule，η=1，挂阶梯 rungs=4）
  │        输出 u = 归一化(q + tanh(W·key))
  │        surprise σ = ‖q − W·key_before‖
  ├─ 路由：候选 = {j : ⟨u, expect_j⟩ > ⟨u, expect_self⟩ − σ}
  │        吸收份额只由内容算（两段式）；无 top-k、无内稳态
  ├─ 拓扑：4 条格点边永久 + 4 条长程边可塑；突触周转（择优，固定度数）
  └─ 停机：吸收 + 质量地板（无 max_hops）
  ↓  按 (token, slot) 质量加权求和 → 逆旋转 → ê
  ↓  查表读出：p = Σ_m w_m p_m，w = softmax(⟨ê, key_m⟩)，M=512
  ↓  prequential 交叉熵；序列化协议（一次一个 token 在途）
```

**当前成绩**（order-2 信源，10 万 token）：**2.5670 比特**，吃下可用上下文的 **47.4%**。

## 25.2 稳固的（与读出无关，不需要重测）

| # | 结论 | 依据 |
|---|---|---|
| 1 | **序列化协议**是唯一给出合法压缩上界的协议 | 泄漏 0.036 比特/token；bypass 两格逐位相同（§12.9） |
| 2 | **绑定输出头结构上做不了这个任务** | $EE^\top$ 对称且半正定，恒等式，有测试 |
| 3 | **阶梯的 $t^{-1/2}$**，$r\in[2,8]$ | 六个数量级内 −0.499，独立台架（§8） |
| 4 | **对数周期涟漪不存在** | 诱饵周期每档都不输（§8.4） |
| 5 | **Kleinberg 指数**：稳健的只有上界 $\alpha\le2$ | §9 |
| 6 | **位级确定性 / 无 all-reduce** | 有测试（§10） |
| 7 | **读出需要的是查表，不是泛泛的非线性** | 单隐层 3.6% vs 查表 62%（§23.2） |
| 8 | **概率空间混合越过可加天花板** | 2.5670 < 3.2089，且 prev 只有 77.5% 可解码（§24.4） |
| 9 | **查表 key 的初始化 $\sigma=\sqrt{2\ln M}$** | 推导，非拟合（§24.3） |

## 25.3 已废弃

**上一代**：fast/slow 模块二分、node/subnode 分裂、bandit 家族、attention+KV cache、`max_hops`、遗忘率 $\lambda$、load-balance 辅助损失、RL 路由。

**这一代实测废弃**：对数周期涟漪、重叠在途、随机序当卖点、读数式入射、top-k 截断、内稳态偏置、路由读深层 rung、§1.4 的 attention/FFN 二元论、§10.1 ④ 的解耦论证、绑定输出头。

## 25.4 重测结果：全部保住，但份量重排

在当前最佳配置下逐项关掉（基线 **2.5670**）：

| 关掉的机制 | 末十分位 | 代价（比特） | 原结论 |
|---|---|---|---|
| **节点阶梯**（rungs=1） | 3.4676 | **0.901** | 保住，且更重要 |
| **多尺度 key**（scales=1） | 2.8930 | **0.326** | **原判"不是主要瓶颈"彻底作废** |
| **surprise 吸收**（改用 relative） | 3.1323 | **0.565** | 保住 |
| **游标入射**（改用内容寻址） | 3.5597 | **0.993** | 保住，且是最大的单项 |
| **§1.9 突触周转**（冻结拓扑） | 2.5771 | **0.010** | 保住但份量极小 |

**没有一项被推翻**——这是好消息，说明旧读出压制了收益但没有颠倒方向。

但**份量完全重排**了。最要紧的两条：

- **游标入射值 0.993 比特**，而它在旧读出下只体现为 0.12 比特（§21，6.1% 的可用上下文）。**被压制了 8 倍。**
- **多尺度 key 值 0.326 比特**，§22 测出 0.036 并据此判它"不是主要瓶颈"。**被压制了 9 倍。** 这条判断此前已在 §23.3 撤回，现在有了直接的端到端确认。

### §1.9 的现状

**周转仍然有效，但损失上的贡献只有 0.010 比特（0.4%）。** 它真正买到的仍是负载：

| | 最忙十分位 |
|---|---|
| 周转开 | **10.1%** |
| 冻结 | 14.8% |

（均匀为 10%。）§18.3 的结论"负载均衡属于拓扑而非路由分数"**保住**，但要诚实标注：**在当前配置下它对预测质量几乎没有贡献**，价值在于负载与可扩展性，不在比特。

## 25.5 未决

| # | 问题 | 状态 |
|---|---|---|
| A | $P$ 是坐标划分还是路径重数 | 两次扫描都作废（欠功率 / 旧读出） |
| B | 网格是否承重 | 游标之后内容寻址已不用于入射，网格只剩"提供距离"这一个作用 |
| C | 有序深度轴 | 未测 |
| D | 路线 (a) 全局梯度 | **一行没写**；A1-pre 从未做 |
| E | 节点记忆低秩 / 扩容 | §1.9 的"生长"那一半仍未实现 |
| F | 路由预测还是残差（A8） | 未测 |
| G | $\hat e$ 里 prev 只有 77.5% 可解码 | 剩余 0.67 比特在这里 |

## 25.6 超参现状（诚实版）

**真超参**：$d_{head}$、网格边长 $G$、学习率、长程边数、阶梯比 $r$、`context_scales`、$M$。
**导出**：阶梯级数 $m$、查表 key 的 $\sigma$、周转节奏与候选数、游标步长与周期、slot 位移半径。
**推理期旋钮**：质量地板 $\epsilon$、$P$。

$M$ 是真超参但**不敏感**：256–2048 的 8 倍区间内差 0.027 比特。

---

# 26. 噪声底：这一轮扫描里有一半是噪声（2026-08-07）

## 26.1 测量

固定信源与 token 流，**只变结构种子**（拓扑 + 模型初始化）。为此把 `--seed` 拆出了 `--structure-seed`——原先一个种子同时控制数据和结构，两个效应分不开。

当前最佳配置，6 个种子：

| | |
|---|---|
| 均值 | 2.6118 |
| 标准差 | **0.0514** |
| 全距 | 0.1400 |

两个独立单跑之差的标准差是 $\sigma\sqrt2=0.073$，故 **单跑对比需超过约 0.145 比特才显著**。

系统对 $10^{-16}$ 扰动宏观发散（§22.3，路由含离散分支），所以这个量级的方差是结构性的，不是实现问题。

## 26.2 按 0.145 重新评定

**站得住**：查表头 0.849 · 游标入射 0.993 · 节点阶梯 0.901 · surprise 吸收 0.565 · 多尺度 key 0.326 · $M{=}512$ vs $64$ 0.264 · $\alpha{=}2$ vs $0$ 0.219

**作废（噪声内）**：$\alpha{=}2$ vs $1$/$3$（0.044/0.068）· $M$ 在 256–2048 之间（≤0.093）· **$P$ 扫描全部**（≤0.13）· 网格大小（≤0.03）· §1.9 周转的损失贡献（0.010）

**一处系统性偏差**：基线 2.5670 用的是默认种子，比六种子均值低 0.87σ——是偏运的抽样，所以所有"X vs 基线"的对比都**系统性地对 X 不利**。凡是"X 略差于基线"的结论都要按这个折价。

## 26.3 配对与非配对不是一回事

- **配对**（只切换机制，构造不变）：周转、`rungs`、吸收规则、`context_scales`、入射模式、以及 RNG 修复后的读出类型。两次跑的拓扑与初始化完全相同。
- **非配对**（改变构造，随机流随之改变）：$\alpha$、网格大小、$P$、`long_range`、以及修复前的 $M$。等同于换种子。

非配对对比按 0.145 判定。配对对比噪声更小，但**一个配对差值仍只是一个样本**，要跨种子重复才能得到效应的均值。

## 26.4 流程变更

1. **所有对照默认至少 3 个结构种子**，报均值与配对差；
2. 单种子数字在文档里标注为"单跑，未定"；
3. 基线不再用某一个幸运种子，用多种子均值。

成本涨约 3 倍。在一个对 $10^{-16}$ 扰动都会发散的系统上，单种子对照本来就不该被当作结论——**这一条应该在 §12 端到端跑通时就建立，晚了十几节**。

## 26.5 顺带确认的两条（与方差无关）

**$\hat e$ 与读出完全无关。** 查表头与线性头的探针逐位相同（3.5985 / 1.6876 / 1.0205）。网络与读出是两个可分别优化的子系统，剩余预算 0.461（读出）+ 0.665（网络）互不干扰。

**决策 A（$P$ 的含义）在这个基准上无法回答。** 非单调、无趋势、全距 0.13 在噪声内，而参数量差 64 倍。既不支持"碎片化有价值"也不支持"是负资产"。留到有长程依赖的数据上再说。

## 26.6 §1.9 的多种子结论

配对消融（同拓扑同初始化，只切换周转），4 个结构种子：

| seed | 周转 | 冻结 | 差 | 负载(on) | 负载(off) |
|---|---|---|---|---|---|
| 1 | 2.6270 | 2.6382 | +0.0112 | 10.1% | 14.8% |
| 2 | 2.5804 | 2.5866 | +0.0062 | 10.1% | 15.0% |
| 3 | 2.5477 | 2.5933 | +0.0456 | 10.1% | 15.1% |
| 4 | 2.5813 | 2.5932 | +0.0119 | 10.1% | 15.0% |

配对均值 **+0.0187**，sd 0.0181，**四个种子全为正**（符号检验单侧 $p=1/16$；$t=2.06$，3 df，单侧 $p\approx0.07$）。

**负载是决定性的**：开周转时**每个种子都恰好 10.1%**（均匀为 10%），冻结时 14.8–15.1%。周转把负载压到均匀，与初始拓扑无关。

**结论**：§1.9 保留。§18.3 的"负载均衡属于拓扑而非路由分数"成立且现在有多种子证据。但**其价值在负载与可扩展性，损失上的贡献很小**（+0.019 比特，$n{=}4$ 尚未确证超过噪声）。作为"可扩展性"这条核心主张的唯一实现，保留有充分理由。

---

# 27. 算力-质量前沿：稀疏激活这条主张目前不成立（2026-08-08）

当前最佳配置，每档 **3 个结构种子**（§26.4 新流程下的第一个扫描）。

| 质量地板 $\epsilon$ | 损失均值 | sd | 访问/token | 比特/千次访问 |
|---|---|---|---|---|
| 0.03 | 3.6052 | 0.0119 | **8.4** | −2.85 |
| 0.01 | 3.3617 | 0.0398 | 64.6 | 3.40 |
| 0.003 | 3.1947 | 0.0632 | 107.6 | **3.59** |
| 0.001（默认） | **2.5850** | 0.0399 | 548.1 | 1.82 |

## 27.1 §1.6 的 anytime 主张成立

损失随 $\epsilon$ 单调改善、算力单调上升。**"调大就更快更糙"第一次被验证**，而且没有免费午餐——算力确实在买质量（108 → 548 次访问换 0.61 比特）。$\epsilon$ 作为推理期旋钮是名副实归的。

## 27.2 但 §0 的"超稀疏激活"目前不成立

$\epsilon=0.03$ 时每 token 仅 **8.4 次访问**——那才是"稀疏"该有的量级。而该档损失 **3.6052，比无上下文天花板 3.5813 还差**：

> **在真正稀疏的工作点上，网络一点上下文都用不上。**
> 当前机制下，上下文能力需要约 **500 次访问/token** 才兑现。

效率峰值在 $\epsilon=0.003$（3.59 比特/千次访问），但该档绝对质量（3.19）离天花板仍远。**没有又稀疏又好的工作点。**

这不是观感问题，是可测量的：§0 一句话图像里"实现超稀疏激活"这条**目前没有实现**，应当降级为待解决问题，而不是当成已有性质。

## 27.3 算力的去向

每 token 548 次访问 × 每次访问约 4000 flops（其中约 75% 是节点阶梯的 relax）≈ **2.2M flops/token**。对照：order-2 计数编码器每 token 约 $V$ 次运算就达到 1.4404。

我们用六个数量级的算力换到的东西，一张 256 项的查找表用几十次运算就超过了。这在合成信源上是必然的（表就是最优解），但它标定了一件事：**当前架构的算力效率不是"接近最优"，而是"远远不是"**，任何关于效率的主张都必须带上这个前沿图。

# 28. E2 持续学习：第一次端到端验证（2026-08-08）

§0 的三条核心主张里，"解决灾难性遗忘"是最贴近这个架构存在理由的一条，也是**从未在系统层面测过**的一条。§1.8 的阶梯（Benna–Fusi 式扩散耦合链）就是为它设计的，但 E0 只测了**单个联想矩阵**的保持曲线——整条系统在域切换下的行为完全可能与单矩阵的行为分离。

## 28.1 基准

多域信源：$k$ 条互相独立的 order-2 Markov 链，每条连续喂 `domain_span` 个 token，循环切换。不改架构，只加信源。

记录量：**重返成本**——每次访问某个域的**开头五分之一**上的平均损失，按"这是第几轮经过这个域"索引。开头五分之一而不是整段，是因为要测的是"回来时起步有多贵"，不是"待久了能学多好"。

判读标准（事先声明）：

- 跨轮**下降** ⇒ 域特定结构被保留了下来
- 跨轮**持平** ⇒ 灾难性遗忘

对照：bypass（旁路网络，只有输出头作为记忆）。头会被后来的域覆写，所以它是"没有分布式记忆"这个假设下的预测曲线。

配置：4 域 × 每次 5000 token × 12 轮 = 24 万 token，3 个结构种子（§26.4 流程），其余为当前最佳配置。

## 28.2 结果

| 轮次 | 网络 | bypass |
|---|---|---|
| 0 | 3.9844 ± 0.0083 | 3.9741 ± 0.0027 |
| 1 | 3.9847 ± 0.0076 | 4.0113 ± 0.0033 |
| 2 | 3.9171 ± 0.0056 | 4.0083 ± 0.0018 |
| 3 | 3.9129 ± 0.0139 | 4.0126 ± 0.0035 |
| 4 | 3.8794 ± 0.0116 | 4.0042 ± 0.0034 |
| 5 | 3.8817 ± 0.0186 | 4.0056 ± 0.0033 |
| 6 | 3.8746 ± 0.0025 | 4.0222 ± 0.0042 |
| 7 | 3.8207 ± 0.0202 | 4.0100 ± 0.0040 |
| 8 | 3.7991 ± 0.0242 | 4.0065 ± 0.0011 |
| 9 | 3.7930 ± 0.0116 | 3.9877 ± 0.0015 |
| 10 | 3.8038 ± 0.0111 | 4.0229 ± 0.0041 |
| 11 | **3.7603 ± 0.0178** | 4.0211 ± 0.0052 |
| **变化** | **−0.2240** | **+0.0470** |

网络的重返成本近乎单调下降 **0.224 比特**，逐点 sd 约 0.01–0.02，远超 §26.1 的 0.145 显著性阈值；bypass 持平甚至略微变差。

> **回到一个域时，网络比上一次更便宜；只有输出头的对照不会。**

对照隔离出了**节点的分布式记忆**是做保留的那个东西——这正是 §1.8 阶梯的设计目的。E0 的单矩阵保持曲线与整系统行为**没有分离**。

## 28.3 需要排掉的混淆

网络的改善也可能来自**泛化性变好**（对任意 Markov 链都更擅长了），而非**域特定的保留**。bypass 不改善是旁证，但它受限于自身容量，不是干净的"泛化"对照。

对照：跑到 75% 处把其中一个域**换成一条全新的链**（`--fresh-domain-at`），此后继续按 §28.1 记录。

判读要**配对**。第一版判读标准写成"新域首访 vs 轮次 0"是错的：轮次 0 是在**未训练**的模型上测的，而新域首访发生在模型已经热身之后，所以即使保留完全是域特定的，新域也会比轮次 0 好——这个比较没有控制热身。

正确的对照是**同一批轮次内**：

$$\Delta = \underbrace{L(\text{换新的那个域})}_{\text{模型没见过}} - \underbrace{L(\text{其余三个域})}_{\text{模型见过 9 次}}$$

两者在**相同的轮次**上读出，模型的总体熟练度、头的状态、拓扑的成熟度全部相同，唯一的差别就是这条链见没见过。

- $\Delta > 0$ 且可观 ⇒ 被保留的是**域特定结构**
- $\Delta \approx 0$ ⇒ 跨轮的改善只是**泛化变好**，§28.2 不能算作持续学习的证据

## 28.4 控制实验结果：保留确实是域特定的

4 域 × 5000 × 12 轮，75% 处把域 0 换成一条全新的链，3 个结构种子。

| | 比特 |
|---|---|
| 换新的那个域（模型没见过） | 4.1507 ± 0.0207 |
| 其余三个熟悉域（同一批轮次） | 3.8003 ± 0.0258 |
| **配对差 $\Delta$** | **+0.3504 ± 0.0417** |
| （参照）轮次 0，未训练模型 | 3.9844 ± 0.0083 |

$\Delta = 0.3504$，跨种子 sd 0.0417，是 §26.1 显著阈值的 2.4 倍。**混淆排除**：§28.2 的跨轮下降是域特定的保留，不是泛化能力变好。§0 的持续学习主张第一次在端到端层面成立。

## 28.5 保留不是免费的

新域（4.1507）比轮次 0（3.9844）**还差 0.17 比特**。轮次 0 时网络是空的；到 75% 处，节点记忆已被四条已建立的链占住，一条新链要挤进去。

这是稳定性-可塑性张力的直接测量：**这套机制的保留是有代价的，代价落在新知识的获取速度上**。0.17 比特这个量级应当与 0.3504 一起报告——只报好的那一半会让持续学习的主张显得比实际更便宜。

## 28.6 绝对水平的说明

本节的绝对损失（3.76–3.98）远差于单域下的 2.585，这是预期的：每个域每次只见 5000 token，总量被四等分，且不断被打断。**本节唯一有意义的量是跨轮的斜率与对照的斜率之差**，绝对值不可与其他章节横比。

# 29. E3 迷失在中间：基准与正对照（2026-08-08）

§0 的第三条从未测过的主张。经典设置：把一条信息埋在长上下文的不同位置，最后统一提问，看回忆质量随"埋得多久以前"怎么变。Transformer 呈 U 形（两端好、中间差）；本架构的主张是**平的**。

## 29.1 设置

- 针 $i$ 是一对符号 $(k_i, v_i)$，取自词表**顶端保留的** $2R$ 个符号，信源建在剩下的 $V-2R$ 个上，因此一个键在整个运行中**恰好出现两次**：教的时候一次，问的时候一次。不保留的话键也会作为普通信源输出出现，检索就是不适定的。
- 教学位置在运行主体上**均匀铺开**；提问全部挤在**结尾一个块**里，块内顺序**打乱**，使块内位置与"多久以前"无关。
- 针是**拼接**进流的，不消耗马尔可夫链的抽样，所以底层信源序列没有被打断。
- 计分：`Scored` charge 的是 `position` **之后**那个 token，所以打分位置取键的槽位；代码里对 `s.token`/`s.target` 做了断言，不靠"应该对齐"。

**哨兵**：取 `--vocab 40 --needles 12` 时 source_vocab = 16，与标准配置的信源完全同构。实测熵率 **1.4145 = 1.4145**（逐位相同），确认加针没有扰动信源。

## 29.2 为什么必须有正对照

一次曝光的零结果有两种读法：**架构做不到一次性绑定**，或者**这个基准看不见绑定**。二者不能靠盯着数字区分。§23 已经因为"把弱学习器当成天花板"栽过一次。

所以加 `--needle-repeats`：每根针教 $R$ 次。$R=1$ 是一次性测试；$R$ 大是正对照——若大 $R$ 下仍然处处随机，那是**测量**有问题，本节任何结论都不成立；若大 $R$ 下出现回忆而 $R=1$ 没有，零结果才是关于**架构**的。

距离-质量曲线只有在正对照通过之后才可解读。

## 29.3 正对照通过了

40k token，12 根针，`--vocab 40`，其余为当前最佳配置。单种子，此处只用于确认基准的灵敏度，不用于任何架构结论。

| 教学次数 | 最老 (28.5k–37k 前) | 中间 | 最新 (5.7k–14k 前) |
|---|---|---|---|
| 1 | 5.2995 | 5.2912 | 6.4849 |
| 4 | 4.4480 | 4.2760 | 5.2397 |
| 16 | 1.3737 | 1.0772 | 1.1466 |
| 64 | 0.5145 | 0.4300 | 0.1763 |

回忆随曝光单调加强，从随机水平一路到 0.18 比特。**基准看得见绑定**，所以 $R=1$ 的零结果可以是关于架构的陈述，而不是关于基准的。

## 29.4 但均匀分布是错的参照线，第一版读数作废

上表用的 "chance = $\ln V$ = 5.3219 比特" **不是**这个任务的零假设。

针符号很稀有：$R=1$ 时每个值在 4 万 token 里只出现 4 次，模型给它的边际概率远低于 $1/V$。所以**得分高于 $\ln V$ 仍然可能是回忆**，得分等于 $\ln V$ 反而可能已经是相当强的回忆。$R=1$ 那行"最新 6.4849 比 chance 还差"看着像反常，其实只是参照线放错了位置。

用错参照线的话，平坦曲线和衰减曲线我都会读错，整个实验作废。这与 §23 把弱学习器当成天花板是同一类错误：**零假设必须是构造出来的，不能是想当然的**。

正确的零假设必须在**曝光次数和稀有度上完全匹配**，只在绑定的对错上不同：

> **配对 foil**：把针分成两半，两半教学方式完全相同；提问时一半问它自己的值（matched），另一半问**另一根针的值**（foil）。两组的值被教了同样多次、同样稀有，唯一的差别是这把键对不对。

$\text{gap} = L_{\text{foil}} - L_{\text{matched}}$ 就是回忆量，而 **gap 随距离的形状**才是 §0 那条主张。gap 平 ⇒ 不迷失在中间；gap 随距离衰减 ⇒ 迷失。

正式实验（120k token，24 根针，3 个结构种子，网络与 bypass 对照）结果见 §29.5。

## 29.5 结果：迷失在中间不存在

120k token，24 根针（12 matched / 12 foil），3 个结构种子。

**每根针教 16 次**

| taught ago | 网络 matched | 网络 foil | 网络 gap | bypass gap |
|---|---|---|---|---|
| 87.7k–115k | 1.2016 | 7.9433 | 6.7417 ± 1.3641 | **7.9696 ± 0.1762** |
| 50.7k–78.4k | 1.8027 | 7.1104 | 5.3077 ± 1.9216 | **7.7565 ± 0.3484** |
| 13.8k–41.5k | 1.1758 | 7.9202 | 6.7444 ± 0.7052 | **8.2999 ± 0.4033** |

**每根针教 1 次**：gap 为 0.6887 ± 0.6642 / −0.3969 ± 1.0539 / 0.2419 ± 1.2595 —— 全在噪声里。**没有一次性绑定。**

距离跨 8 倍量程（1.38 万到 11.5 万 token），gap **没有系统性形状**：既非 U 形也非近因衰减。逐种子看波段排序完全不一致（网络 s1 最新最好，s2、s3 最老最好；bypass 同样杂乱）。bypass 的跨波段离散仅 0.733 比特，网络 2.077 比特。

> **迷失在中间：在这个基准上不存在。**

## 29.6 但这个基准把功劳记错了地方，bypass 更强

| | 网络 matched | bypass matched | 配对差 |
|---|---|---|---|
| 9 个配对（3 波段 × 3 种子） | — | — | **+1.023 ± 0.999，9/9 同号** |

符号检验 $p = 2^{-9} \approx 0.002$。**bypass 比网络做得更好**，而 bypass 除了输出头没有任何记忆。

原因是基准设计的缺陷，应当在设计时就看出来：**针是 `键 → 值` 的二元组，这是一个 order-1 任务**。§11 已经证明未绑定的头配 $\eta=1$ 的 delta 规则可以一次精确存下一个二元关联。所以这个任务头单独就能满分，网络的上下文机器不但多余，还**主动损害了精确检索**——提问时的上下文与教学时不同，键经过网络后的表示因此漂移，检索变成近似的。

这给出一条独立的、负面的、可复现的测量：

> **上下文整合以精确回忆的精度为代价，量级约 1 比特。**

因此 §0 那条主张现在的准确状态是：

- **系统层面**：不迷失在中间 ✅
- **归因到分布式网络**：❌ 本基准无法支持，因为头单独就够，且更好

## 29.7 下一版必须让针需要上下文

要把功劳记给架构，针必须是"只看最后一个 token 证明不够"的：**键用两个符号 $(k_1, k_2)$，且每个符号在多根针间复用**，值由这一对决定。此时 order-1 的头 provably 无法消歧，必须记住 $k_1$——而记住 $k_1$ 正是网络该做的事。

这不是新设计，是基准的修正；§29.5 的平坦性结论在新版下需要重测，§29.6 的 1 比特代价则应当保留为独立结果。

# 30. E3 第二版：让针需要上下文（2026-08-08）

## 30.1 布局

针 $i$ 的键是一对符号 $(k_1, k_2) = (\lfloor i/P \rfloor,\ i \bmod P)$，值由**这一对**决定，共 $P^2$ 根针。每个 $k_2$ 出现在 $P$ 根针里、对应 $P$ 个不同的值，所以

$$\text{只看最后一个 token} \Rightarrow \text{答案在 } P \text{ 个里均匀} \Rightarrow L \ge \ln P$$

$P=5$ 时这条线是 **2.3219 比特**。它是**算出来的，不是解释出来的**——低于它就必须把 $k_1$ 带过来了，而带 $k_1$ 过来正是网络该做的事。这是 §29 那版缺的判别器。

保留符号 $P + P^2 = 30$，`--vocab 46` ⇒ source_vocab = 16，熵率哨兵 **1.4145**，与标准配置逐位相同。

## 30.2 第一次运行的波段形状不可读（我的赋值方式坏了）

第一次运行把教学位置取成 $\propto i$。但共享同一个 $k_2$ 的五根针是 $i, i+P, \dots, i+4P$，于是**每把键的最后一次写入必然落在最新波段**——波段成员身份与"我是不是这把键的最后一次写入"完全共线。

测出来的陡峭梯度（最新 2.50 → 最老 6.30）因此测的是**后写覆盖先写**，平凡且与距离无关。

修法：教学位置取**随机置换**，且 matched/foil 改按**位置奇偶**而非针序号奇偶划分，既解开共线又保住两半的距离配对。

这一条与 §29.4 的参照线错误是同一个教训的两个面：**基准里任何与"位置"相关的量，都必须检查它是不是和别的东西共线。** 合成基准的自由度太高，构造者很容易把想测的效应和平凡效应捆在一起。

## 30.3 归因结论（第一次运行版，下一节修正）

| | 网络 | bypass |
|---|---|---|
| 跌破 2.3219 天花板 | **0 / 9** | **0 / 9** |

matched 的 9 个配对差（网络 − bypass）为 $+0.1326 \pm 0.3053$——网络还略差一点，但在噪声内。

天花板这一条不依赖波段划分，所以不受 §30.2 的缺陷影响：**网络从未完全解开这对键。**

但"没有任何增益"这个更强的说法是**错的**，它来自共线版把所有波段混在一起看。解开共线后（§30.4）网络在近距离上有真实增益。写在这里作为记录：**一个坏掉的分组不只会污染分组内的形状，它还会污染跨组汇总的结论。**

## 30.4 定稿：网络重新分配回忆，但不增加回忆

置换布局，120k token，25 根配对键针，**9 个结构种子**，配对 t 检验（df = 8，$|t| > 2.31$ 为 $p < 0.05$）。

| 波段 | 网络 | bypass | 配对差 | $t$ |
|---|---|---|---|---|
| 88.8k–115k 前 | 7.0572 ± 0.3489 | 6.7527 ± 0.2727 | +0.3044 ± 0.4675 | +1.95 |
| 53.3k–80.0k 前 | 6.5292 ± 0.4138 | 6.6370 ± 0.4345 | −0.1078 ± 0.6264 | −0.52 |
| 17.8k–44.4k 前 | 5.5574 ± 0.4790 | 5.9371 ± 0.2697 | −0.3797 ± 0.5100 | −2.23 |

| 汇总量 | 配对差 | $t$ | 同号 |
|---|---|---|---|
| 近因斜率（最老 − 最新） | **+0.6842 ± 0.6930** | **+2.96** | 8/9 |
| **总回忆量**（三波段均值） | **−0.0610 ± 0.2950** | −0.62 | — |
| 跌破 2.3219 天花板 | **0 / 54** | — | — |

> **网络把回忆重新分配给了近期，但没有增加总回忆量。**

斜率显著变陡（$p \approx 0.018$），总量的配对差却是零。网络在最新波段赚到的，在最老波段原样还了回去。而 0/54 说明它**从不解开这对键**——它没有把 $k_1$ 带到绑定里，只是让近期的条目在竞争中占了上风。

## 30.5 三种子那版没有活下来

同一实验 3 个种子时，最新波段的配对差是 **−0.885 ± 0.285（3/3 同号）**，我据此写过"网络的上下文绑定有有限视界"。9 个种子后缩到 **−0.380 ± 0.510（7/9）**，$t = -2.23$，勉强在边缘。

均值回归。**§26 那条 0.145 的方差教训在这里第二次生效**，而且这次是在一个我已经知道要多跑种子的地方——3 个种子的配对 sd 本身就是个不可靠的估计量，它既可能夸大也可能缩小效应，不能当作"效应很干净"的证据。

更要紧的是两种说法对设计的含义完全不同：

- **"有限视界"**（作废）：网络增加了绑定容量，容量随距离衰减 ⇒ 该去延长视界
- **"重新分配"**（成立）：容量没变，只是权重挪位 ⇒ 延长视界是无效功，缺的是让 $k_1$ 进入绑定的机制

第一种说法会把后续工作引向完全错误的方向。这是本轮最值得记住的一次修正。

# 31. 定位 §30.4 的缺口：两次猜错（2026-08-08）

§30.4 的结论——网络重新分配回忆但不增加回忆，且从不跌破 order-1 天花板——需要定位到具体机制才有下一步。记录两次猜测和它们怎么被否掉，因为**否定的过程比结论有用**。

## 31.1 猜测一：上下文进不了记忆的键。错

读代码即证伪：

```rust
self.memory.write(&scratch.key_before, q, params.eta)    // 键 = 折入 q 之前的上下文键
self.memory.read().mul_vec(&self.key, &mut scratch.pred) // 读 = 含 q 的上下文键
```

节点记忆本来就是**由多时标上下文键寻址**的，不是由载荷寻址。`more_timescales_make_the_key_depend_on_older_inputs` 这个既有测试明确断言 $\text{key}(a,b) \neq \text{key}(c,b)$。做合取绑定的机制一直都在。

**教训**：在提出机制之前先读代码。这次的猜测完全来自对架构的印象而非实际实现，而实现和印象差了一整层。

## 31.2 猜测二：键的上下文宽度写死了，且当前值在坏的一侧。也错

诊断是：键 $= \mathrm{normalize}(\sum_j c_j)$，$k_2$ 权重约 2、$k_1$ 约 0.6、其余是更早的源 token；而教学时针嵌在源流中、提问时针在查询块中，**慢尺度成分在两个时刻不匹配**。预测：宽度扫描应出现内点最优。

实测（120k、配对键针、每档 5 个结构种子）：

| `context_scales` | 均值 | 跌破天花板 |
|---|---|---|
| 1 | 6.3179 ± 0.235 | 0/15 |
| 2 | 6.4171 ± 0.127 | 0/12 |
| 4 | 6.1758 ± 0.360 | 0/15 |
| 8 | 6.5001 ± 0.251 | 0/15 |

**全平，0/57。** 决定性的是 `scales = 1`：此时键恰好是 $k_2$ 的载荷，$k_1$ **结构上不可能**进入节点的读取——它却和 `scales = 8` 打平。

> 节点键的构成对这个任务的结果不起作用。**瓶颈不在节点读取。**

一并作废的还有基于这个诊断排出的三个机制方案（惊异选宽度、多键混合、乘性绑定）——它们都是在一个不是瓶颈的地方增加自由度。

## 31.3 下一个猜测，以及为什么它更可信

已有数据里有一条现在变得关键：**前一个 token 从组装表示中的线性可解码率是 77.5%**。$k_1$ **在**组装向量里，信息没有丢失。

所以问题不是"$k_1$ 到不了"，而是"**$k_1$ 到了，但读出用不上**"。这条与 §29.6（网络的上下文扰动反而损害精确检索）以及 §30.4（重分配而非增容）都相容。

如果成立，它意味着 §25 记的两个预算——读出 0.461 比特与网络 0.665 比特——**可能不是两个缺口而是一个**。

判别不需要新设计：查表头在概率空间混合（§24），原则上能表达合取，但槽位要分配得到这些组合。扫 `--head-slots`，若加槽能跌破 2.3219，则网络一直没问题。

## 31.4 第三次猜测也错了，但错法给出了答案

槽位扫描（120k、配对键针、每档 5 个结构种子）：

| `head_slots` | 均值 | 跌破天花板 |
|---|---|---|
| 512 | 6.2739 | 0/12 |
| 2048 | 6.4922 | 0/15 |
| 8192 | 5.9609 | 0/15 |

16 倍槽位换来的差别在噪声里，**0/42**。读出容量也不是瓶颈。

三个猜测全错，共同点是：**我一直在猜哪个部件不够，却从没测过信息在不在。** 而 §23 的教训是探针失败只给下界，证明不了不存在——所以需要一个不依赖学习器的测量。

## 31.5 检索几何：绑定在，而且只可能来自网络

对每根针取它最后一次教学时的组装向量与提问时的组装向量，在共享同一个 $k_2$ 的 $P$ 根针中问：提问向量的最近邻是不是它自己的教学向量？随机为 $1/P$。这是表示的几何性质，与任何学习器无关。

120k，6 个结构种子：

| | 命中率 | $z$ | margin |
|---|---|---|---|
| **网络** | **94/150 = 62.7%** | **+13.1** | +0.00793 ± 0.01001 |
| bypass | 30/150 = 20.0% | +0.0 | +0.00000 |

bypass 精确落在 $1/P$、margin 精确为 0，因为它的组装表示就是 token 嵌入，共享 $k_2$ 的五根针向量完全相同，只能靠 tie-break——这同时是这个测量没有偷跑的证据。

> **网络做出了合取绑定，而且这个绑定只可能来自网络。**

因此 §30.4 的标题结论"重新分配回忆但不绑定"**是错的**。损失侧的测量全部有效，错的是解释。正确的说法是：**绑定了，但绑定读不出来**。

## 31.6 病因是幅度：埋在 token 决定的成分底下

margin 只有 0.008 余弦。把共享 $k_2$ 的组内均值减掉——组均值正是**当前 token 决定的成分**，减掉后剩下的恰好是依赖 $k_1$ 的部分：

| | 命中率 | margin |
|---|---|---|
| 原始组装向量 | 56% | +0.0024 |
| 去掉 token 条件均值 | **76%** | **+0.1730** |

margin 放大 **72 倍**。至此所有平坦的扫描都有了同一个解释：

| 观测 | 原因 |
|---|---|
| 加 16 倍槽位无效 | 判别方向只带 0.008，容量再大也读不出 |
| 上下文宽度扫描全平 | 信号在不在与宽度无关 |
| 重分配而非增容（§30.4） | 信号太弱，改变不了 argmax |
| order-1 针上 bypass 更强（§29.6） | 头直接拿 $k_2$；网络那点合取信号淹没在扰动里 |

**§25 记的两个预算（读出 0.461、网络 0.665）是同一个缺口**：网络已经算出了读出丢掉的东西。

## 31.7 机制：读出前减去 token 条件均值

对每个词表条目维护组装向量的在线均值，喂给头之前减掉。

- **针对测到的病因**，不是针对猜的病因——前三个方案都死在这一点
- **无超参**：均值是逐次平均，没有系数
- $O(d)$/token，不加重 §27 标出的算力短板
- **不给粒子加字段**
- 均值在**计完损失之后**才更新，减法背后的平均只含更早的 token，损失仍是合法的前序编码长度

守卫测试两条：关掉时逐位等同旧路径；开启时运行均值收敛到该 token 固定的成分。

风险，一并记下：头看到的是残差而非原始表示，§11.2 的透传性质会变，**以 passthrough 为基准的历史数字需要重测**。

## 31.8 只减不还是净损失，$b_t$ 是必要的补完

20k 上 centring 变差（5.2841 → 5.5210），我最初判断这是欠收敛。**部分对，但更重要的原因是提案本身有缺陷。**

减掉 token 条件均值，减掉的不只是妨碍读出的成分，**还有 order-1 证据本身**——那是这个读出手里最有用的单一信息。头只看残差就再也不知道"当前 token 单独预测什么"。

我在提案时写"直接针对测到的病因"，这句没错；漏掉的是**病因和药效落在同一个成分上**。而且诊断里的检索几何**对此免疫**（最近邻在组内比较，order-1 成分对所有候选相同、自动抵消），损失却不免疫。所以几何上 72 倍的放大，不蕴含损失上的收益——**我当时把这两个指标对同一个减法的敏感性当成了一回事**。

120k 上的实测（3 种子，尚无 $b_t$）：

| | 标准基准（末十分位） | 配对键针（matched 均值） | 几何 |
|---|---|---|---|
| centring off | 2.5122 ± 0.0450 | 6.2523 | 30/50 = 60% |
| centring on | **2.3775 ± 0.1064** | 7.7716 | **51/75 = 68%** |

配对增益 **+0.1348 ± 0.1333，3/3 种子改善**（$t = 1.75$，尚不显著）。前序总长 343,203 → 340,976。

方向恰好相反的两个任务，同一个解释：标准基准上上下文持续有价值，减掉噪声净赚；针任务的天花板**本身就是 order-1 水平**，减掉 order-1 证据自然最疼。

## 31.9 $b_t$ 的形式：查表头上是多一个专家，不是 logit 偏置

线性头上 $b_t$ 是字面的 logit 偏置。但当前最佳配置用查表头，而它在**概率空间**混合——直接加 logit 会把 §24 确立的那条性质破坏掉。

所以做成**每 token 一个专家**：分布是按 token 学的一行，门控 logit 也按 token 学，与槽位争同一份 softmax 质量。梯度**没有新规则**，责任度公式原样适用：

$$r_t = \frac{w_t\, p_t[y]}{p[y]}, \qquad \frac{\partial L}{\partial\,\text{values}_t} = r_t(p_t - e_y), \qquad \frac{\partial L}{\partial\,\text{gate}_t} = w_t - r_t$$

附带的好处是门控**学的正是"何时该信 bigram、何时该信网络"**，而这本来就是希望自适应的量。

守卫测试三条：关掉时逐位等同旧路径；运行均值收敛到该 token 固定的成分；**把组装向量固定住使残差趋零、槽位只能预测均匀，此时学到的一切必然经由 $b_t$ 通道**——最后一条测的是目的而不是"能跑"。

代价：`token_values` 是 $V \times V$。基准尺度无所谓，真实词表需要因子化或稀疏化。

## 31.10 操作事故：部署脚本杀掉了自己的实验

为部署新版本，部署步骤里的 `taskkill /F /IM annp.exe` 杀掉了当时正在跑的 6 种子几何确认作业。下游分析拿到空结果、报除零。

失败形态值得记：**它不报错，只让分析拿到空数据**——最难发现的那一类。同一个无条件 taskkill 已经误杀两次。

改法：部署前先 `tasklist` 查询，非零则中止部署。构建产物被占用是真实问题，但解法不能是"杀掉一切同名进程"。

## 31.11 $b_t$ 在两个任务上都没赚到东西，原因是我给了它一个没推导过的初值

120k、6 种子（标准基准 5 种子，一个种子 ssh 断开）：

| | 标准基准（末十分位） | 配对键针（matched） | 几何 |
|---|---|---|---|
| centring off | 2.5241 | 6.3695 | 94/150 = 62.7% |
| centring on + $b_t$ | **2.3614** | 8.1268 | 94/150 = 62.7% |
| 配对增益 | **+0.1497 ± 0.1130**, $t = 2.96$, 5/5 | **−1.7573 ± 0.6378**, $t = -6.75$, 0/6 | — |

几何两侧完全相同，这是**对的**：centring 发生在读出内部、不改变表示。当作一致性检查它通过了。

而 $b_t$ 的贡献**约等于零**：seed 1 是 2.2802 vs 2.2802（无 $b_t$），seed 3 是 2.3612 vs 2.3611。上表 +0.1497 全部来自 centring。

### 原因

槽位 logit 是 query 与 $\sigma = \sqrt{2\ln M}$ 尺度的键做点积，故 $\text{logit} \sim \mathcal{N}(0,\, 2\ln M)$，$M$ 个中的最大值约 $\sigma\sqrt{2\ln M} = 2\ln M \approx 12.5$（$M = 512$）。

我把 $b_t$ 的门控 logit 初始化成了 **0**。softmax 里它拿到的质量是 $e^{-2\ln M} = M^{-2} \approx 4\times 10^{-6}$。

> 这不是"起步弱"，是**结构性排除**：责任度小到梯度推不动门控，它永远爬不上来。

单元测试之所以通过，是因为那里残差被强制归零、槽位只能预测均匀——**该测试证明了通道能工作，却证明不了它在竞争中能被使用**。这是测试设计的一个真实盲点，记下来。

### 修法是推导出来的，不是调出来的

$$b_t^{\text{gate}}(0) = 2\ln M$$

与获胜槽位平起平坐：既不强制开启，也不结构性排除。**与 §24 定 $\sigma$ 用的是同一个论证**，所以它是导出量而非旋钮。

如果按此初始化后 $b_t$ 仍然赚不到，就删掉——加了复杂度却测不出好处正是 KISS 要拦的东西。

# 32. 外部审查与修复（2026-08-08）

上真实语料之前，请一个独立审查者全面过一遍：代码、模块间反馈、数理推导、可复现性，并明确要求**把注释和本文档都当作待验证的断言**。

## 32.1 核心算法是干净的

独立重推并逐行核对后确认与实现一致的部分：阶梯的显式欧拉步与通量记账、$C_k \propto r^k$ / $g_k \propto r^{-k}$、$m$ 的公式、delta 规则在 $\eta=1$ 的一次精确性、查表头的责任度梯度（含 $b_t$）、$\sigma=\sqrt{2\ln M}$ 是重根、旋转的往返精确性、绑定头 $EE^\top$ 的对称性论证、质量守恒（含地板截断走吸收而非丢弃）、线程无关的确定性、前序更新次序（含 `observe_centre` 在计损之后）、串行模式的排空、xoshiro256++ 与 SplitMix64 的常数、`next_below` 的拒绝采样无偏、各处 softmax 的减最大值、`checked_pow` 防溢出。

**没有发现任何数学断言与实现不符。** 这一条本身是结论：几何放大 72 倍而损失零收益（§31.6）**不是编码错误**。

## 32.2 四条真问题，全在流程与方差估计

| # | 问题 | 位置 |
|---|---|---|
| 1 | CLI 默认值是被废弃的配置 | `main.rs` `embed_rungs=3`、绑定头、`ingress=Content` |
| 2 | `run`/`topology` 从不写 manifest | 文件头声称"每个子命令都写"，只有 `e0.rs` 写 |
| 3 | `turnover_rng` 硬编码常数种子 | `runtime.rs:175`，不随 `--structure-seed` 变 |
| 4 | 拓扑与模型初始化共用一条 RNG 流 | `run.rs`，`small_world` 的抽取次数依赖 `long_range` |

第 1 条实测：裸跑 `annp run` 得 3.9413，**比无上下文天花板 3.6014 还差**——正是 §19–§21 诊断并修掉的那个病态系统。我们记录的数字不受影响（每次都显式传旗标），但工具的默认行为与文档相反。

第 2 条是**注释撒谎**的实例，正是本次审查特意要找的那类。

第 3 条的影响必须分两种情况，不能一概而论：

- **非配对比较**（§26.1 的 $\sigma=0.0514$ 与 0.145 阈值）：周转随机性在种子间固定，测出的方差**漏掉一个结构随机源，是低估**，阈值偏松
- **配对比较**（§28 的 $+0.3504$、§31 的 $+0.1497$，同种子 on/off 相减）：周转固定反而是**匹配对照**，差分消掉，**不受影响**

第 4 条是同一个坑的第三个实例（前两次见 §17 与 §24）。

## 32.3 修复，每一条都用会失败的检查钉住

1. 默认值改为文档所述配置，绑定改成 `--tied`（默认解绑）。裸跑现在是 **3.2045**，低于无上下文天花板 3.6014 与可加天花板 3.2708
2. `run`/`topology`/`e0` 统一写 manifest：git 版本 + **完整 argv** + 解析后配置的 `Debug` 转储。用 `Debug` 而非逐字段手写，是因为手写的那份正是会腐烂的那份——新增字段自动进去
3. `turnover_rng` 改为 `structure_seed ^ salt`
4. 三条结构流各自加盐：拓扑、模型、周转互不干扰
5. 删掉 dead 的 `turnover_surprise`（每次访问算一次、唯一调用点用 `_` 丢弃）
6. 给 $b_t$ 补真实竞争下的守卫测试

**每个守卫都验证过它在对应 bug 下会失败**：

- $b_t$ 的新测试在零初值下报 FAILED（专家只拿到 $2.9\times10^{-7}$ 门控质量），而**旧的那个"目的性测试"在同一 bug 下照样通过**——盲点被演示了出来
- 流分离的检查：`--bypass` 下（结果只取决于模型初始化）扫 `long_range`，修复前是 11301.8 / 11305.1 / 11380.1，修复后**三档完全相同**

> 一个从没见过它要防的 bug 的守卫，不是守卫。这一条现在是本项目的规矩。

## 32.4 重标定：噪声底比记录的高 29%

修复 3 与 4 之后，当前默认配置、120k token、**8 个结构种子**：

```
2.4501  2.5719  2.4911  2.5845  2.6151  2.5960  2.4538  2.5743
```

| | sd | 两次运行的显著阈值 $2\sqrt{2}\,\sigma$ |
|---|---|---|
| §26.1（周转被钉死在常数种子） | 0.0514 | 0.145 |
| **本次（周转随结构种子变化）** | **0.0664** | **0.1877** |

均值 **2.5421**，这是修正后的当前基线。

方向与 §32.2 的预测一致：**旧的 σ 少采样了一个结构随机源，是低估**。阈值上移 29%。

### 哪些结论受影响，哪些不受

- **非配对的单次对比**：任何落在 $[0.145,\ 0.1877]$ 区间内、并据 0.145 判为显著的差异，**现在不再被支持**。§26.2 与 §27 中处于这一带的判定需按新阈值重读
- **配对比较不受影响**：§28 的 $+0.3504 \pm 0.0417$ 与 §31 的 $+0.1497 \pm 0.1130$（$t=2.96$，5/5）都是同种子 on/off 相减，周转在两侧相同、差分即消。它们各自带着自己的配对 sd，从来没有用过 0.145 这条线

特别说明后一条，因为它容易被误读：§31 的 0.1497 **小于**新阈值 0.1877，但那条线是给**非配对**比较用的，对配对检验没有约束力。混淆二者会把一个成立的结论错判掉——这与 §29.4 用错零假设是同一类错误的镜像。

# 33. 真实语料桥接（2026-08-08）

## 33.1 tokenizer 是自带的，且逐 token 钉死

不引依赖，理由与 `rng.rs` 相同：**一个只能靠外部包的某个版本才能重现的测量不算测量**。而 tokenizer 比多数依赖更危险——分词悄悄变了，下游每个数字都跟着变，却永远不会报错。

从模型自己的 protobuf 里读出来的事实（不是从惯例假设的）：

- `model_type = BPE`，分数是**取负的 merge rank** 而非对数概率 ⇒ 推理是贪心配对合并，**不是** unigram 的 Viterbi 格
- `normalizer = identity`，`precompiled_charsmap` 为空 ⇒ 没有 NFKC 表要重实现
- `add_dummy_prefix = false` ⇒ 不补前导空格，`"The cat"` 的首片是 `The` 而非 `▁The`
- `escape_whitespaces` 缺省即 true ⇒ 空格先变 U+2581
- 256 个 `BYTE` 型 piece ⇒ 有字节回退，正常文本永远不会变成 `<unk>`
- 词表 **262144**，不是语料文件名暗示的 32001

**校验**：与参考实现 `sentencepiece` 逐 token 比对 4000 篇、284,909 个 token，**零不匹配**。测试里固定了 10 个短句的 id 序列作为回归守卫。

### 写的过程中修掉的一个静默 bug

待合并对入堆时按当时的右端算出目标字符串；此后右半边可能又向右合并使其变长，而"两者仍相邻"的检查**依然成立**，于是这条陈旧记录被弹出并合并，产出一个**与入堆时不同**的字符串。若它不在词表里（如 `▁Etext`），整段退回字节。

参考实现靠在配对里记录长度、弹出时比对来挡住。这个 bug 不崩、不报错，只让所有下游数字建立在错误分词上——**没有逐 token 对照就永远发现不了**。

## 33.2 词表必须截断，且要报告代价

输出头是 $V \times d_{model}$，$b_t$ 是 $V \times V$。26 万片的完整清单不是当前架构扛得动的东西，所以按语料频次保留最高频的 $V-1$ 个、其余并入兜底 id，并**把落入兜底的 token 比例与结果一起报告**。

排序按 (频次, piece id) 双键，避免结果依赖哈希表的遍历顺序。

## 33.3 绝对标尺按预算开关，缺席时明说

order-$k$ 计数编码器要 $V^{k+1}$ 个计数格。$V=16$ 时无所谓，$V=4096$ 的 order-2 要**半个太字节**。所以每个标尺只在装得下固定预算时构建，装不下就**报告为不可用**，绝不静默换成另一个。

这一条是防止 §29.4 那类错误重演：**参照线缺席比参照线错误好，但两者都必须让读者看见**。

## 33.4 流式读取

按 `input_text` 键流式扫描 JSON 数组，只保留 id 向量，**内存由 `--tokens` 决定而与语料大小无关**。手头这份 11 MB 本可以整个读进来；坚持流式是因为下一份不行，而一个只在某个尺寸以下能用的加载器，会在最不该重写的时候被迫重写。

测试用 1/2/3/7/64 字节的分块强制读取边界落在键名、转义序列和多字节字符中间——这正是整文件加载器永远遇不到、流式加载器天天遇到的失败。
