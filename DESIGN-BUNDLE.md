# DESIGN-BUNDLE：上下文作为地址空间上的平坦联络

> **状态**：机制草案 + 判决实验（2026-08-20）
> **定位**：不是阶梯的改进，也不是阶梯的替代——阶梯不在这个方案里。
> 这是对「地址由什么决定」这一条从未被检验的公理的替换。

## 1. 问题不是"记忆不够好"，是一个旋钮控制着两件互相冲突的事

Mode B 要求同一个 `(entity, relation)` 按上下文映射到不同 target。
此前所有稳定性机制（多时标阶梯、EWC、外积张量）在这里全部失败，
原因不在它们各自的实现，而在于**它们都被要求去修一个寻址问题**：
$\Phi$ 终身冻结、top-$k$ 确定，同一个 key 结构性地落在同一批列上。

那么把地址空间放大是否可行？实测（$k{=}16$，span 8000，Mode B）：

| | $D{=}512$ | $D{=}8192$ |
|---|---|---|
| 域间重叠 | 0.463 | **0.256**（分离变好） |
| 同一编码保持@1 步 | 0.378 | **0.221**（稳定变差） |
| 同一编码保持@100 步 | 0.120 | **0.039** |
| 0-shot 保持率 | 38.1% | 37.1%（**净持平**） |

两个方向相反的效应精确抵消。机理是：**top-$k$ 对输入扰动的敏感度由单一标量 $k/D$ 决定**，
所以这一个旋钮同时放大「把不同域分开」与「把同一事实的不同时刻也分开」。
**它们无法独立设定。**

> 这条结论此前差点被误读成「寻址不是瓶颈」。它不是。
> 消融检验的是**被转动的那个旋钮**，不是挂靠在它上面的那个概念。

## 2. 几何视角：地址空间是上下文之上的纤维丛

不要把 $T\in\mathbb{R}^{V\times D_1\times D_2}$ 读成"$D_2$ 个矩阵"——那只是矩阵集合换个写法。
读成：

- **底空间** = 上下文
- **纤维** = 内容地址空间
- **同一纤维坐标、不同底点 = 全空间中不同的点**

关键不在丛本身（平凡丛 $B\times F$ 就是外积，实测容量掉 4 倍），
**关键在纤维之间的联络**：上下文变化时，内容码被**平行移动**，而不是被切分进某个子块。

三种联络构成一个连续谱，两端都已实测：

| 联络 | 隔离 | 容量 | 实测（Mode B, span 20000） |
|---|---|---|---|
| 恒等（可加，现状） | 弱（重叠 0.446） | 满 | 54.0% |
| **正交移动（本方案）** | 随上下文距离连续变化 | **满** | 待测 |
| 投影到子块（外积） | 硬 | **÷4** | 18.8% |

## 3. 两条从几何直接落到实现的硬约束

**(a) 地址只能是 $f(\text{context},\text{key})$。**
写入时有 `(context, key, value)`，读取时只有 `(context, key)`。
surprise 依赖 value，因此 $A_{\text{write}}=g(z,\tilde s)$ **在读取时算不出来**——写进去就取不回来。
把地址存下来可以绕开，但那就是间接表（DESIGN.md 用图承担了这个角色）。

> 推论：**surprise 可以决定"写多少"，不能决定"写在哪"。**

**(b) 联络必须是平的（零和乐）。**
域轮转 $1\to2\to3\to4\to1$ 是上下文空间中的闭环。
若移动依赖路径，转一圈后域 1 的地址已经漂走，上一圈写入的位置这一圈不再去看——
**保持率的损失变成结构性的，与记忆机制无关**。
平坦 $\Longleftrightarrow$ 移动只依赖当前上下文。已加入操作检验（`cycle_return`，实测 0.794 @ $D{=}512$）。

## 4. 机制

$$u \;=\; \Phi\big(R(\hat z_{\text{ctx}})\,\hat z_{\text{content}}\big),
\qquad R=\prod_{p} G_p(\theta_p),
\qquad \theta_p=\gamma\,(a_p\cdot\hat z_{\text{ctx}})$$

- $G_p$：作用在内容坐标对 $(2p,2p+1)$ 上的 Givens 旋转；$a_p$ 单位范数、固定随机
- 内容 = rung $[0,\text{split})$，上下文 = rung $[\text{split},m_{in})$，默认 $\text{split}=m_{in}/2$
- $R$ 正交 $\Rightarrow$ **双射**：内容空间维度一个不少，$k/D$ 一点不动，域内编码稳定性保持原样
- 域内上下文近乎恒定（实测上下文档保持 0.92）$\Rightarrow R$ 近乎恒定 $\Rightarrow$ 地址稳定
- 跨域上下文不同 $\Rightarrow R$ 不同 $\Rightarrow$ 地址分离
- **分离量是上下文距离的连续函数**，无阈值、无状态、无反馈环、纯前馈

唯一新超参：$\gamma$（弧度量级的旋转增益）。

## 5. 判决实验（含内部对照）

同 seed、同参数预算、同 $\eta$ 网格 $\{1,3,10,30\}$，Mode B：

| 臂 | 含义 |
|---|---|
| `base` | 现状：全 trace 可加投影 |
| `g0` | **内部对照**：只投影内容、不旋转。分离"去掉可加上下文"与"旋转本身"的贡献 |
| `g0.5 / g1 / g2` | 机制，扫增益 |

`g0` 这一格是必须的：没有它，任何提升都无法区分是旋转带来的，
还是仅仅因为把上下文从可加投影里拿掉了。

**判据（事先写死）**：
- 若 `g1 > base` 且 `g1 > g0` $\Rightarrow$ 旋转本身有贡献，寻址确实是瓶颈，方向成立
- 若 `g1 ≈ g0` $\Rightarrow$ 旋转无贡献，收益（若有）来自别处
- 若两者都 $\le$ `base` $\Rightarrow$ 该方向判死，不再投入

Mode A 同时跑 `base` 与 `g1`：该模式一阶信息即足够，旋转**不应**带来收益；
若它在那里也涨，说明测到的是别的东西，结论不可信。
