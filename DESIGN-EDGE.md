# DESIGN-EDGE：路由固定、记忆在边上、类标识作为张量的一维

> **状态**：机制定稿 + 判决实验（2026-08-21）
> **定位**：这不是既有方案的改良。三条路已被实测封口——巩固阶梯无收益、
> 寻址不是我以为的瓶颈、拓扑分布式栽在检索一致性——本方案换的是承载记忆的**部件**。

## 0. 一句话

> 节点不再有记忆，只做**固定的、初始化时生成的**路由；
> 记忆挂在**边**上，并按**类标识**分片；
> 保护来自"写在不同的切片"，不来自"写得慢"。

## 1. 为什么是这个形状：三次失败的唯一共同死因

| 变体 | 路由熵 | **路由一致性** | Mode B |
|---|---|---|---|
| 硬 argmax + 全局 trace | 3.45 | 0.078 | 0.0% |
| 质量分流 + 全局 trace | 3.46 | 0.144 | 0.0% |
| 硬 argmax + 局部上下文 | 4.00 | **0.057** | 1–2% |

16 个节点的随机命中率是 $1/16 = 0.0625$。**三次的一致性都在随机水平**——
写进去的东西，读的时候在别的地方找。机制每次都在跑（熵接近满值），检索每次都失败。

共同死因不是任何一次的实现细节，而是：**路由的判据会漂移**。
自适应的节点状态 + 离散 $\arg\max$ = 任意微小差异被放大成完全不同的终点。

一体化记忆之所以能工作（Mode B 38–54%），正因为 top-$k$-of-$D$ 是**渐变**的：
不同时刻的稀疏码部分重叠，学习能在共享列上累积。离散节点没有"部分重叠"。

**本方案的对策**：让路由**无状态**。节点的边键 $K_n$ 在初始化时随机生成后永不改变，
payload 又是内容的确定性函数，于是**同一事实永远走同一条路径**——
一致性不是调出来的，是构造出来的。

## 2. 机制

### 2.1 前向（$d=32$，$H=3$ 跳）

$$p_0 = \mathrm{norm}\big(\mathrm{emb}(e)+\mathrm{emb}(r)\big)$$

$$j_h = \arg\max_{j\in\mathrm{out}(n_{h-1})}\big\langle K_{n_{h-1}}[j],\ p_{h-1}\big\rangle$$

$$a_h = E[j_h,\,c]\,p_{h-1},\qquad p_h = \mathrm{norm}\big(p_{h-1}+\tanh(a_h)\big)$$

$$\mathrm{logits} = R\,p_H$$

- $K_n$：节点 $n$ 的固定随机边键。**无状态、不学习、不漂移。**
- $E[j,c]$：边 $j$ 在类 $c$ 下的 $d\times d$ 联想记忆。**这就是"连接本身有记忆有处理"。**
- $R$：共享的小读出，$V\times d$。

**边上不挂阶梯。** 单时标。保护来自分配（不同类切片、不同边），不来自慢时标——
再挂阶梯就是把已经证伪三次的东西请回来。

### 2.2 类标识

Mode B 的实体跨域共享，$p_0$ 在四个域里**逐位相同**，所以类不能是 payload 的函数。
数据里唯一的域信号是近期 target。

用**一个全局慢速 EMA**（不是 8 档阶梯），$\tau=\mathrm{span}/5$——
域内稳定、切域后几分之一访问跟上——再用 $C$ 个固定随机原型 $\arg\max$ 量化。
按域快照/恢复，与现有探针协议同构。

> 这个慢信号此前被塞进 $H$ 步的混沌路由，被放大成噪声。
> 这里它只做**一次**离散决定，而这正是它稳定性够用的场合
> （实测：跨域重叠 0.105、域内保持 0.922）。

### 2.3 学习：沿走过的路径反传，只此一条

$$\frac{\partial L}{\partial p_H}=R^\top(\mathbf{p}-e_y)$$
$$\frac{\partial L}{\partial u_h}=\frac{I-p_hp_h^\top}{\lVert u_h\rVert}\frac{\partial L}{\partial p_h},
\qquad
\frac{\partial L}{\partial a_h}=\frac{\partial L}{\partial u_h}\odot\big(1-\tanh^2a_h\big)$$
$$\frac{\partial L}{\partial E[j_h,c]}=\frac{\partial L}{\partial a_h}\otimes p_{h-1},
\qquad
\frac{\partial L}{\partial p_{h-1}}=\frac{\partial L}{\partial u_h}+E[j_h,c]^\top\frac{\partial L}{\partial a_h}$$

只更新**实际走过的 3 条边**。路由器不参与求导——路径是硬结构，只有边参数学习，
一致性因此不受学习影响。

**这偏离了 DESIGN.md「不反传、只用局部信用」的立场，是有意的。**
替代方案是再发明一条未经验证的局部规则，而那正是本项目反复翻车的做法。
用精确的信用分配，被检验的才是机制本身，而不是规则设计的手气。

### 2.4 参数预算（$V=1536$）

| | 大小 |
|---|---|
| $E[64\ \text{边},\,8\ \text{类},\,32,32]$ | 524k |
| $R[V,32]$ | 49k |
| emb + 路由键 | 51k |
| **合计** | **624k** |

一体化基线 $1536\times512=786\text{k}$。**本方案更少**，对照是保守的。

## 3. 三个开题要素怎么落地

- **分布式（拓扑意义）**：记忆分布在 64 条边上，粒子在图上多跳，路径决定用哪几条边。
- **塑性–稳定性**：二者对立**只在参数共享时成立**——共享才逼你在"改得快"和
  "别改坏"之间选。分配消解它：新知识落在别的类切片/别的边，**全速写入**，
  而持有旧知识的切片**未被触碰**。没有慢横档、没有锚点、没有罚项。
- **有效遗忘**：长期无流量的 $(边,类)$ 切片衰减、容量回收。
  失去的是**不再被使用的**，而不是被最新一次写入撞坏的。

## 4. 事先写死的判据

1. **路由一致性必须 $=1.000$**（冒烟测试即可见）。构造保证；不是 1.0 说明理解有误，**当场停**。
2. **类稳定性**：域内切换次数 $\approx 0$，跨域必须切换。否则类维度是摆设。
3. **Mode B 必须超过一体化基线**。打平即不成立。
4. **Mode A 阴性对照**：一阶信息即足够，本机制**不应**带来大幅收益；
   若在那里也大涨，说明测到的是别的东西（旋转方案正是被这一条否掉的）。

## 5. 两条必须写在前面的风险

1. **类本质上是在线推断的域标签。** 无监督、类数不等于域数，但这是本设计最软的地方，
   审稿人必问。
2. **路径不学习。** 系统学不会"换条路走"，只能在给定路径上把参数调好——
   这是换取一致性的代价，意味着**路由质量完全由初始化决定**。
   若判据 1 通过而结果仍差，下一个要查的就是它。

---

# 结果（2026-08-21，seed 1）

## 6. 判据逐条核对

| # | 判据 | 结果 |
|---|---|---|
| 1 | 路由一致性 $=1.000$ | **通过**（前三个变体 0.078 / 0.144 / 0.057，随机水平 0.0625） |
| 2 | 类域内稳定、跨域切换 | 通过（0.003 切换/token） |
| 3 | Mode B 超过一体化基线 | **通过**（73.3% vs 40.8%） |
| 4 | Mode A 阴性对照不应大涨 | **未通过**——见 §8，判据本身错了 |

判据 1 在冒烟测试里当场抓到一个真实缺陷：第一版一致性只有 0.527，因为路由器虽无状态，
但喂给它的 payload 已被边记忆改写，第 2、3 跳因而依赖学出来的参数、路径随训练漂移。
分成「路由用原始 $p_0$、处理用累积 payload」之后才真正到 1.000。
**盯准确率永远发现不了这个缺陷。**

## 7. 主结果与三个被排除的替代解释

Mode B，$V{=}2560$，切片数固定为 $16\times8\times(\text{1 跳}) = 512$：

| 事实数 | 每切片事实数 | mono | **edge** |
|---|---|---|---|
| 520 | 1.02 | 40.8% | **73.3%** |
| 1600 | 3.12 | 10.9% | **72.3%** |

**替代解释一：更深的非线性 + 反传赢过单层线性。** 排除——1 跳（94.2%）优于 3 跳（79.0%）。
收益不随深度增加，深度反而有害。

**替代解释二：查找表。** 512 个切片对 520 个事实几乎一一对应，是我挑参数时无意造成的。
排除——负载提到 3.12 事实/切片，**edge 只掉 1pp**（73.3→72.3），而 mono 崩塌（40.8→10.9）。
优势随负载从 1.8 倍扩大到 6.6 倍，与容量伪影方向相反。

**替代解释三：类只是额外地址位。** 排除——见下。

## 8. 收益的成分分解，与一条被推翻的判据

把类的来源从推断的上下文换成 $\mathrm{hash}(e,r)$，**切片总数完全不变，上下文信息归零**：

| | mono | 类=上下文 | 类=hash |
|---|---|---|---|
| **Mode B**（实体共享，必须靠上下文） | 40.8% | **73.3%** | **4.4%** |
| **Mode A**（实体域特异，不需上下文） | 11.9% | 56.5% | **60.4%** |

Mode B 里 hash 把 73.3% 打到 4.4%——比基线还差，因为它让同一个 $(e,r)$ 在四个域里
落进**同一个**切片，四个冲突 target 正面对撞。Mode A 里 hash 毫发无损。

于是收益分成两块，各自的作用范围不同：

| 成分 | 何处生效 | 证据 |
|---|---|---|
| **分配粒度**（切片多 → 干扰少） | 两个模式 | Mode A 中 hash $\approx$ 上下文；3 倍负载只掉 1pp |
| **类携带上下文** | **仅在需要上下文处** | Mode B 73.3→4.4；Mode A 无影响 |

**判据 4 是错的，不是结果错了。** 它的前提是「Mode A 不需要上下文，所以机制不该帮」，
但 Mode A 同样有域轮转干扰，共享参数下域 1 的训练照样覆写域 0——分配机制在那里当然有用。
关键是：这条重新解释**由一个独立的新对照支持**（Mode A 的 hash 控制），而不是事后辩解；
$c1$（类数=1）掉到 2.7% 则单独确认了类维度不可移除。

复核者原本预测「类只是地址位」，被数据否定。

## 9. 尚未成立的部分

1. **单一 seed、单一配置。** 双种子、更大规模的复现仍在跑。
   上一个方案（上下文旋转）正是在 seed 2 上翻车的，此条不出来不算数。
2. **定向遗忘目前有害**：开启后 13.5%（关闭 79.0%）。未查。
3. **类实质上是在线推断的域标识。** 无监督、8 类对 4 域、系统从未被告知域数，
   但这是本设计最软的地方，且 §8 已证明收益的关键成分正在于此。

## 10. 【已作废，见 §21】读出层是最后一条共享通道，堵上它才有节省效应

边切片按 $(边,\text{类})$ 分配，域 1–3 训练时碰不到域 0 的切片。但读出 $R[V,d]$ 全局共享，
每步对全词表稠密写——**域 1 的训练必然覆写域 0 要读回来的那些行**。
分配保护了记忆，没有保护读出。

把读出也按类分片 $R[\text{类},V,d]$ 之后，每轮内遗忘量（bits，12 轮，$\text{span}=4000$）：

| 读出 | 逐轮遗忘量 | 方向 |
|---|---|---|
| 共享 | +3.73 +4.96 +5.35 +5.68 +5.74 +5.84 +5.91 +6.13 +6.19 +6.30 +6.36 +6.39 | **单调上升** |
| **类索引** | +4.94 +3.93 +3.22 +2.82 +2.68 +2.62 +2.59 +2.54 +2.50 +2.46 +2.31 **+2.17** | **单调下降** |

**共享读出下越练忘得越多；类索引读出下越练忘得越少。**12 个点单调，不是噪声。
这就是"遗忘后重学习逐步减小遗忘"的节省效应，而它**只在读出层也被分配时出现**。

### 10.1 一个被我自己制造出来又消除的假权衡

初次对比时类索引读出只有 49.8%（共享 73.3%），看起来是"节省效应要拿准确率换"。
但那次为了塞进预算把 $d$ 从 32 砍到 16，**同时动了两个变量**。补齐 2×2：

| 读出 | $d{=}16$ | $d{=}32$ |
|---|---|---|
| 共享 | 48.5% | 73.3% |
| **类索引** | 49.8% | **76.5%** |

49.8% 完全是 $d$ 减半的代价（共享在 $d{=}16$ 也只有 48.5%）。**同 $d$ 下类索引读出更好。**
不存在权衡：它同时给出更高的准确率**和**节省效应。那个"权衡"是混淆对照的产物。

## 11. 规模复现（seed 1）

| | mono | edge |
|---|---|---|
| span 8000, 520 事实 | 40.8% | 73.3–76.5% |
| **span 20000** | **54.0%** | **97.1%** |

优势随规模与负载扩大，与容量伪影方向相反。

## 12. 一个反复出现的错误模式，值得单独记一条

本项目至今有**五次**同类错误，每次都是把一个「每步」的常数当作「每周期」的常数：

| 出错处 | 症状 | 真因 |
|---|---|---|
| EWC anchor/Fisher trail | $\lambda{=}0$ 恒胜 | $\tau{=}20$ 步 vs 域访问 6666 步 |
| 输入阶梯泄漏 | 编码冻结、$m_{in}$ 与 hub_ratio 全无影响 | 无泄漏 → 随机游走式增长 |
| 权重阶梯深度 | $m{\ge}4$ 灾难性 | $\tau$ 超过一次域访问的档跨域平均 |
| 节点上下文 EMA | 路由一致性 0.057 | $\tau{=}100$ 次吸收 vs 一次访问 10000 次 |
| 定向遗忘率 | 开启后 13.5% | 0.05/步 → 离开一轮存活 $3\times10^{-4}$，湮灭 |

**结论：任何带时间常数的量，定值前必须先换算成"相对于现象周期"的单位。**
定向遗忘按此换算后（$10^{-5}$/步，离开一轮存活 0.92）伤害消失，但也不再带来收益——原因见 §13。

## 13. 有效遗忘目前无法被检验，这是信源的缺口

信源四个域无限轮转，**没有任何知识会永久过时**。而定向遗忘的价值前提是
"容量确实被耗尽，且旧知识确实不再需要"——这个情形在当前基准里从未出现。

遗忘率扫描（$\text{span}=8000$，一个域离开约 8000 步）：

| forget/步 | 0-shot | 离开一轮后切片存活率 |
|---|---|---|
| 0 | 73.3% | 1.000 |
| $10^{-5}$ | 73.7% | 0.923 |
| $10^{-4}$ | 71.9% | 0.449 |
| $10^{-3}$ | 15.4% | 0.0003 |

正确量级下它既不伤也不帮（+0.4pp，噪声）。**这不是机制无效，是基准从未给它一个能起作用的场合。**
要检验开题的第三个要素，需要**带退役的信源**：跑到第 $k$ 轮后部分域不再出现，容量压力才是真的。

## 14. 又一次静默空转

`ebbinghaus_probe` 对 edge 臂的训练调用落到了 `projs` 为空的默认分支，循环零次、
不报错，产出一条完全合理的平坦曲线（11.322 bits 恰为 $\log_2 2560$，均匀分布）。
topo 臂当时加了 `unreachable!()` 守着，edge 臂忘了加，同一个坑在旁边又踩一次。
两个臂现已用 `assert!` 挡住，且断言消息直接写明"这样会静默地什么都不训练"。

## 15. 限制（三条，都要主动写进论文）

### 15.1 在最优配置下，"拓扑"是装饰性的

容量 $=$ 边数 $\times$ 类数。跳数决定一个事实**碰几条边**，不决定有几条边——
而且实测跳数越多越差（1 跳 94.2% > 3 跳 79.0%），因为多跳让更多事实共享同一条边。
**跳数是干扰旋钮，不是容量旋钮**，$H{=}1$ 不是妥协而是最优。

但 $H{=}1$ 时，入口按内容在 16 个节点里选、再选一条出边——
**本质是一个两级内容哈希（64 路）**。环与捷径只在多跳可达性里起作用，而多跳已被证明有害。
所以：**分配是真的（512 个切片），图不是。**开题写的是拓扑意义上的分布式，
这一条应当主动声明为限制。

### 15.2 类可以生长，边基本不能

约束是**已存知识的地址不能变**，两个轴对此完全不对称：

- **加边**：改变已有事实的路由 → 违反一致性 → 写进去的取不回来。堵死。
- **加类**：类是对 $C$ 个固定原型取 $\arg\max$。新增原型**只会抢走离它比离任何旧原型更近的上下文**，
  因此只要放在**从未被占用的区域**，已有分配逐位不变。

见 §16 的生长规则。

### 15.3 节省效应尚未与"单纯收敛"分离

遗忘量逐轮下降也可能只是收敛：预测变准 → 残差 $(p-e_y)$ 变小 → 每步写入变小 → 干扰自然变小。
反驳它的是共享读出下遗忘**逐轮上升**（§10），纯收敛解释不了这个分岔——
但尚未直接隔离。检查方式见 §17。

## 16. 类生长：vigilance 放在它能成立的位置

**规则**：新上下文与所有现有原型的最大相似度低于其历史分位数下界时，
在该上下文方向新建一个原型（若尚有未用类位）。

$$\text{novelty} = 1 - \max_c \langle \hat{\mathrm{ctx}},\ \mathrm{proto}_c\rangle$$

**为什么这次 vigilance 不会失控。** 复核者早先否掉过 ART 式 vigilance，理由是
"分配决定路由、路由又决定下次分配"的反馈环会塌缩或碎裂——那是把它放进了**每粒子每跳**的路由。
这里它是**一次全局的慢决定**：类由一个 $\tau=\mathrm{span}/5$ 的 EMA 驱动，
每次域访问才可能触发一次。同一个机制，位置不同，动力学完全不同。

**为什么它保持一致性。** 新原型只夺取"离它最近"的上下文；放在无人占用的方向上时，
所有已有上下文的 $\arg\max$ 不变，已写入知识的地址不动。
**"无人占用"是可测的**，不是拍脑袋的阈值：记录每个方向上历史落点的密度。

**必须同时报的操作检验**：已有类分配的改变率。新建原型后若有旧上下文改换门庭，
那部分知识就地失联——这个数必须是 0，否则生长规则不成立。

## 17. 把节省效应与收敛分离

逐轮记录**写入量的范数** $\sum_t \lVert \Delta W_t\rVert$（读出与边记忆分开记）。

- 若遗忘量的下降比例 $\approx$ 写入量的下降比例 → **是收敛**，节省效应是平凡结果
- 若遗忘量降得更多 → 多出来的部分才是分配带来的

同一指标在共享读出与类索引读出下各测一次：两者的写入量轨迹应当接近
（都在收敛），而遗忘量一升一降。**若真是如此，收敛解释即被排除。**

---

# 结果续（2026-08-22）

## 18. 类生长：成立，且消除了种子方差

$\text{init}{=}1$、$k{=}1$（Mode B，1 跳，四个种子）：

| seed | 固定 8 类 | **生长** | 存活类 | 失联率 |
|---|---|---|---|---|
| 1 | 73.3% | **94.4%** | 8.0 | **0.000** |
| 2 | 96.0% | 92.3% | 8.0 | **0.000** |
| 3 | 67.9% | **89.2%** | 8.0 | **0.000** |
| 4 | 95.2% | 91.7% | 8.0 | **0.000** |

**方差从 28pp 塌到 5pp**（67.9–96.0 → 89.2–94.4），最差的两个种子各抬升约 20pp，
最好的两个基本不动，均值 83.1% → 91.9%。
固定随机原型的种子方差，正是"靠运气把域分开"的代价；生长把运气换成了构造。

**§15.2 因此需要修订**：类不仅"可以"生长，生长还是更好的默认。

### 18.1 两处必须记下的失败与更正

**第一版生长完全失败**（4.0–4.6%，比基线还差），两种方式：
$k{=}3$ 时四个种子有两个从未触发；触发的两个 `growth-steal` $=1.000$。
根因是高维几何：随机原型与数据近乎正交（点积 $\approx 0$），
而放在**数据方向**上的新原型对**所有**数据相似度都更高，于是赢走全部 $\arg\max$。
改为只取**现有原型解释不了的残差方向**（正交化）后问题消失。

> 反过来这解释了固定随机原型为何好用：**它们实质上是上下文的随机哈希**，
> 而随机哈希恰好能把域分开。"按域放置比碰运气更有原则"这个直觉是反的。

**失联检验本身也测错了对象。** 第一版统计最近 64 个上下文，
而它们几乎全部来自刚触发生长的那个域——那些**本就应当**被新原型接管。
按此测得 0.28–0.57，看似违反 §16 的"必须为 0"。
改为**按域各留一个样本、只统计其他域**（即已写过知识、真会失联的那些）后，四个种子全部为 0.000。

## 19. 有效遗忘：判据未过

8 个域，0–3 在第 4 轮后永久停训（$V{=}3584$）：

| | 退役域 | 在役域 |
|---|---|---|
| mono 基线 | 27.5% | 20.0% |
| edge, forget 0 | 40.0% | **48.8%** |
| edge, forget $10^{-5}$ | 39.6% | **48.8%** |
| edge, forget $10^{-4}$ | 59.6% | **47.3%** |

判据是"**在役域准确率随遗忘率上升**"。实测 48.8 → 48.8 → 47.3，**未上升**。
回收退役容量**没有转化为新知识的性能**，开题第三要素在此测试中不成立。

且有反方向异常：**退役域准确率随遗忘增强而上升**（40.0 → 59.6）。
遗忘本应让退役知识掉得更快。最可能是 $10^{-4}$ 的衰减在起权重衰减的作用，
而非"定向回收"——**这本身即是红旗：遗忘规则的行为与其模型不符**，
在解释清楚前不得写为正面结果。

**但这一格里有一个不该归给遗忘规则的扎实结果**：真实容量压力下
edge 全面优于 mono（40.0/48.8 vs 27.5/20.0），且 mono 的在役域（20.0%）
低于它自己的退役域（27.5%）——**单体记忆在容量饱和后学不进新东西**，edge 没有此问题。
这属于塑性–稳定性，不属于有效遗忘。

## 20. 仍未解释

1. **远端 seed 2 = 48.5%**（$\text{span}=20000$, $V{=}1536$, 3 跳）。
   跳数在本地只解释 16pp（96.0 → 80.2），余下 32pp 无解释。
2. **遗忘的反方向行为**（§19）。

## 21. §10 作废：节省效应由容量压力决定，与读出索引无关

补齐 2×2（$\text{span}=4000$，12 轮，逐轮内遗忘量，bits）：

| | $d{=}16$ | $d{=}32$ |
|---|---|---|
| **共享读出** | +3.38 → **+2.89**（先升至 4.0 再降） | +3.73 → +6.39（升） |
| **类索引读出** | +4.94 → **+2.17**（降） | +3.42 → +10.14（升） |

**$d{=}16$ 时两种读出都出现节省；$d{=}32$ 时两种都没有。**
决定节省效应的是 $d$，不是读出索引方式。

§10 的结论建立在"类索引@$d{=}16$ 对比共享@$d{=}32$"——**同时动了两个变量**。
§10.1 已经因为同一个错误更正过一次准确率的比较，却没有对节省效应做同样的事。**§10 作废。**

**修正后的说法**：节省效应需要**容量压力**。$d{=}16$ 表征紧，模型被迫跨轮压缩复用，
重复暴露转化为固化；$d{=}32$ 有余量逐轮硬记，干扰反而累积。
类索引读出在 $d{=}16$ 下确实降得更低（2.17 vs 2.89），但那是程度差异，不是有无差异。

**收敛解释仍然被排除**：两种条件的写入量轨迹几乎重合
（共享 2864→1249，类索引 3278→1259），所以差异不来自"预测变准、写入变小"。

## 22. 第二个反复出现的错误模式：同时动两个变量

除了 §12 的时标错配，本项目还有一类同样稳定的错误——**比较时同时改变两个量**：

| 出错处 | 混淆的两个变量 | 后果 |
|---|---|---|
| 张量的规模测试 | 探针协议 + 数据规模 | "差距随规模扩大"无法归因 |
| 旋转方案的种子格 | 种子 + 轮数 | 号称"只换种子"，实则不是 |
| 类索引读出的准确率 | 索引方式 + $d$ | 制造出一个不存在的权衡（§10.1） |
| 类索引读出的节省效应 | 索引方式 + $d$ | 得出错误的机理（§10，本节作废） |

**四次。**而且第三次已经补过 2×2 并写下更正，第四次仍然重犯——
说明"知道该补对照"并不足以避免它，必须在**跑之前**逐项核对两个配置的差异，
而不是在结果好看之后才去检查。

---

# 主结果（2026-08-23）

## 23. 参数匹配、三种子、未饱和

Mode B，$\text{span}=20000$，$V{=}4096$，**1600 个事实对 512 个切片 = 3.1 事实/切片**：

| seed | mono | **生长版 edge** | 优势 |
|---|---|---|---|
| 1 | 22.0% | **96.0%** | +74.0pp |
| 2 | 17.2% | **96.9%** | +79.7pp |
| 3 | 21.3% | **95.8%** | +74.5pp |

参数：edge 624k，mono 786k——**基线更多**。
edge 三种子跨度仅 1.1pp；mono 崩至 17–22%。

**必须选在这个负载上报告。** 在 1.02 事实/切片处，四个种子全部 100.0%（mono 52–65%），
但那是天花板不是测量：切片数与事实数几乎一一对应时，问题被完全解决，优势无法量化。
两个点一起给才诚实——它们构成一条随负载退化的曲线，而 mono 退化得快得多
（52–65% → 17–22%，edge 100% → 96%）。

### 23.1 增益的来源分解

| 配置 | Mode B |
|---|---|
| 一体化基线（同预算） | ~20% |
| edge + 固定原型（单种子） | 72.3% |
| **edge + 类生长** | **~96%（三种子）** |

固定原型 → 生长值 24pp，且把种子跨度从 28pp 压到 1–5pp。

### 23.2 已排除的替代解释

| 解释 | 排除依据 |
|---|---|
| 更深的非线性 + 反传 | 1 跳（94.2%）优于 3 跳（79.0%），深度反而有害 |
| 查找表（切片数≈事实数） | 负载升至 3.1 事实/切片，edge 仅掉 1pp，mono 崩塌 |
| 类只是额外地址位 | 类换成 $\mathrm{hash}(e,r)$：Mode B 73.3% → 4.4%；Mode A 不受影响 |
| 种子运气 | 三种子跨度 1.1pp |
| 原型抽签 | 生长后 vocab 摆动由 50pp 降至 ~0 |

## 24. 原型抽签：三个谜团的共同根因

类原型在词嵌入**之后**从同一随机流抽取，因此**改动 `vocab` 会静默重摇原型**。
这一个实现细节同时制造了三个此前被分别误判的现象：

| 现象 | 曾被归因于 | 真因 |
|---|---|---|
| 种子跨度 28pp | 种子运气（方向对） | 原型抽签 |
| 远端 seed 2 = 48.5% | 先归因种子、再归因跳数，**两次皆错** | `vocab` 重摇原型 |
| seed 3 类碰撞 0.50 | 两域落入同一原型格 | 同上 |

生长版实测（seed 2，3 跳）：

| | 固定原型 | 生长 |
|---|---|---|
| $V{=}1536$, span 8000 | 36.9% | **84.6%** |
| $V{=}1536$, span 20000 | 48.5% | **99.2%** |
| $V{=}2560$, span 8000 | 87.5% | 87.1% |
| $V{=}2560$, span 20000 | 97.3% | 99.0% |

**生长只抬升输的格子、不动赢的格子**——消除抽签应有的形状。

**待修**：原型应从独立随机流抽取，使 `vocab` 与原型解耦。
暂缓，否则历史数字失去可比性。

## 25. 节省效应：机制无效（探针修正后）

`ebbinghaus_probe` 此前只恢复输入阶梯、**未恢复类状态**，因此在最大延迟处
是用**最后训练的那个域**的切片去查域 0 的事实——测的是"用错误上下文查询"，不是保持。
这与 `DESIGN-SDR.md` 附录 C.1 是同一个 bug，只是发生在后加的类轴上：**修了一个，未回头检查另一个。**

修正后（三种子 × $d\in\{12,16\}$，负值消失）：

| $d$ | 类索引优势 | 判决 |
|---|---|---|
| 12 | 均值 −0.01，标准差 0.09，符号混杂 | 无效应 |
| 16 | 均值 +0.33，标准差 0.46，符号混杂 | 无效应 |

早先那条"+4.94 → +2.17"是**混淆的 $d$ + 坏探针**两层伪影叠加。
节省现象本身存在（各条件斜率均为负），但**不归功于本机制**。

## 26. 有效遗忘：不成立，两次修复后仍然如此

8 域，0–3 在第 4 轮后永久停训。判据是"**在役域准确率随遗忘率上升**"——
回收退役容量应当转化为仍在使用的知识的性能。

| 版本 | forget=0（退役/在役） | forget=$10^{-4}$（退役/在役） |
|---|---|---|
| 衰减作用于边变换 | 40.0% / **48.8%** | 59.6% / **47.3%** |
| 衰减作用于读出（部件修正） | 42.3% / **47.1%** | 3.3% / **8.8%** |
| 用量时标覆盖一个轮转（时标修正） | 42.3% / **47.1%** | 6.9% / **12.5%** |

**三个版本，在役域从未上升。**

两次修复各自都是真的：

1. **部件错了**。衰减原先作用在 `edge_mem`（payload 变换）上，而知识在类索引读出里。
   $E\to0$ 只是让变换退化为恒等，payload 原样通过、读出照常映射——**没有遗忘任何东西**，
   反而把无流量期间漂坏的变换拉回恒等，于是退役域"越忘越好"（40.0 → 59.6）。
   修正后退役域随遗忘率单调下降，反常消失。
2. **时标错了**。`usage` 的 EMA 时间常数取自**切片数**（$1/512$）而非**轮转周期**，快了 40 倍。
   一次域访问是数千次观测，因此任何不在当前访问中的域用量都会跌破份额——
   **规则区分不了「已退役」与「正处在两次访问之间」**，在役域有 7/8 的时间被当作退役的衰减。
   改为由周期导出后，破坏减轻（8.8% → 12.5%）但方向未变。

**结论**：在这个基准上，定向遗忘无法带来正面收益。开题第三要素**未能证实**。
这不是"机制无效"的强断言——两次修复说明此前的测量确实无效——
而是：**在修掉所有已知缺陷之后，仍然测不到收益，且不再有第四个待查的假设。**

### 26.1 代价记录

"遗忘"这一个问题消耗约 8 小时、5 次尝试，其中仅 2 次产出科学内容
（错部件、错时标），另外 3 次分别是：初始配置过大（4 率 × 3 eta，其中只有 Plain 行有用）、
`pkill` 自伤导致脚本从未运行、本机内存压力被迫迁移。
另有固定浪费：每个 run 的最后一轮都要计算 few-shot 恢复谱
（每域 5 个 budget × (≤200 步训练 + 130 事实探针) + 每次 clone 整个 bank），
**而本问题只读 0-shot 的两个数**。

## 27. 读出层阶梯：被证伪的机制第一次拿到它需要的前提

阶梯在 §1 被证伪，这一节不是给它翻案。翻案和重试的区别在于**前提变了没有**。

当初的死因是：深档把**互相冲突的域**平均掉。同一个键在域 0 要求答案 A、在域 1 要求答案 B，慢档存下的是 A 和 B 的混合——一个不属于任何域的值。深档越慢，混得越匀，越没用。这个诊断是对的，`--edge-rungs` 挂在共享读出上会原样复现它，所以代码里禁止那个组合。

分配机制改变了这个前提。一个读出切片是 `R[class]`，而一个类就是一个域制度（§16、§18：stranding 0.000，说明切片确实是按域分开的）。**切片内没有冲突可平均**——深档平均的是同一个域对同一个键的一致取值，也就是去噪，而不是调和矛盾。这是阶梯一直声称自己需要、却从未得到过的条件。

### 27.1 为什么挂在读出层，而不是边记忆上

边切片按 `(edge, class)` 索引，且扩散是**活动门控**的：只有刚被写入的那个类切片推进一步。于是域 0 缺席期间，它的切片既不被写、也不推进时间——**不侵蚀，也不漂移**。边记忆上没有可供阶梯挽救的损失。

真正的侵蚀在读出层：每一步对全词表稠密写，域 1 的写必然压过域 0 的行。这也是实测过的（§21：共享读出下遗忘逐轮上升）。所以阶梯该挂的地方，是**既有侵蚀、又已按域隔离**的那一层——类索引读出，两个条件只有它同时满足。

### 27.2 时间常数不接受字面量

`tau_k = r^(2k)/g1`，单位是**该切片的激活次数**，不是流步数。两者差一个占空比，而把它们弄混是本项目犯得最多的错误（§12 已记六次，第六次发生在写下禁令之后）。

因此 CLI 不暴露 `g1`。它暴露 `--edge-ladder-visits`：第一慢档应当平均掉几个「域访问」。`g1` 由它和访问长度在 `run_arm_trial` 里算出来：

```
per_visit = span_tokens / 3          // 一次访问里落到该类切片的激活次数
g1 = r^2 / (visits * per_visit)
```

单位错误因此不再是记性问题，而是需要显式改代码才能犯。这条对应 §12 结尾提出、一直没做的那个措施。

### 27.3 判据（先注册，后跑）

首测已确认它**不是空操作**：rungs 1→2→4 时遗忘 +3.53 → +1.21 → +0.97 bits。但同一张表里 delay-0 准确率 0.74 → 0.57 → 0.36 一起掉了，所以这张表什么也没证明——学得少，可遗忘的就少，这正是 §22 记过两次的混淆。

而且有一个更省事的解释摆在桌上：**`eta` 本来就在换塑性与稳定性**。如果阶梯只是复现了小学习率的效果，它就是学习率的重参数化，判决与「H=1 时拓扑是装饰」（§23）相同。

所以唯一算数的比较是**在同等塑性下**比遗忘：

- 用 `eta ∈ {0.125 … 4.0}`（rungs=1）扫出基线的塑性—稳定性权衡曲线；
- 对每个 rungs>1 的格点，取塑性最接近的基线格点，比遗忘；
- **gain > 0**（阶梯在同等塑性下遗忘更少）才算成立；gain ≈ 0 判为重参数化。

三种子。遗忘同时按写入幅度归一（§25：原始遗忘的下降由收敛解释，除以写入幅度后节省效应消失）——这一条对阶梯同样适用，不因为它是新机制就豁免。

## 28. 作废：§28.2 的「遗忘是入侵」诊断建立在两处被污染的测量上

保留原文以便追溯，但 **§28.2 与 §28.3 的结论作废**，理由见 §29。简述：当时测得入侵仅占 0.1%，据此断定「遗忘是入侵」并推出一整套解释；实际那个 0.1% 是在类机制完全空转的构型里量的——四个域 100% 写进同一个类。修正后入侵占 32.4% 的写入、44.1% 的写入幅度。诊断的方向是对的，数字和据此排除的方案全错。

## 28（原文）. 阶梯做到了什么，以及节省效应为何一直不出现

### 28.1 先纠正目标

「阶梯的目标是节省效应」这个前提是错的。Benna-Fusi 要解决的是有界突触下的容量/寿命权衡，也就是塑性—稳定性前沿；节省效应是艾宾浩斯的行为学现象，从来不是那套机制的判据。

而塑性—稳定性前沿正是开题报告标题里的东西。按这个判据，§27 的阶梯**做到了它该做的**：在高塑性端把前沿往外推（同等塑性下少遗忘 +0.66 bits），且最优时标落在推导值（一个域访问）上而非调出来的值上。判据没有失败，是被换成了另一个判据。

### 28.2 这个架构的遗忘是入侵，不是衰减

代码可以定死：`observe_fact` 只写 `readout` 和 `edge_mem`，`emb` 从不被学习。在 `--edge-class-readout` 下两者都按类切片，探针又恢复上下文。于是域 0 的存储在别的域训练期间**只有一条改变途径：某次写入被判错了类**。

两条独立证据支持：

- 遗忘随 eta 从 1.06 涨到 7.66 bits。衰减账户不会有这种依赖（衰减率与写入幅度无关），入侵账户必然有——eta 越大，一次错判写得越狠。
- 阶梯的增益也集中在高 eta（+1.14，低 eta 处约为 0）。阶梯在这里的实际作用是**把偶发的错类写入低通滤掉**，所以只有在入侵够大时才有东西可滤。

**节省效应是衰减现象**：痕迹淡去、残余尚存，故重学便宜。没有衰减就没有淡去的痕迹。这解释了 §19/§25/§26 三次失败的共同根因——不是机制不够好，是这个架构里根本没有「淡去」这件事。之前加 `--edge-forget` 时，衰减无差别地打在知识本身上，那是擦除不是淡去。

### 28.3 第一个能产生节省效应的构型

`--edge-forget` 衰减的是 `readout`（表面档）和 `edge_mem`，**不碰 `readout_rungs`**。所以 `--edge-forget X --edge-rungs 2` 是本项目第一次出现「行为淡去、潜在痕迹保留」的构型——正是节省效应所要求的前提。此前没有跑过，因为在 §27 之前根本没有储备档可退。

判据：同等**重学后准确率**下，有储备档一侧是否用更少的写入。比值 < 1 才是节省效应。

| 衰减 | 写入比（阶梯 / 无阶梯，同等准确率） |
|---|---|
| 关闭 | 2.66 |
| 1e-4 | 1.19 |

三种子，各 eta 一致。表面允许淡去时，阶梯的写入代价从 2.66 倍塌到 1.19 倍——**储备档确实被取用了**，这是全程第一个方向正确的信号。但仍未跨过 1.0，所以节省效应**依然不成立**，现在是第三种构型下的不成立。衰减率的剂量反应正在跑，判断它是否会跨过 1.0。

### 28.4 顺带：定向遗忘不再是三小时一格

原实现每次 observe 都把每个切片的每个权重乘一遍，O(切片 × vocab × d)，这是「一格三小时」的来源，也是每次遗忘实验都被迫缩水的原因。现改为惰性衰减：存储值是真值除以一个每切片标量，切片被触碰时才折算回去。衰减是标量乘法，与折算可交换，所以这是**精确的**，不是近似。

与原实现逐位对比：47 行探针表与两条遗忘曲线**完全相同**，小构型上快 8.2 倍；真实构型一格从约一小时降到 2 分 06 秒。鉴于「有效遗忘」在标题里，这一项解除的是整条线的限速。


## 29. 两处「为 0-shot 测量而分离」漏进了架构

这是本项目反复出现的那一类错误里最贵的一次：两条训练路径都被各自的探针污染，方向相反，而且互相掩盖。

### 29.1 `run_arm_trial`：探针把类**发**给了系统

每个域访问开始时 `bank.topo_restore(domain_nodes[d])`，快照按**真实域下标 `d`** 取。而 `EdgeMemory::snapshot` 存的是 `ctx` 加 **`class_now` 本身**——也就是说域的类编号被直接发回给它，之后没有任何撤销就流进 `observe_fact`。

访问开始时的分化因此不是推断出来的，是**指派**的。代价被完全抹掉：入侵 0.1%，且只出现在切换后 100 步内。

### 29.2 `ebbinghaus_probe`：探针把训练**拖**向域 0

每个检查点 `bank.topo_restore(nodes0)` 把上下文设成域 0 的，同样没有还原。一次访问四个检查点，于是上下文被反复拽回域 0，**所有域都写进域 0 的类**。

这条路径上类机制完全空转（4 个域 100% 同一个类），而本次会话的全部测量都走这条路径。

### 29.3 修正与真实数字

两处都改成：restore 前存自然状态，探针跑完把自然状态放回，再训练。探针只观察，不驾驶。

修正后两条路径一致，且分化是**挣来的**：

| | 修正前 | 修正后 |
|---|---|---|
| 独立 home 类（4 域） | 1（探针路径） | **4** |
| 共享类的写入占比 | 99.9% | **0.0%** |
| 入侵（按次数） | 0.1% | **32.4%** |
| 入侵（按写入幅度） | 0.3% | **44.1%** |

幅度占比高于次数占比，直接证实了**反常放大**：错投的写入撞上不认识该事实的切片，误差近最大，delta 规则的步长也近最大——最错的写入下手最重。

入侵按「距域切换的观测数」分布：

| <100 | 100–499 | 500–1999 | 2000+ |
|---|---|---|---|
| 98.9% | 96.9% | 25.1% | **0.0%** |

纯粹的**过渡现象**，到 2000 步干净归零，不在访问中段弥散。这个形状是可被寻址置信度门控针对的——现在是测出来的，不是猜的。

### 29.4 作废清单

走探针路径、因而全部作废：本次会话的 Pareto 前沿、双时标 +0.178、节省效应 2.66→1.19、剂量反应、远端 126 格种子数据，以及更早的 §21/§25 艾宾浩斯衰减与节省效应结论。

走 `run_arm_trial`、不受空转影响但受 §29.1 指派影响：headline、五个替代解释对照、生长统计、拓扑实验、退役式遗忘实验（§19/§26）。这些需要在修正后的代码上重测，但**负面结论反而加强**——它们是在有外援的条件下取得的失败。

### 29.5 教训（这次是可执行的，不是口号）

探针与训练共用 `bank`，而 `topo_restore` 没有配对的还原点。**任何 `topo_restore` 都必须有一个在同一作用域内的自然状态还原**，否则测量就会驾驶实验。§12 那条时标规则最后是靠改成 `--edge-ladder-visits`（不可直接设 g1）才真正生效的；这一条同样需要结构性保证，而不是记性。

## 30. Headline 撤回，以及扩容不重训

### 30.1 96% 是 oracle 辅助的

§29.1 的泄漏修好后，在**完全相同**的 headline 构型（4 域 × 400 事实、span 20000、vocab 4096）重测：

| seed | edge（分配） | mono 最佳臂 |
|---|---|---|
| 1 | 17.6% | 16.4% |
| 2 | 21.0% | 13.7% |
| 3 | 13.6% | — |

原报告的 **96.0 / 96.9 / 95.8%** 撤回。差距从 +74pp 塌到几个百分点，与种子噪声同量级。**在固定构型下比准确率，分配相对单体的优势不成立。**

连带受影响：§23 的参数匹配论证、§18 的生长收益、五个替代解释对照、拓扑实验（§26 之前全部走 `run_arm_trial`）——**它们全部在指派类的条件下取得，必须重测**。负面结论不受损反而收紧（有外援仍失败）；正面结论一律降级为待验证。

### 30.2 尺子换了：信息利用率

在固定构型下比准确率，等于**为分配没用上的容量向它收费**。换成每参数利用率，同一批数据读法不同：

| | 准确率 | 参数量 | %/M参数 |
|---|---|---|---|
| edge s1 | 17.6% | 1.57M | 11.19 |
| mono s1 | 16.4% | 2.10M | 7.82 |
| edge s2 | 21.0% | 1.57M | 13.35 |
| mono s2 | 13.7% | 2.10M | 6.53 |

高 1.4–2.0 倍。n=2，压测会给出可信版本。

这也重新定位了整个论点。有限容量面对无界信息，必须丢掉一些东西，区别只在丢什么：

| | 放弃 | 表现 |
|---|---|---|
| EWC | 塑性 | 固化，新任务学不动 |
| 纯有界 | 旧信息 | 选最新 |
| 分配 | **单域保真度**（每域 1/N） | 均匀降级 |

「分配」的代价此前从未明说：它把「保全一部分、牺牲一部分」换成**所有人一起均匀变差**。要让这个非传统选择站得住，需要证明的不是「不遗忘」，而是**信息利用率更高 + 只在需要时扩容**。

### 30.3 扩容不重训（已成立）

类切片是追加结构，所以扩容对既有知识是 O(0)：

- `edge_mem` 原为**边优先** `(eid * classes + c)`，类数一变每个既有偏移都要挪。改为**类优先** `(c * n_edges + eid)`，与本就类优先的 `readout` 一致，新类成为纯追加。
- `expand_classes` 在运行时追加切片。单元测试断言既有权重**逐字节不变**、且 `forward` 的 logits **逐位相同**——不是「接近」。
- `--edge-expand` 把它接到 `maybe_grow`：槽位用尽且未达上限时扩容一格。从 4 类起步扩到 16，与一开始就给 16 **到达相同分配**（12 域 → 8 个 home 类）。容量因此是数据流的结果，不是要提前猜对的超参。

**mono 结构上做不到**：其容量是 SDR 宽度 `d_sdr`，加宽会改变每个编码所依赖的投影，扩容即重训。这是结构性不对称，不是调参结果。

### 30.4 定向遗忘的惰性实现

原实现每次 observe 把每个切片每个权重乘一遍，O(切片 × vocab × d)，一格约一小时。改为惰性：存储值 = 真值 / 每切片标量，切片被触碰时折算回去。标量乘法与折算可交换，故**精确**。与原实现逐位对比：47 行探针表与两条遗忘曲线完全相同；真实构型一格降至 2 分 06 秒。

### 30.5 遗忘实验此前全部缺少争用

所有遗忘实验都给了分配器**多于域数**的槽位（4 域 / 8 类，8 域 / 16 类），于是每个域各据一格，退役一个释放的切片**没有任何人想要**——没有争用可缓解，有效遗忘无从「有效」。这一次性解释了 §19/§25/§26 的失败。

而且衰减与生长从未连通：`--edge-forget` 会衰减切片，`maybe_grow` 只分配新类、从不回收，所以衰减释放的东西没有机制能认领。

推论（尚未实验证实）：**容量复用本身就是有效遗忘**，「删除最不用的」与「遗忘那一部分」是同一件事的两个说法。因此标题里的「有效遗忘」不需要额外机制去凑，它是按需扩容的另一面。

## 31. Mode A：分配真正成立的地方

### 31.1 结果

修复后的代码，4 域、400 事实/域、span 20000、6 轮、vocab 4096，三种子：

| seed | edge（分配） | mono（单体） | 差 |
|---|---|---|---|
| 1 | 59.9% | 7.5% | +52.4 |
| 2 | 55.8% | 9.6% | +46.2 |
| 3 | 53.1% | 9.8% | +43.3 |
| **均值** | **56.27%** | **8.97%** | **+47.30pp** |

edge 用 1.57M 参数，mono 用 2.10M——**少 25% 参数，高 47 个百分点**，三种子同向。

这与 §30.1 撤回的那个 96% 性质不同：那个是 oracle 指派类的产物；这个是在泄漏修复、`Displaced` 守卫装上之后测的。

### 31.2 为什么是 Mode A 而不是 Mode B

Mode B 对分配是**构造性敌对**的：实体与关系跨域逐字节相同，只有目标不同。基于上下文的路由几乎无信号可依，于是类归属靠的是上下文 EMA 的微弱差异——这也正是 §29.3 那 44% 入侵的来源。

Mode A 里各域有自己的实体，路由有真实依据。分配机制要求「制度可被寻址」，Mode A 满足，Mode B 不满足。

**因此本项目此前的主战场选错了。** 我们用整个项目的时间在一个对分配最不利的信源上调分配机制，并把失败读成机制不行。Mode B 仍是有价值的**压力上界**，但不该是主张的落脚点。

### 31.3 按需扩容：成立，但时间常数必须推导

§30.5 报告生长跑到天花板（48 分配 / 约 9 使用），并据此说新颖度判据是双稳的、必须换成效用驱动。**那个结论下早了**——grow_k 扫描是在**没有去抖**的条件下做的。

去抖按**过渡长度**表达（不是裸计数；裸计数正是 §12 记了六次、并在引入该旗标的那次提交里第七次犯下的单位错误）：

| hold（过渡数） | 观测数 | 分配 | 使用 | 效率 |
|---|---|---|---|---|
| 0 | 0 | 48.0 | 8.3 | 17% |
| 1 | 500 | 25.9 | 8.7 | 34% |
| 2 | 1000 | **12.3** | 7.3 | **59%** |
| 4 | 2000 | 4.3 | 3.7 | 过度压制 |

hold=2 时 12 个域分配 12.3 个切片、7.3 个主导。**按需扩容成立**，不需要换判据。诊断也得到确认：过度分配的主体是**域切换被误判为新制度**，而切换恰是入侵的来源——同一个过渡现象造成两处损害。

### 31.4 「越学越省」撤回

§30 之后曾报告后到的域学得更好（早期 0.104 → 后期 0.249，比值 2.39），并称其为节省效应的正确形式。**不成立。**

干净对照（生长与数据放置原型全程不变，只改初始类数）：

| 初始类数 | 早期四域 | 后期四域 | 比值 |
|---|---|---|---|
| 1 | 0.036 | 0.073 | 2.02 |
| 6 | 0.204 | 0.299 | 1.46 |
| 12 | 0.055 | 0.040 | 0.73 |

初始容量越大效应越小直至反转，说明这是**早期域在容量尚小时被挤伤**，不是共享结构被摊销。这也与架构一致：读出按类切片、边记忆按 (边,类) 切片、嵌入固定随机、路由键固定——跨域几乎没有可摊销的学习量。

（先前的预分配对照是混淆的：`--edge-init-classes 24` 同时关掉生长**并且**把原型从数据放置换成随机初始化。本节的对照全程保持生长与数据放置。）

### 31.5 C 组重验

全部在泄漏条件下取得，此处在修复后代码上重跑（4 域 headline 构型，三种子；未消融的 edge 基线 17.4%）：

| 消融 | 修复后 | 泄漏时期 | 方向 |
|---|---|---|---|
| hash-class | 0.77% | 4.4% | 不变（摧毁 Mode B） |
| 3 跳 | 9.40% | 79.0% | 不变（有害） |
| 64 节点 | 6.23% | 12.7% | 不变（有害） |
| 关闭生长 | 16.00% | 生长值 24pp | **幅度崩塌**：生长仅值 1.4pp |
| Mode A | 见 §31.1 | linear 胜 SDR | **符号反转** |

三项方向不变、绝对值全线下调，与「泄漏虚高一切正面数字」一致。两项结论改变：生长的价值几乎消失（泄漏时期生长的原型恰好对上被指派的类）；Mode A 的 regime split **符号反转**，需整段重写——那是唯一变号的一项。

## 32. 重设计：共享编码器 + 在线推断的私有读出头

### 32.1 根因

边记忆是 `E[edge, class] ∈ R^{d×d}`，每个 (边,类) 一份完整私有矩阵。新增一个类，就在每条边上追加一块**全零**的 d×d——新域从既有的边继承到的是**零**，不是「少」，是字面的零。

这一条同时解释了三个此前各自记录的失败：

- **§31.4 越学越省不存在**：没有可摊销的基底。当时写的「跨域几乎没有可摊销的学习量」是根因，不是旁注。
- **§19/§26/§30.5 有效遗忘四次未证实**：切片之间零耦合，腾出一块对别人没有价值。
- **§30.1 / B2 Mode B 无优势**：Mode B 的生成结构是**实体与关系跨域逐字节相同、只有目标不同**。我们却把共享的那部分也切成了私有——用一个否认共享的结构去学一个以共享为本质的信源。

「加大网格」不是出路：§31.5 测到 64 节点 6.23% 对 16 节点 17.4%，多跳同样有害。边多了流量被稀释。问题不是边不够，是**边上的记忆不共享**。

### 32.2 按信源结构切，而不是按直觉切

同一事实的 payload 由实体与关系构成，这两者跨域相同；不同的只有目标。于是分工是不对称的：

| 组件 | 变换的对象 | 应当 |
|---|---|---|
| 边记忆 | payload（共享内容） | **共享** |
| 读出 | 映射到目标（域特异） | **私有** |

当前实现把两者都私有化，于是既丢了共享，又没在私有上换到好处——目标本来就由读出分开。

### 32.3 一个旋钮连接两个端点

$$E[e, c] \;=\; \sum_{m=1}^{M} \alpha[c, m]\, B_m[e] \;+\; \underbrace{U[e,c]V[e,c]^\top}_{\text{秩 } r \text{ 私有残差}}$$

- $M=1, r=0$：完全共享的编码器 + 私有读出头。这是持续学习中一个已知的强架构。
- $r=d$：退回当前设计。

**当前设计是这条曲线的一个端点，而我们已经测到那个端点很差。** 字典（$M>1$）比单一 $S$ 强在转移是**有选择**的：单一基底让新域继承「所有旧域的平均」，那个平均对它可能是错的；字典让新域挑相关的分量。

差异化的主张因此收窄为一句可辩护的话：**共享表示 + 在线推断的私有读出头 + 按需扩容**——没有任务标签，头是推断的，容量是长出来的。

### 32.4 后验路由合并实现，理由是它变便宜了

§29.3 测得入侵占写入幅度 43–44%，全在过渡期；§31 之后确认减小可疑写入无效（门控 −0.076、clip −0.775）。所以问题是**写到了错的地方**，而非「不确定时写太狠」。

修法是写入用**后验**：目标揭晓后，按各头对该目标的似然选切片。预测仍用先验（不能碰标签）。

这在当前架构里很贵——边记忆按类索引，每个候选类都要重跑走边。**编码器一旦共享，payload 与类无关**，评估所有头只是同一 payload 上的 $C$ 次矩阵乘，后验路由近乎免费。两项改动因此是协同的，合并实现；后验路由挂旗标，归因只需一次两格对照。

### 32.5 代价：A2 降级为更窄但更诚实的性质

共享基底会被后续域更新，所以「扩容后既有域逐位不变」（§30.3）不再全域成立。

它不是消失而是变窄：**私有读出头仍逐位不变，新增域不触碰任何既有域的头**；漂移只发生在共享编码器，且可直接测（新域训练前后既有域准确率的变化）。$M$ / $r$ 同时决定 转移强度 ↔ 干扰 ↔ 扩容非破坏性——一个旋钮定三方，这是它不同于「又加一个超参」的地方。

### 32.6 预注册判据

1. **$M$（或 $r$）的剂量反应**在 Mode B 上应呈倒 U。单调则该分解不成立。
2. **越学越省第一次具备物理基础**：第 $k$ 个域的成本随 $k$ 下降。现状 0.73–2.02 且已证明是早期挤伤。
3. **Mode B 上 edge 追平或超过 mono**（现状 15.10 对 20.86）。做不到则这条路到此为止。

实现顺序：共享编码器（$M=1$）→ 后验路由旗标 → 字典 $M>1$。第一步即可判断诊断是否成立，代价最小。

## 33. 共享编码器成立，共享读出被证伪

### 33.1 结果（Mode B，12 域，6 种子）

| arm | 均值 | 配对差 |
|---|---|---|
| shared（共享编码器 + 私有读出） | **29.42%** | — |
| private（当前架构） | 12.98% | **+16.43pp**，6/6，t=8.44，**p<0.001** |
| mono（单体） | 21.28% | +8.13pp，5/6，sd 10.59，t=1.88，**p=0.119** |

**对当前架构的改进成立**（§32.1 的诊断得到确认：新类在每条边上继承全零，是 Mode B 失败的主因之一）。

**对单体基线的超越未成立**：seed 5 反向（11.0 对 21.5），六种子下 p=0.119。三种子时曾为 +12.50pp 全正并据此宣称判据 3 达成，**该说法收回**。这是本会话第五次「小样本漂亮、满样本回落」。

### 33.2 共享读出：被证伪，且原因是 §1 的老教训

| corner | 均值（n=3） |
|---|---|
| neither | 12.98 |
| enc | 29.42 |
| read | 10.07 |
| both | 5.97 |

- 读出基底叠加于共享编码器：**−27.50pp，0/3**
- 读出基底单独使用：−5.20pp，0/3

共享读出块在**每次**写入时更新，因此累积所有域的目标映射；而 Mode B 的定义正是同一 (实体,关系) 在不同域指向不同目标。该块于是在平均互相矛盾的取值——**与 §1 杀死阶梯深档的机制完全相同**——并且被加到私有块上，所以不是无益而是主动破坏。

参数量直觉（读出占 96%，故「继承零」的大头在读出）**是对的但不相关**：读出继承的零之所以必须是零，正因为那里是域之间冲突的所在。转移只能发生在不冲突的地方。

### 33.3 净结论：§32.2 的不对称划分被双向确认

| 组件 | 变换对象 | 应当 | 验证方式 |
|---|---|---|---|
| 边记忆 | payload（跨域逐字节相同） | **共享** | +16.43pp, p<0.001 |
| 读出 | 目标映射（跨域冲突） | **私有** | 共享它 −27.50pp |

一侧由改动确认，另一侧由**测试其反面**确认。这比只做前者更强。

### 33.4 尚存的缺陷

- **eta 网格边界**：本批全部三臂均报 `WARN: eta boundary`，故各臂都未跑在最优点。该警告是 harness 的慢性问题（C 组同样全中），三臂同等受影响，但这批数字不应作为上界引用。
- **超越单体尚未证实**，需要更多种子或更强的机制；p=0.119。
- 判据 §32.6 中的另两条（hub/domain-specific 差距收窄、第 k 个域成本下降）**不再可由共享读出验证**，因为该路径已被证伪。若要验摊销，须另找共享载体。

## 34. 后验路由：证伪，且诊断量说明了原因

写入按后验（哪个头对观测目标打分最高）而非先验（上下文 EMA）路由，Mode B、12 域、共享编码器、6 种子：

| arm | 均值 |
|---|---|
| prior（先验路由） | **29.42%** |
| post（后验路由） | **0.05%** |

**−29.37pp，0/6。** 不是无效，是摧毁。

诊断量给出了原因：

```
posterior re-routed 79.8% of writes;   intrusion rate 30.8%
```

重路由比例（79.8%）**远高于**误投比例（30.8%），所以它不在纠正误投，而在重路由几乎所有写入。纯极大似然路由退化：训练初期各头近似为零，argmax 近乎任意；一旦某头对某目标略占上风，该目标的全部写入涌向它而**不分域**——赢家通吃，域分离被抹除。

§32.6 注册判据时只写了两种情形（重路由率 ≈ 入侵率 → 在纠正；≈ 0 → 先验本就正确），**遗漏了第三种：远高于入侵率 → 塌缩**。实际落在第三种。该诊断量是随机制一同加入的，因此这次失败是可诊断的而非神秘的。

**不做修补版。** 加先验做 MAP（似然 × 先验）可防塌缩，是一行改动；但预期收益本就很小——共享编码器之下误投**大部分已无害**（写错类时边记忆那一半是共有的），要抢救的损害已被结构性消除。本会话六个机制中四个失败，继续加旋钮的边际收益为负。

### 34.1 机制探索小结

| 机制 | 结果 |
|---|---|
| 共享编码器 | **+16.43pp，6/6，p<0.001** |
| 生长去抖（按过渡长度） | **分配效率 17% → 59%** |
| margin 门控 | −0.076，劣于纯 eta 曲线 |
| 写入范数 clip | −0.775 |
| 共享读出 | −27.50pp，0/3 |
| 后验路由 | −29.37pp，0/6 |

成立的两个都是**换掉一个结构决策或把时间常数改为推导**；失败的四个都是**在既有结构上加一个旋钮**。这个划分本身可能是本会话最可迁移的经验。

## 35. 节省效应：前提补齐后仍不成立

§32 的共享编码器第一次提供了可摊销的基底——§31.4 的失败是在边记忆完全私有、即**根本无物可摊销**的条件下取得的。此处在有基底的条件下重测（12 域、顺序到达、`--edge-init-classes 6` 以避开 §31.4 那个会伪造信号的早期挤压）。

判据（预先注册）：第 $k$ 个域的单位准确率成本随 $k$ 下降，**且 shared 下降而 private 不下降**——后者用于排除位置效应。

| | s1 | s2 | s3 | 均值 | <1 |
|---|---|---|---|---|---|
| shared | 0.98 | 1.37 | 0.72 | **1.02** | 2/3 |
| private | 1.18 | — | 0.92 | 1.05 | 1/2 |

按域聚合时为 0.94（shared）对 1.03（private），但**拆到种子即散开**（0.72–1.37），均值 1.02 ≈ 1。判定为噪声。

**这一条现在可以干净地结案**：共享基底存在、早期挤压已排除、对照组齐备，摊销仍未出现。与 §28.2 的艾宾浩斯形式（需要衰减，架构中不存在）合起来，**节省效应在本架构下两种形式均不成立**，且不再是「测量条件不允许」。

### 35.1 同一批数据给出的一个正面结论

shared 的准确率为 private 的 **2.2 倍**（早期 0.352 对 0.161，后期 0.368 对 0.163），且**早晚一致**。

因此共享编码器的收益是**均匀的**，不是位置效应。这独立佐证了 §33.1 的 +16.43pp，并排除了「只是后到的域占了便宜」这一替代解释——那正是 §31.4 曾经栽过的那个陷阱。

## 36. Pareto：仪器覆盖不足，尚不可判

修复后的探针路径首次用于前沿测量（类分离正常：12 域 7 个 home 类，非 §29.2 时期的 1 个）。

| arm | eta 0.1 | 0.3 | 1.0 | 3.0 |
|---|---|---|---|---|
| mono 塑性 | 0.033 | 0.040 | 0.082 | 0.198 |
| shared 塑性 | 0.320 | 0.643 | 0.793 | 0.922 |

**两条曲线不重叠**：mono 在网格上限的塑性（0.198）仍低于 shared 在网格下限的塑性（0.320），且 mono 随 eta 单调上升、未饱和——eta 网格对 mono 截得太低。插值器拒绝外推（该保护是刻意加入的，外推正是「免费增益」的产地），故未产出任何配对增益。

**这是覆盖不足，不是结果。** mono 的 eta 10/30 已补齐，结论见 §36.1。

### 36.1 补齐后：不是「超过」，是「够得到 mono 够不到的地方」

补跑后 mono 的 eta=30 必须作为**发散点剔除**：其轮内遗忘为负（首轮 −4.3 / −11.6 / −4.3，后续轮次亦翻负）。负的轮内遗忘意味着域 0 在刚训练完时反而劣于长延迟之后——那是没学进去，不是记得牢。塑性在 eta≥10 后停滞（0.302 → 0.309）随即发散。

| arm | 稳定区间内的塑性 | 对应遗忘 |
|---|---|---|
| mono | 0.033 → **0.302**（eta 0.1→10） | 0.017 → 0.240 |
| shared | **0.320** → 0.922（eta 0.1→3） | 0.321 → 4.014 |

**两条曲线几乎不相接**：mono 稳定区内的最高塑性（0.302）仍低于 shared 的最低点（0.320）。全部三种子中仅有一个重叠点（seed 2），该点上 shared **劣 0.523 bits**。

**结论**：

- **严格判据（同等塑性下少遗忘）未达成**，且在唯一可比的点上为负。
- **可成立的是另一句**：mono 在任何稳定学习率下都无法进入 shared 的工作区间。shared 以低塑性端略差的稳定性，换取了 mono 根本进不去的高塑性区间。

这是前沿在一个维度上的**延伸**，不是**支配**。对标题中「逼近或超过塑性稳定性 Pareto 最优」而言：「超过」不成立；「逼近」勉强，且唯一重叠点上仍差 0.5 bits。**不建议作为正面结论使用。**

## 37. 回溯审计：三个大坑都来自很早定下的基调

本会话最贵的两个发现（§29 探针泄漏、§37.1 目标空间不相交）都不是近期改动引入的，而是项目早期定下、此后一直没被质疑的选择。于是对承载结论最多的旧代码做了一次系统回溯。

### 37.1 Mode B 对分配是**双重**最坏情况

| | Mode A | Mode B |
|---|---|---|
| 输入侧 | 实体按域私有 → 路由有信号 | 实体关系跨域**逐字节相同** → 几乎无信号 |
| 输出侧（目标并集/域数，12 域） | 97.0 → **51.1**（次线性） | 150.0 → 150.0（**严格线性**） |

Mode B 的目标由 `d * shared_entities_count` 切成互不相交的块，**跨域重叠恒为零**。Mode A 的 Tier 0/1 目标都是 hub，天然跨域共享，约 45% 的目标是已见过的。

**因此节省效应一直是在唯一一个摊销构造上不可能的模式上测量的**（§28、§31.4、§35 全部为 Mode B）。这三节的负面结论需要在 Mode A 上重测才算数。

而项目把 Mode B 当主战场这个决定，等于**用一个否认共享、且不提供路由信号的信源去评估一个依赖二者的机制**。

### 37.2 Mode B 的类信号来自标签，不是输入

`absorb_token(target)` 只在训练路径调用（`predict_fact` 不调用），**不是泄漏**——探针不吸收被问的答案。

但其后果需要写进主张：Mode B 中输入 (实体, 关系) 跨域相同，唯一区分域的 token 是目标，所以**类推断依据的是标签流**。

「无需任务标签」应收窄为「**无需任务 ID，但使用到达的标签**」。这仍远弱于 EWC 所需的任务边界，但不是无监督任务推断。Mode A 不受影响：那里实体本身域特异，输入即足够。

### 37.3 `best eta` 按 loss 选，不按准确率

`is_better` 先比 `final_loss`，其次才比准确率。故表中「0-Shot Acc」是**loss 最优 eta 处的准确率**，未必是该臂的最高准确率。各臂一致，比较仍公平；但用 `--eta` 固定单点的运行不受影响，扫描类实验（如 §36 Pareto）受影响。

### 37.4 清白的部分

- `FrozenProbeSlice::evaluate`：每个事实前恢复上下文快照，不改记忆。
- walk 的域纯度：Mode B 纯域内；Mode A 域内 + 共享 hub（设计如此）。
- Zipf 权重确实驱动 walk（`1/(i+1)^s`），层级按实体下标划分与频率一致；曝光诊断（hub 144/376/3048 对 tail 0/24/96）从经验佐证。
- Mode A 的 `prefixes` 为空，仅影响第 1 轮探针，其后由域快照恢复。

### 37.5 本轮自己引入并已修的

- **dense 写入路径每次分配 4096 长的 Vec**（收集 `0..vocab`）。此后所有 dense 运行被拖慢。已改为迭代索引区间。
- **`--target-overlap` 在 Mode A 下静默无效**。已加断言，现在响亮失败。
- **稀疏路径下 `wn_r` 只累加被触碰的行**，故写入幅度**不可跨 dense/sparse 比较**。已在代码注释声明；任何成本分析必须留在同一路径内。

## 38. 阶段总结：长程、容量、以及架构究竟是什么

### 38.1 架构的诚实定位

结构上，本架构等价于**单层、top-1、router 不学习、专家可按需增长的稀疏 MoE**：

| MoE | 本架构 |
|---|---|
| 学习的 router | 固定随机内容哈希（不学习） |
| top-1 稀疏路由 | 是（argmax 选一条边） |
| 专家 = FFN | 专家 = `(边, 类)` 切片 |
| 固定专家数 | **按需生长，追加不破坏既有权重** |
| 多层 + attention | **单层 H=1，无 attention** |

「router 不学习」此前被当作优点（一致性 1.000）陈述，实则是限制：它**排除了负载均衡与学出的专精**。而 attention 被移除且无等价物，长程关联的缺口因此是**结构性**的，不是调参问题。

### 38.2 长程：缺口成立，四次尝试后才测对

前三次测量全部无效，原因各不相同且都是设计缺陷：

1. 目标不依赖线索——没有需要携带的东西
2. edge 分支不读输入阶梯，重建探针 ladder 对它无影响
3. **探针从按域索引的快照恢复 `ctx`**——把要推断的制度身份直接递给模型（§29.1 的模式，在唯一未被修复的角落）

修正后（探针从呈现的 episode 重建 `ctx`），六种子：

| gap | 线索残留 $(1-r)^{\text{gap}}$ | 准确率 |
|---|---|---|
| 1 | 0.990 | 10.10% |
| 16 | 0.851 | 10.33% |
| 64 | 0.526 | 8.40% |
| 256 | 0.076 | 6.83% |

**配对退化 +3.27pp，5/6 同向，p=0.013。** 断崖位置由 `ctx_rate` **事先算出**并落在预测区间——本项目第一个先验预测后被证实的实验。

（三种子时曾读作 6.97→2.80 的腰斩；六种子后是 32% 相对下降。第六次「小样本偏乐观」。）

### 38.3 两次机制尝试，两次都是尺度/配置没校对

- **上下文加进 payload**：注入项模长实测为内容的 **1.20 倍**，内容被淹没。这是无效测试，**不是机制否定**——记成后者会埋掉一个还活着的想法。
- **上下文加进地址**（联合寻址）：预注册「长 gap 变好 **且** 查表变差」，实测反过来（gap=1 +0.90，长 gap ≈0）。按解释规则这意味着**上下文没真正进入地址**，而我这次**又没先测模长比**——同一个疏忽重复了一次。

### 38.4 容量增长：测出线性，但漏了关键旗标

| 域数 | 占用行 | 行/域 |
|---|---|---|
| 2 | 198 | 99 |
| 8 | 1068 | 134 |
| 24 | 3124 | 130 |
| 48 | 6790 | 142 |

**幂律 $\alpha = 1.093$**，线性偏超线性。但该网格**未开 `--edge-share-readout`**——跨类共享目标行的排他归属正是次线性所依赖的机制，缺它则两个域共用同一目标仍各占一行，Mode A 那 45% 的目标重叠完全没被利用。与压测漏开 `--edge-expand` 同类错误，重测中。

**独立的坏消息**：准确率随域数崩塌（8 域 67.9% → 32 域 26.5%），与共享无关。

### 38.5 多跳可能被错杀

此前以「固定容量下的查表准确率」否定多跳（3 跳 9.40% 对 1 跳 17.4%）。**这是错误的轴**：H 跳用**路径**区分事实，E 条边给出 $E^H$ 条可区分路径——**从线性记忆得到组合式寻址容量**，是均摊主张而非准确率主张。准确率下降也可能只是每条边分到的流量变少（欠训练），边流量诊断现已能把两者分开。正确问法是「达到同等准确率需要多少容量」，重测中。

### 38.6 差异化的轴：对着 Transformer 的痛点，而不是对着基线的分数

| Transformer 痛点 | 本架构 | 量 |
|---|---|---|
| 全局通信 | 一 token 只碰 H 条边与一个类切片 | 结构性 |
| 长上下文瓶颈 | 记忆由内容界定，不随 N 增长 | KV cache = 0 |
| 上下文访问代价 | $H d^2 = 1024$，**与 N 无关** | attention 为 $Nd$ |
| lost in the middle | 按内容寻址，无位置偏置 | 结构性 |

上下文访问的交叉点在 **N = d = 32**：超过 32 token，attention 每 token 读取即超过本架构，且差距线性增长（N=4096 时 **128×**）。

**必须同时说明的不利事实**：整体激活比例 6.178%，其中**边记忆仅占 1,024**，其余全部来自读出对全部 $V$ 行打分。所以「稀疏激活」目前只对专家部分成立（1.6%），读出仍是稠密的。若把打分限制在该类实际发出过的行（实测约 538 行），可再降 **15.2×**——这是一个已识别但尚未实现的改进。

## 39. 生长链的三处缺陷，及其污染的六次测量

### 39.1 缺陷

**判据**：新颖度对照**全局**运行均值与方差。制度一多，该分布就是所有制度的混合，于是每个制度相对它都典型，无一新颖。实测 8、32、64、128 域**全部塌进一个 home 类**，而 12 域时还能分出 7–8 个——判据随制度数退化，方向与持续学习的需要相反。

改成 per-class 统计**同样失败**：`init-classes 1` 时只有一个类，per-class 即 global；且一个类吸收越多制度、自身方差越大，阈值 `grow_k·sd` 反而升高，**在最需要分裂时最难分裂**。

最终判据是**类内弥散度** `sd/|mean| > grow_k`：服务单一制度的类相似度紧密，被拉扯到多个制度的类弥散。该量不随类数变化，也不自我挫败。

**放置**：新原型置于**对已有原型正交的残差**。高维中该方向同时几乎垂直于数据，于是原型永不赢得 argmax——实测分配了 28 个类而 16 个制度仍全部路由到一个。该规则当初是为回应 growth-steal = 1.000 而引入，**而那个统计量后来被发现是错的**（采样偏向触发生长、本就该移动的那个域）。统计量修了，为它引入的补丁没人回头改，存活了整个项目。改为**置于当前上下文**（数据处）。

**速率**：每次事件只在当前上下文放一个原型，即只分出一个制度；16 个制度需约 15 次事件。`grow_k=0.05, hold=2` 时 16 域分出 14 个（26 个已分配类）；`grow_k=0.2` 只分出 4 个。

### 39.2 被污染的测量

| 测量 | 状态 |
|---|---|
| §35 / §31.4 节省效应（Mode A） | **作废**：12 域 1 个 home 类 |
| §38.4 大规模容量曲线 | **作废**：8–128 域全部 1 个 home 类 |
| §38.5 多跳（容量轴） | **作废**：8 域 1 个 home 类 |
| 多制度准确率崩塌（67.9%→26.5%） | **作废**：崩塌幅度被判据缺陷放大 |

### 39.3 修复后的重测（唯一有效的一批）

**多跳**（Mode A，8 节点，类分离正常）：

| H | 0-shot | 占用行 |
|---|---|---|
| 1 | **55.15%** | 750 |
| 2 | 46.00% | 737 |
| 3 | 28.50% | 750 |

占用几乎相同，准确率单调降 26.7pp。**多跳的否定成立**，且这次在有效配置下。故「H 跳以 $E^H$ 条路径换容量」在本信源上不成立。

**规模**：

| 域数 | 0-shot | 占用行 | 行/域 | home/域 |
|---|---|---|---|---|
| 8 | 49.53% | 530 | 66 | 0.75 |
| 16 | 43.07% | 1142 | 71 | 0.64 |
| 32 | 37.65% | 2412 | 75 | 0.70 |
| 64 | **34.80%** | 5198 | 81 | 0.78 |

（同格 Linear 基线 15.30%，故 35–50% 非平凡分数。）

### 39.4 多制度退化的成因分析（open question）

8→64 域退化 14.7pp。数据排除了两个常见解释：

- **不是容量饥饿**：行/域 66→81，容量随制度线性供应，未被压缩。
- **不是入侵恶化**：入侵占写入幅度 57.0 → 54.7 → 51.0 → 55.0，**与制度数无关**，未随规模上升。
- **不是分离退化**：home/域 稳定在 0.64–0.78，未随规模下降。

剩下的解释是**检索难度**：可混淆的已存目标从 530 增至 5198（**9.8×**），而准确率仅降至 0.70×。预测的不同目标数 47.0 → 56.8，说明模型仍在广泛区分而非坍缩到少数热门目标。

即：**退化对候选数的增长是次比例的**——存的东西多了十倍，每项的可检索性只降三成。这与灾难性干扰（预期更陡）不同，更像是「知识越多、区分越难」的固有代价。

**留作 open question 的形式**：本机制的退化是否遵循 $\text{acc} \propto N^{-\alpha}$（$N$ 为已存条目数，实测 $\alpha \approx 0.16$），以及该指数能否通过更强的寻址（而非更多容量）改善。这可被证伪，且不需要新机制即可测量。

### 39.5 解析器错误：54.2% 被报成 100.00%

会话中临时写的多个解析器在主表分隔线处用 `continue` 而非 `break`，扫入下游同列数的表格，取到了非准确率的列。据此曾报出「64 域 98.25%」「H=1 达到 100%」「修好生长即无崩塌」——**全部作废**。

解析已统一至 `mainacc.py`：锚定 `Arm Name` 表头、**按列名**取 `0-Shot Acc`、在闭合分隔线处停止。此后所有数字只经此入口。
