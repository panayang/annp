# DESIGN-SDR：连续高维稀疏激活与自洽动力学架构设计方案

> **状态**：方案草案（2026-08-17 深度迭代与实测重构）  
> **核心定位**：彻底解决此前架构中“稠密矩阵代数平均导致的遗忘加速”、“单步 reset 导致信源退化为离散 2-Gram”、“全横档等电荷求和导致代数短路与算法数值完全相同”、“全局无差别 relax 导致静默突触在虚无中自发消解”、“多时标加权读出导致前向有效步长腰斩且深层横档反向阻尼破坏少步恢复”、“全局 Top-K 混放竞争造成快信号对慢信号的系统性挤兑与表征漂移”等系统性矛盾。
> 本方案确立**8-to-8 时标同构分层皮层架构（8-Band Isomorphic Cortical Architecture）**：通过**8 级输入因果时标阶梯（$m_{\text{in}} = 8$）**与**8 个独立分层皮层带（8 Cortical Bands）**的 1-对-1 严格同构映射、**分带局部 $k$-WTA 独立激活**、**统一同质突触深度（$m=8$）**、**纯粹表面原生读出（Pure Surface Readout $U_1$）**与**活动门控事件扩散（Activity-Gated Event Diffusion）**，在容量过载的无标度齐夫图游走流上构建真实反映多时标记忆力与前向可塑性的判决性系统。

---

## 0. 底层认知迭代与历史教训总结（去人工化与物理自洽哲学）

回顾此前 `DESIGN.md`（图拓扑与全网络扩散）、`DESIGN-NEXT.md`（旋转寻址与隐马尔可夫流）、`DESIGN-TREE.md`（动态二叉树分叉）以及最新 16 核真实基准测试的实验轨迹，团队沉淀了十五条最深刻的物理与方法学教训：

### 教训 1：多时标巩固阶梯在稠密共享表征上数学上必然失效（代数平均陷阱）
- **现象回溯**（`RESULTS.md` §10.3）：在单层稠密共享权重矩阵上挂载 Benna–Fusi 阶梯，跨域轮转测试测出“再入新域代价高出 3 倍、总损失惨败于纯塑料权重”的反常现象。
- **机理解剖**：阶梯扩散方程是各向同性的低通积分器。当所有域共享同一套稠密参数时，跨域交替输入会被深层横档无差别积分为“全域混合均值 $\bar{W}$”。重回旧域时，深层横档不仅无法提供特异性记忆，反而成为阻止浅层快速适应当前域的强力弹性阻尼。
- **物理硬结论**：**多时标阶梯能发挥抗漂移固化威力的唯一物理前提，是表征必须具有“稀疏空间隔离（Sparse Spatial Isolation）”。**

### 教训 2：全局无差别 relax 导致静默突触在虚无中自发消解（必须时间局部化）
- **现象回溯**：在 $D=512, k=16$ 的稀疏网络中，每步有 496 个静默列。若全局每步调用 `relax()`，在经历 6000 步跨域迭代后，未被触碰的静默突触浅层电位自发消解衰减至 $\sim 0.013$。
- **机理解剖**：生物突触内部的生化扩散是由动作电位激发的 $\text{Ca}^{2+}$ 离子内流门控的。没有神经冲动通过的静默突触处于生化冻结态，绝不应当随全局时钟空转耗散。
- **物理硬结论**：**必须实行“活动门控时间局部化扩散（Activity-Gated Event Diffusion）”——仅对当前步选中的 $k$ 个活跃列推进扩散；静默列时间暂停，零自发耗散，且计算量直降 $D/k = 32$ 倍。**

### 教训 3：活动门控下的事件电导率必须与激活事件时钟对齐（自洽电导率 $g_1 \approx 0.1$）
- **现象回溯**：将全局 relax 改为活动门控后，单神经元在一个域内仅被激活约 13 次。若仍沿用全局时钟电导 $g_1 = 1/\text{Span} = 0.0005$，13 次激活传导到深层横档的电荷只有 $10^{-10}$（深层完全未通电），导致 $m=4$ 与 $m=8$ 发生物理简并产生完全相同的数字。
- **机理解剖**：活动门控的物理时钟是“事件时钟（Event Clock）”，每次激活代表一次真实的突触事件。
- **物理硬结论**：**事件电导率必须设为事件尺度的自洽值 $g_1 \approx 0.1$，让 10~20 次激活能将电荷有效渗透到深层 $U_2 \dots U_m$ 水库中。**

### 教训 4：稀疏梯度更新必须使用相对共振信用分配（消除 $\eta$ 边界陷阱）
- **现象回溯**：若更新项为 $\eta \cdot \alpha_i \cdot \mathbf{\delta}_t$，由于 $\alpha_i \approx 1/k$，前向 Logit 改变量被无辜削弱了 $k = 16$ 倍，导致优化器在搜索时被迫贪心撞向右边界 $\eta = 3.0$。
- **机理解剖**：单神经元更新必须进行尺度归一化以维持 Logit 空间步长的尺度不变性，同时保留相对信用分配。
- **物理硬结论**：**更新项严格采用相对共振信用分配：$U_1(i) \mathrel{+}= \eta \cdot (k \cdot \alpha_i) \cdot \mathbf{\delta}_t$。使均值有效步长为 $\eta$，让最优学习率自然回落至健康的内点区间。**

### 教训 5：全横档总电荷求和会导致代数短路（必须非等权势能）
- **现象回溯**：若将读出定义为 $W_{\text{eff}} = \sum C_k U_k$，由于线性扩散内部通量对消 $\frac{d}{dt}\sum C_k U_k = \text{输入通量}$，导致 $\sum C_k U_k(t) \equiv W_{\text{plain}}(t)$，所有算法输出结果精确到小数点后 4 位完全相同。
- **机理解剖**：总电荷求和直接把阶梯动力学短路掉了。

### 教训 6：多时标特征读取融合（Readout Fusion）在实测中彻底证伪（必须纯粹表面读出 $U_1$）
- **现象回溯**：试图通过反比势能加权 $W_{\text{read}} = \sum w_k U_k$（$w_1 \approx 0.50$）让前向直接听到深层横档。实测显示：Ladder-8 的 20 步恢复率从 **30.0% 暴跌至 8.0%**，全面惨败给 Plain 的 26.0%！
- **机理解剖**：
  1. **前向有效学习率被无辜腰斩**：由于更新进入 $U_1$，但在前向中 $U_1$ 只有 $w_1 = 0.50$ 的权重，导致单步适应力度减半，微调极其迟钝；
  2. **深层横档变成反向阻尼**：深层横档沉淀了上一任务的参数，手握 50% 投票权死死投给上一任务，强行压制浅层对新任务的适应。
- **物理硬结论**：**必须坚定回归 Benna & Fusi（Nature 2016）第一性原理——前向预测严格且纯粹只读表面可塑电导 $W_{\text{read}} \equiv U_1$！深层横档 $U_2 \dots U_m$ 仅作为隐式扩散水库，通过物理回流 $g_1(U_2 - U_1)$ 协助 $U_1$，绝不能在前向中直接干扰决策。**

### 教训 7：无上下文的同名对抗覆写属于概念漂移（Concept Drift），非真实持续学习
- **现象回溯**：在无领域指示 token 的前提下，强行让相同输入 $[E_i, R]$ 映射到互斥的真值（Domain 0 为 $T_0$，Domain 1 为 $T_1$）。
- **机理解剖**：这是单值函数的逻辑矛盾。当 Domain 1 连续执行 666 步时，它是在对突触下达洗脑指令，要求彻底清除 $T_0$。让系统在无上下文时猜出被覆盖的旧标签属于不合逻辑的死局。
- **物理硬结论**：**必须放弃无上下文对抗洗脑，采用符合自然知识规律的“层次化无标度齐夫图游走信源（Hierarchical Zipf-Graph Walk Stream）”。**

### 教训 8：0-Shot 与 Few-Shot 在物理上不可兼得，生物终身学习的核心是“线索激发下的极速回忆”
- **生物机理对齐**：人类大脑不会在工作记忆表面（$U_1$）同时维持一生的全部知识。当环境切换时，工作记忆聚焦当前上下文，深层记忆在慢时标分子横档（$U_2 \dots U_m$）中休眠。**一旦给予极微量线索提示（Few-Shot 提示 / Priming），深层电荷瞬间反向充入浅层，实现极速唤醒回忆（Rapid Memory Retrieval）**。
- **物理硬结论**：**持续学习系统的核心判决指标必须是“多范围少步快速回忆恢复曲线 $\operatorname{Acc}(N), N \in [5, 10, 20, 50, 200]$”与“前向可塑性保留度”。**

### 教训 9：经典基线的致命缺陷（EWC 僵化与 Plain 互斥泥潭）
- **现象回溯**：
  - **Proximal EWC**：通过 Fisher 惩罚将参数死死锁在多任务均值上，导致 0-Shot 测出 25%、200-Shot 还是 25%、新任务学习率也只有 25%——**彻底丧失可塑性（灾难性僵化，Catastrophic Intransigence）**；
  - **Plain SGD**：由于缺少多时标水库保护，在对抗冲突下陷入新旧知识互相覆写的泥潭；
  - **Ladder-8**：在保持 100% 新知识吸收力的同时，提供了高斜率的极速回忆反弹力。

### 教训 10：时标比率 $r=2.0$ 在 $m=8$ 下量程自洽（无需盲目增大 $r$）
- **现象回溯**：几何比率 $r=2.0$ 在 $m=8$ 下覆盖时标 $\tau_8 \approx 2^{14} / 0.1 = 163,840$ 步，已完全覆盖万步级持续学习量程。保持 $r=2.0$ 的密集时标阶梯，确保多时标连续谱的平滑渐进性。

### 教训 11：拒绝为证明阶梯优势而人为给 Plain/EWC 施加负面惩罚（绝对公平原则）
- **物理硬结论**：**Plain 保持纯粹 SGD、EWC 保持原生近端 Fisher 约束、Ladder 保持原生多时标回补。所有算法在真实的数据竞争中同台竞技。**

### 教训 12：单步 `trace.reset()` 导致信源退化为离散 2-Gram
- **物理硬结论**：**必须采用连续语篇流/图游走流，让输入阶梯 $Z_t$ 的多时标因果流自然沉淀语境。**

### 教训 13：离散槽位管理与动态阈值必遭冷启动陷阱与特征爆炸
- **物理硬结论**：**必须放弃任何形式的离散开槽、硬阈值分叉与动态分配，转向高维连续几何空间的天然准正交性（Continuous High-Dimensional SDR）。**

### 教训 14：低负载无干扰环境下阶梯退化为表面信噪比稀释器，阶梯的唯一物理生境是“容量过载与重度重叠干扰”
- **现象回溯**：在 $D=512, k=16$（理论容量 $\approx 4700$ 个事实）的高维空间中仅存入 200 个自洽事实（负载率仅 4.2%），任意两事实重叠仅 0.5 个神经元。Plain SGD 表现为一个 0 干扰的完美无损哈希表，0-Shot 保持率高达 **86% ~ 92.5%**；而阶梯随着层数增加将浅层电荷自发扩散稀释到深层（$C_{\text{total}} = 255$ 导致表面电位衰减至 $1/255$），导致保持率单调暴跌。
- **物理硬结论**：**基准评测必须置于容量过载压缩区（$D=64 \sim 128, k=8$），且评测指标必须按 Zipf Rank 实行“分层段落评测（Stratified Rank Evaluation: Top-20% Hubs, Mid-30% Domain, Tail-50% Leaves）”，精准呈现高频核心知识的深层固化与长尾事实的少步唤醒。**

### 教训 15：全局 Top-K 混放竞争造成快信号对慢信号的系统性挤兑，必须实行各时标独立单位范数归一化（Independent Unit-Norm Normalization）
- **现象回溯**：若将 8 个输入时标直接拼接送入大投影矩阵 $\Phi$，高频即时 Token（$Z_1$）因瞬间脉冲能量远大于平缓的长程语境，会在点积中压倒慢信号。
- **物理硬结论**：**各时标必须独立执行单位范数归一化 $\hat{Z}_b = Z_b / (\|Z_b\|_2 + \epsilon)$，使得每一个有效时标在全脑点积中贡献严格等权的能量期望 $\mathbb{E}[\|\hat{Z}_b\|^2] = 1$！**

### 教训 16：人为硬编码阈值（如 `1e-6`）破坏物理连续性，必须采用连续平滑能量权重
- **现象回溯**：硬编码 $\mathbb{I}(E_b > 10^{-6})$ 阶跃跳变，且将微弱渗漏的慢皮层赋予与满负荷皮层完全相同的 $1/N_{\text{act}}$ 权重，违背了物理连续性。
- **物理硬结论**：**废弃一切人工硬阈值，各神经元的激活权重自然由连续非负投影电位 $\alpha_i = (u_i)_+ / (\sum (u_j)_+ + \epsilon)$ 决定，平滑、连续且零失真。**

### 教训 17：分带 1-WTA 造成组合空间坍缩与 100% 严重哈希碰撞，SDR 必须依托全脑高维 Top-$k$ 组合空间与多时标联合感受野
- **现象回溯**：将全脑硬性拆分为 8 个 $D_{\text{band}}=16, k=1$ 后，组合空间从 $\binom{128}{8} \approx \mathbf{1.4 \times 10^{12}}$ 剧烈坍缩至 $16 \times 8 = \mathbf{128}$ 个局部离散槽位。50 个事实映射到 16 个槽位上必然产生 100% 的重叠碰撞覆写，直接导致五条臂 0-Shot 全线拉平在 32.5%。
- **机理解剖**：大脑海马-皮层中的神经元绝非“单一时标孤立放电”，而是具有跨时标的“联合感受野（Joint Spatio-Temporal Receptive Field）”——一个神经元同时整合即时 Token $Z_1$ 与宏观环境 $Z_8$ 的联合约束。
- **物理硬结论**：**彻底废除硬性 1-WTA 分割，重回全脑高维 SDR 空间（$D=128 \sim 512, k=8 \sim 16$）。输入阶梯保持 8 级多时标独立单位范数归一化，通过全局固定正交随机矩阵 $\Phi \in \mathbb{R}^{D \times (8d)}$ 投影并执行全局 $k$-WTA，恢复上万亿级组合容量与天然准正交抗干扰隔离！**

### 教训 18：终身学习超参数网格搜索必须以零样本保持损失（0-Shot Retention Loss）与保持准确率为首要优化目标，严禁将重学指标（Few-Shot Re-learning）置于首位或使用模型训练学习率作为探测步长
- **现象回溯**：在网格扫描中，所有对照臂的最优学习率均被异常选为上界 $\eta = 10.0$。
- **机理解剖**：
  1. **目标颠倒**：原逻辑将 `few20_acc`（20 步少样本重学习准确率）作为超参数选择的第一优先级（Primary Key），而将 0-Shot Retention 压在后面；
  2. **探测步长污染**：`evaluate_few_shot` 直接采用了模型的训练超参数 $\eta$。当 $\eta=10.0$ 时，模型在测试微调中凭借 100 倍的大步长暴力覆盖记忆，使得 `few20_acc` 随 $\eta$ 单调递增，网格搜索被强行绑架至上边界 $\eta=10.0$；
  3. **真实物理图景**：按真实的 0-Shot 保持损失评估时，Plain (m=1) 的真正物理最优学习率是 $\eta^* = 0.1$（Loss 4.06，当 $\eta=10.0$ 时严重恶化至 4.50）；Ladder-8 的物理最优学习率则是 $\eta^* = 1.0 \sim 3.0$（因深层固化保护，能够容纳 10~30 倍的高速吞吐，Loss 达到 4.06）。
- **物理硬结论**：
  1. **网格搜索优选准则必须严格遵循终身学习第一性原理**：首要目标为 **0-Shot Revisit Retention Loss（最小化遗忘损失）**，次要目标为 **0-Shot Revisit Retention Accuracy**，第三目标为 **Forward Plasticity**；
  2. **Few-Shot 探针测试步长必须标准化固定（如 $\eta_{\text{probe}} = 0.1$）**，公平检验不同突触结构在深层固化支撑下的真实少步反弹能力。

---

## 1. 核心架构：8 级多时标联合因果 SDR 系统（8-Scale Joint SDR）

系统由“8 级因果输入阶梯 $\to$ 全局高维联合 SDR 稀疏投影 $\to$ 表面原生读出 $\to$ 活动门控突触阶梯”五大模块组成：

```
       历史已见 Token 流: x_0, x_1, ..., x_{t-1}
                        │
                        ▼
       【步骤 1：8 级输入因果上下文阶梯流动 (Input Context Ladder, m_in = 8)】
           Z_1(t) += e(x_{t-1}), 推进内部 8 级 RC 扩散 relax_in()
           · Z_1: 即时单字级 (τ ≈ 2 步)         · Z_5: 段落级语境 (τ ≈ 32 步)
           · Z_2: 短语短程级 (τ ≈ 4 步)         · Z_6: 篇章长程级 (τ ≈ 64 步)
           · Z_3: 分句句法级 (τ ≈ 8 步)         · Z_7: 局部领域级 (τ ≈ 128 步)
           · Z_4: 长句命题级 (τ ≈ 16 步)        · Z_8: 终身宏观级 (τ ≈ 256+ 步)
           各时标独立单位向量归一化: Ẑ_1, Ẑ_2, ..., Ẑ_8 ∈ R^d
           拼接全时标状态向量: Ẑ_t = [Ẑ_1; Ẑ_2; ...; Ẑ_8] ∈ R^{8d}
                        │
                        ▼
       【步骤 2：全脑高维联合随机投影与全局 k-WTA 稀疏选择 (Joint SDR Projection)】
           · 固定感知基: Φ ∈ R^{D × (8d)} (每行单位范数归一化, 终身冻结)
           · 膜电位投影: u_t = Φ · Ẑ_t ∈ R^D (每列天然整合多时标联合感受野)
           · 全局稀疏激活: A_t = argtopk(u_t, k) (例如 D=128, k=8, 组合空间 C(128,8) ≈ 1.4×10^12)
           · 连续非负余弦权重: α_i = (u_i)_+ / (∑_{j ∈ A_t} (u_j)_+ + ε)
                        │
                        ▼
       【步骤 3：表面原生读出 (Pure Surface Readout)】
           Logits_t = ∑_{i ∈ A_t} α_i · U_1(i) ∈ R^V
           p_t = softmax(Logits_t)
                        │
                        ▼
       【步骤 4：揭晓真实目标 Token y_t，计算交叉熵】
           Loss_t = -log p_t(y_t)
                        │
                        ▼
       【步骤 5：活动门控突触更新与局部事件扩散 (Activity-Gated Synaptic Ladder)】
           残差 δ_t = 1_{y_t} - p_t
           · 活跃列 i ∈ A_t: U_1(i) += η · (k · α_i) · δ_t, 推进该列 relax_columns(i)
           · 静默列 i ∉ A_t: 外部输入为 0, 内部阶梯扩散暂停 (时间冻结, 零自发耗散)
```

### 1.1 静态参数与结构标定
* **词表大小**：$V = 512$；
* **基础嵌入维度**：$d = 16$ 或 $32$（单 Token 正交编码）；
* **输入阶梯层数**：$m_{\text{in}} = 8$（总输入维度 $8d = 128 \sim 256$）；
* **SDR 空间维度**：$D = 128$（基准）或 $512$（高容量），组合状态空间 $\binom{D}{k} \ge 1.4 \times 10^{12}$；
* **活跃神经元数**：$k = 8$（$D=128$ 时稀疏度 $6.25\%$）或 $k = 16$（$D=512$ 时稀疏度 $3.125\%$）；
* **突触巩固阶梯深度**：$m=8$（总电容 $C_{\text{total}} = 255$）；
* **突触事件电导率**：$g_1 = 0.1, r = 2.0$。

### 1.2 相对共振信用分配与梯度反向传播
在每个皮层带 $b$ 内部：
$$u_{b, i} = \phi_{b, i}^\top \hat{\mathbf{Z}}_b(t) \in [-1, 1]$$
$$\alpha_{b, i} = \frac{\max(0, u_{b, i})}{\sum_{j \in \mathcal{A}_t^{(b)}} \max(0, u_{b, j})} \quad (\forall i \in \mathcal{A}_t^{(b)})$$
- **分带反向更新**：
  $$U_1^{(b)}(i) \mathrel{+}= \eta \cdot (k_{\text{band}} \cdot \alpha_{b, i}) \cdot \mathbf{\delta}_t$$
- **几何与物理自洽**：
  - 各皮层带在均值意义下贡献相等的更新步长 $\eta$，各时标独立学习并沉淀专属特征，绝无快慢时标之间的相互压制。

---

## 2. 信源设计：层次化无标度齐夫图游走流（Hierarchical Zipf-Graph Walk Stream）

### 2.1 终身共享 Hub 骨干与领域特异长尾拓扑（Lifelong Hub Backbone & Domain Leaves）
知识图谱由 $N_E$ 个实体节点 $\mathcal{E}$ 与各个领域的关系边 $\mathcal{R}$ 构成：
1. **实体分层与齐夫流行度分布（Zipfian Power-Law）**：
   - 实体流行度满足 $w(e_i) = \frac{1}{(i + 1)^s}$（$s \approx 1.0$）；
   - **全局终身共享枢纽（Global Interleaved Hubs，前 20%~25%）**：作为所有领域通用的常识语义骨干，**在每一个领域的流式游走中均以高概率持续出现**，实现跨任务的终身交织强化；
   - **领域专属长尾叶子（Domain-Specific Leaves，其余 75%~80%）**：完全隔离分配至各个领域社团内，仅在当前领域处于训练状态时被激活，其余时间面临其他领域的连续冲刷干扰；
2. **组合自洽无矛盾（Combinatorial Non-Contradiction）**：
   - 全图无逻辑矛盾的冲突标签；
   - 冲突产生于**全局 Hub 实体在不同领域被不同上下文反复拉扯微调引起的局部突触争抢**；
3. **均衡分层探针集（Balanced Stratified Probing）**：
   - 为每个领域严格按比例构建探针集：
     - **Tier 0 (Hub Facts)**：$(E_{\text{hub}}, R) \to E'$（跨领域高频骨干）；
     - **Tier 1 (Mid Facts)**：$(E_{\text{leaf}}, R_{\text{domain}}) \to E_{\text{hub}}$（领域叶子连接到全局 Hub 的纽带）；
     - **Tier 2 (Tail Facts)**：$(E_{\text{leaf}}, R_{\text{domain}}) \to E_{\text{leaf}}'$（纯领域专属长尾事实）。

### 2.2 带偏连续图游走动力学（Biased Graph Random Walk）
* **游走序列生成**：
  - 游走者在当前领域子图内漫步，出边转移概率正比于目标实体的 Zipf 权重 $P(E_{t+1}) \propto w(E_{t+1})$；
  - 游走者约 50% 时间游弋于全局 Hub 骨干，约 50% 时间探索当前领域的特异性叶子分支；
  - 输出连续流式 Token 序列：$\dots \to E_t \to R_t \to E_{t+1} \to R_{t+1} \to E_{t+2} \dots$
* **物理动力学效应**：
  - **全局 Hub 骨干**：经历终身跨域反复激活，源源不断向深层横档 $U_4 \dots U_8$ 注入电荷并永久自发固化；
  - **领域长尾叶子**：在离开该领域后进入休眠，浅层电荷扩散；重回旧领域时，**深层已固化的 Hub 骨干作为联想检索的锚点（Associative Anchors），引导长尾事实在极少步（Few-Shot 5~20 步）内极速唤醒！**
  - **输入阶梯协同**：$Z_7, Z_8$ 沉淀长程领域语境，$Z_1, Z_2$ 捕获局部组合关系。

---

## 3. 评测协议：多范围少步快速回忆谱（Few-Shot Memory Retrieval）与前向可塑性

```
      【域 1 连续流式游走 (L 步)】 ──► 【干扰流 (经历 域 2,3,4 共 T_delay 步)】
                                                        │
                                                        ▼
                                           【重回 域 1 的边界时刻】
                                                        │
                                     ┌──────────────────┴──────────────────┐
                                     ▼                                     ▼
                      【阶段 1：零样本保留基线 (0-Shot)】     【阶段 2：多范围少步极速回忆 (Few-Shot)】
                      · 冻结学习率 η = 0, 阶梯暂停；           · 给予 N ∈ [5, 10, 20, 50, 200] 步线索提示；
                      · 测量【0-Shot 静态基线】；               · 记录【各步数下的恢复准确率曲线】；
                      · 观察高频枢纽记忆的深层固化保持率。     · 检验深层多时标横档反向回补浅层的爆发力。
                                                                           │
                                                                           ▼
                                                              【阶段 3：前向可塑性 (Forward Plasticity)】
                                                              · 记录新域初次收敛步数与斜率；
                                                              · 检验是否发生 EWC 式的灾难性僵化（死锁）。
```

### 3.1 核心评价指标体系（按 Zipf Rank 分层评测）
1. **分层齐夫秩保留矩阵（Stratified Zipf Rank Retention）**：
   - **Tier 1: Top-20% Hub Facts（全局核心骨干）**：检验深层横档 $U_4 \dots U_8$ 的长期自发固化与抗覆写能力；
   - **Tier 2: Mid-30% Domain Facts（中频领域知识）**：检验中层横档 $U_2 \dots U_3$ 的过渡记忆力；
   - **Tier 3: Tail-50% Leaf Facts（长尾偶发事实）**：检验浅层 $U_1$ 与线索提示下的少步唤醒爆发力；
2. **多范围少步极速回忆谱（Few-Shot Recovery Curve，主核心指标）**：
   - 测量在微调 $N \in [5, 10, 20, 50, 200]$ 步后的分层准确率恢复矩阵 $\operatorname{Acc}(N)$；
3. **前向可塑性指数（Forward Plasticity Index，健康度红线）**：
   - 确保系统保持 $>90\%$ 的新知识吸收灵敏度；
4. **零样本保留基线（0-Shot Retention Baseline）**：
   - 检验高频核心骨干知识在重度突触干扰下的自发深层固化保持率。

---

## 4. 对照组设计（Control Arms）

| 臂编号 | 名称 | 架构与超参数配置 | 验证目标 |
|---|---|---|---|
| **Arm 1** | **8-Band Cortical + Ladder-8** | 8 级输入阶梯 $\times$ 8 皮层带（$D=128, k=8$）+ 权重阶梯 $m=8$（表面 $U_1$ 原生读出 + 活动门控 $g_1=0.1, r=2.0$） | **主被测系统**：检验在输入-皮层-突触严格同构下，深层横档对高频骨干与长程语境的自发稳固与长尾记忆的极速唤醒 |
| **Arm 2** | **8-Band Cortical + Ladder-4** | 8 级输入阶梯 $\times$ 8 皮层带（$D=128, k=8$）+ 权重阶梯 $m=4$（表面 $U_1$ 原生读出 + 活动门控 $g_1=0.1, r=2.0$） | 阶梯深度消融：中等时标覆盖 |
| **Arm 5** | **8-Band Cortical + Online EWC** | 8 级输入阶梯 $\times$ 8 皮层带（$D=128, k=8$），权重使用近端 EWC 锚定惩罚 | **基线 2**：对比经典持续学习机制（同时监测其在流式 Zipf 上的僵化缺陷） |

---

## 5. 流模式设计（Stream Modes）

### 5.1 Mode A：正交独立领域流（Orthogonal Domains）
- 各领域实体与局部关系互斥，仅全局 Hub 共享；
- 检验宏观领域骨干提炼与长尾知识隔离。

### 5.2 Mode B：同名概念语义冲突流（Shared-Entity Semantic Collision）
- 各领域共享同一批实体池 $E$ 与关系池 $R$，但目标映射 $[E_i, R_i] \to \text{Target}_d(i)$ 100% 冲突；
- 系统必须依托 8 级输入阶梯中的慢时标语境累积（$Z_4 \dots Z_8$）完成自动时标展开与语境消歧；
- 检验在真实语义冲突与领域切换过渡态冲击下，突触多时标阶梯（Ladder）对瞬态边界噪声的时间滤波与抗污染能力。

---

## 6. 经验教训总结（Lessons Learned）

* **教训 19：8 级输入多时标因果展开天然赋予系统非稀疏同名冲突的消歧能力，突触阶梯则充当领域切换边界的时间滤波护盾**。
  在同名概念冲突流（Mode B）中，微观快时标 $Z_1, Z_2$ 虽 100% 重叠，但慢时标 $Z_4 \dots Z_8$ 沉淀了领域连续游走的语境流，使得投影后的 SDR 激活神经元集合 $\mathcal{A}_{\text{Dom 1}} \cap \mathcal{A}_{\text{Dom 2}} \approx \emptyset$ 自动正交。突触阶梯的核心价值在于吸收领域切换时的暂态过渡污染，防止单层 Plain 发生不可逆的表面权重破坏。

