# DESIGN-TREE：以 node 为基本单元的生长树（2026-08-16 起）

新文件，不继承 `DESIGN.md` / `DESIGN-NEXT.md` 的任何设计。前两份留档只作为"已花掉的搜索预算"。

**KPI**：组里回来之前完成基础机制验证，产出一份英文技术报告投 Zenodo。
数据一律用**合成信源**，不再碰真实文本。

---

## 0. 为什么重开：之前的对照组选错了类别

`DESIGN-NEXT.md` 里全部端到端对照是 FFN、FFN+阶梯、窗口 MLP。**这些本质上都是无记忆算法。**
在平稳流上、短预算下，它们几乎必然更快更好——而我们要主张的是非平稳下的保留。
拿无记忆算法当对照，等于用它们的主场判我们的客场，且混杂因素多到无法控制。

> **不变量 ①：对照必须与被测系统属于同一问题类别。**
> 持续学习架构的对照是持续学习方法（EWC 一族起步），不是任何无记忆/非持续学习的模型。

第二条来自昨天的语料排查：

> **不变量 ②：一律先在合成信源上测，且信源的分布形态必须显式验证，不能假设。**
> BPE 语料的秩-频曲线是弯的（rank 2 ≈ rank 1，真 Zipf 应为一半），
> 尾部只占 1% 质量——**保护尾部的机制在那份数据上没有作用对象**。
> 合成 Zipf 信源可以把分布指数当作实验变量，而不是一个未经检验的假设。

第三条来自昨天的六次量具事故：

> **不变量 ③：每次运行默认输出自检项**——胜出超参是否落在扫描边界、
> 各输入分块的范数、路由的使用份额与纯度、资源三口径（活跃参数 / 持有状态 / MACs per token）。
> 报告"没有"之前先验证量具能报告"有"。

第四条是这个架构的立论前提，也是它与上一版设计的分界：

> **不变量 ④：推理与训练不分开。**
> 任何"先决定架构形状、再用它推理"的机制都引入了一个训练期，与单遍在线的立论冲突。
> 结构上的自由度必须**始终开放**，并靠**结构约束**而非阶段划分来定界。

---

## 1. 基本单元：node

**node = 非线性写入 + 阶梯**，是这个架构里唯一的计算与存储单元。FFN 不再作为宿主出现。

$$\text{预测：}\ p = \mathrm{softmax}(W k), \qquad
\text{写入：}\ W \mathrel{+}= \eta\,(\mathbf{1}_y - p)\,k^{\top}$$

$W$ 整个坐在 Benna–Fusi 阶梯上（rung 1 活跃，深横档为纯状态）。

这个写入规则**同时**是"非线性局部写入"和"线性 softmax 层交叉熵的精确梯度"，
所以**"无反向传播"在这里不是一个要付代价的约束，它就是梯度本身**——
只因为没有隐层，梯度不需要传播。

**阶梯阶数 $m$ 在整树上测最优，不单独在一个 node 上测。**
理由：单 node 上 $m$ 的最优值反映的是那一个 node 承担的时间尺度；
树里不同层的 node 承担的尺度不同，最优 $m$ 是架构级的量。

---

## 2. 拓扑：按 surprise 分裂的生长树

- 每个 node 最多 $n$ 个子 node（实验取 $n=2$），**按需实例化**
- 输入自根部逐层下行：当前层的 node 把输入交给已存在的子 node，**surprise 最小者接手**
- 若归一化后**所有**已存在的子 node 的 surprise 都高于各自的 rolling surprise，
  说明这个输入不属于现有任何一支 → 未满 $n$ 个则**新建一支**接手，已满则交给最小者
- **推理不设提前停止**：每个输入一路走到 $D_{\max}$
- 沿路径的各层都出预测，按 §3.2 混合

每 token 算力恒为 $D_{\max} \times$ node 成本，内存上限由 $(n, D_{\max})$ 钉死——详见 §4。

---

## 3. 动手前定死的三点

### 3.1 【必须改】routing 用的 surprise 在因果上跑不通

按字面定义，surprise 是"预测与真值的差"，而**路由发生在看到真值之前**。
prequential 协议是：先预测 → 计费 → 再写入。用真值路由等于泄漏答案。

**修法：路由用的 surprise 只能由输入本身算出。**给每个 node 一个**原型** $\mu_j$：

$$\text{surprise}_j(x) = 1 - \frac{\langle x, \mu_j\rangle}{\|x\|\,\|\mu_j\|},
\qquad \mu_j \text{ 是该 node 吸收过的输入的运行均值}$$

$\mu_j$ 本身也挂阶梯——于是"rolling"不是一个新引入的滑动窗口，
而是同一套多时标机制，**局域性与平均性的平衡由阶梯的时间尺度给出，不需要再多一个超参**。

这是自组织映射式的无监督路由：**按内容找归属，不看答案**。
写入时仍然用真值（那时它已经可用），所以只有路由是无监督的。

> 另一种可选定义：用 node 最近的预测误差走势当 surprise。
> 它不随输入变化，无法在同一时刻区分两个子 node，**因此不适合做路由**，只适合做生长判据。

### 3.2 【必须定】只用叶子预测，还是沿路径混合

这是对 Zipf 数据影响最大的一个选择。

| 方案 | 后果 |
|---|---|
| **只用叶子** | 深层 node 看到的数据极少；Zipf 尾部正好落在深层——**样本最少的地方承担最难的预测** |
| **沿路径混合** | 粗层吃频繁上下文（数据多），细层吃稀有上下文（数据少），逐层细化 |

**定稿：沿路径混合。**它就是层次化 backoff——**处理 Zipf 尾部的经典解法**，
而 Zipf 正是选定的信源形态。混合权重由各层的 rolling surprise 给出，不引入新参数。

只用叶子的版本保留为消融，因为它是"树是否真的在分工"的直接检验。

### 3.3 【已定】固定上限，而且它与深度是同一个旋钮

**"有界内存下的持续学习"是仅存的几个可主张点之一**，所以生长必须有界。
定稿是固定上限，且这个上限**不是一个独立参数**——见 §4。

---

## 4. 深度：始终生长，每条路径深度有界

我原本提的方案是"最深层还在降 surprise 就加一层"。**那个方案是错的**，
因为它把"架构形状还在被决定"划成了一个阶段——那就是训练期，与不变量 ④ 冲突。

**定稿**：

- **生长始终开放**，任何时刻遇到新奇输入都可以实例化新 node，没有"停止生长"这一步
- **每条路径的深度上限 $D_{\max}$ 固定**
- 每个 node 最多 $n$ 个子 node（实验取 $n=2$），子 node **按需实例化**：
  当所有已存在的子 node 的归一化 surprise 都高于各自的 rolling surprise 时，
  若尚未满 $n$ 个则新建一个接手；已满则交给 surprise 最小的那个

于是**节点总数被结构本身钉死**：

$$N_{\max} = \frac{n^{D_{\max}+1} - 1}{n - 1}$$

$n=2, D_{\max}=10$ 时是 2047 个 node。**内存上限不是一个新超参，它是 $(n, D_{\max})$ 的推论。**

同时每 token 的算力是 $D_{\max} \times$ node 成本——**固定、可预测、与树已经长到多大无关**。
所以这一个旋钮同时钉死了两件事：

| $D_{\max}$ 决定 | 值 |
|---|---|
| 每 token 算力 | $D_{\max}$ 次 node 计算 |
| 内存上限 | $(n^{D_{\max}+1}-1)/(n-1)$ 个 node |

**树满之后的行为**：新奇输入路由到 surprise 最小的叶子并写入那里，
由阶梯承担干扰。这是有界内存下的正常降级，不是失败模式——
**"满了之后会怎样"本身就是要测的东西**，因为持续学习的主张正是关于内存耗尽之后。

### 逐层 surprise 仍然是主判据

$D_{\max}$ 不再由 surprise 曲线决定，但**那条曲线仍然是"树是否在分工"的直接检验**，
且它现在更便宜：一次运行就能读出各层的贡献。

> **若第 2 层相对第 1 层的 surprise 没有下降，说明分工没有发生，拓扑方向当场否定。**

这条优先于任何端到端损失数字。若下降存在但在某层之后进入噪声，
那就说明 $D_{\max}$ 取大了——如实报告，并作为下一次运行的依据。

---

## 5. 不切片，改为降低信源维数

**定稿：不切片。**整个输入向量走一条路径。

理由：切片会破坏各维之间的相关性，而随机码的信息本来就均匀摊在各维上，
**切片很可能什么结构都分不出来**——而那个失败会与"树是否在分工"这个主问题混在一起。
一次只动一个变量。

代之以**把合成信源的维数调低**，让单条路径上的 node 规模保持可控。
维数是信源的参数，不是架构的机制，改它不引入新的自由度。

---

## 6. 对照组

| 臂 | 角色 |
|---|---|
| **本架构**（树 + node + 阶梯） | 被测 |
| **online EWC** | 同类别对照：梯度学习 + Fisher 加权的锚定惩罚，任务无关版本 |
| **单 node + 阶梯**（$D=1$，无树） | 隔离拓扑：树到底加了什么 |
| **单 node 无阶梯** | 隔离阶梯 |

EWC 与我们的写入规则相容：node 的 softmax delta 规则就是线性层的梯度，
所以 Fisher 对角的在线估计与锚定惩罚可以直接加上，**两条臂共用同一个写入实现**，
差别只在惩罚项——这样对照就不会混入实现差异。

**不再出现**：FFN、FFN+阶梯、窗口 MLP、计数编码器。它们是无记忆算法（不变量 ①）。

---

## 7. 信源：合成 Zipf，非平稳

- **边缘**：Zipf$(s)$，$s$ 作为实验变量（$s=1$ 对齐 E0-b）
- **条件结构**：order-1 转移表，保证"当前符号是充分统计量"，任何失败都归因于学习机制
- **非平稳**：多个域轮转，域之间**词表窗口重叠**——
  重叠区产生干扰（有遗忘可防），非重叠区提供内容签名（路由可分辨）
- 每次运行输出实测秩-频曲线与局部指数，验证信源确实是想要的形态（不变量 ②）

**主指标**：再入代价 $-$ 同访问稳定代价（gap），逐访问的**轨迹**，不是末位数字。
**次指标**：逐层 surprise 下降、node 数增长曲线、资源三口径。

---

## 8. 第一步（最小可判决实验）

1. 建 node + 树，**不切片**，$n=2$，$D_{\max}$ 固定（节点总数随之钉死），生长始终开放
2. 合成 Zipf 非平稳信源，先验证分布形态
3. 输出逐层 surprise 曲线——**若第 2 层相对第 1 层无下降，拓扑方向当场否定**
4. 通过之后，才跑 EWC 对照与阶梯 $m$ 的扫描

$m$ 的最优值在**整树上**扫，不在单 node 上扫。

---

## 9. 信源标定（2026-08-16）

不变量 ② 要求分布形态实测而非假设。第一次 8 域运行报出"头尾指数不一致"，
追下来发现**诊断本身没有分离变量**：合并边缘同时受 `zipf_s`、`tilt` 和**域混合**三者影响。

### 9.1 单域下两个旋钮各管各的

| tilt | top-10 | 头 / 尾指数 | 互信息 |
|---|---|---|---|
| 0.0 | 47.7% | −0.995 / −1.103 | 0.211 |
| **1.0** | 43.7% | **−0.972 / −1.152** | **0.655** |
| 2.0 | 42.4% | −0.964 / −1.303 | 1.062 |
| 4.0 | 42.2% | −0.964 / −1.303 | 1.180 |

| `zipf_s` | 0.8 | **1.0** | 1.3 | 1.6 |
|---|---|---|---|---|
| 实测斜率 | −0.803 | **−1.005** | −1.318 | −1.637 |

**`zipf_s` 要什么给什么**；**tilt 买条件信息、卖尾部平直度**。

### 9.2 两个必须记住的判读陷阱

**① 互信息有噪声底。**tilt=0 时所有状态的后继分布完全相同，真实互信息为零，
实测 0.211——那是从计数估计互信息的正偏（$256\times256$ 格、20 万样本）。
**互信息只能相对 tilt=0 读，不能绝对读。**

**② 多域会压平合并边缘。**8 域重叠窗口下实测 −0.77，单域下 −1.005。
重叠区的符号从两个域拿质量，混合把幂律压平。**这不是构造错误，是混合的性质**——
但报告时必须区分"每域的分布"（设计意图）与"模型实际看到的合并分布"。

第一版把这两个来源混在一起，于是 tilt=0 时也测出 2.134 比特互信息，
**其中 2.13 是域身份、0.84 才是 tilt**。诊断不分离变量，和实验不分离变量一样坏。

### 9.3 工作点

$$\texttt{zipf\_s} = 1.0, \qquad \texttt{tilt} = 1.0$$

单域实测斜率 −1.005、头尾差 0.18（干净幂律）、扣除噪声底后条件信息约 0.44 比特。
与 E0-b 的 Zipf(s=1) 对齐。tilt=2.0 信息更多但尾部弯到 −1.303，不划算。

---

## 10. 分层判读：阶梯保护的地方不是出问题的地方（2026-08-16）

聚合的比特每 token 在 Zipf 下几乎就是**头部**损失，而阶梯的机制只作用于**尾部**。
之前六次"阶梯为负"全都是用一个不含被测对象的量在判被测对象——
与 §15 的 BPE 语料事故同型，只是那次是数据里没有尾部，这次是**指标把尾部平均掉了**。

按目标符号的全局秩分段重测（192k token，8 域，$s{=}1.0$，tilt$=1.0$，节点与参数全部匹配）。

### 10.1 阶梯的代价随秩递减，但从不转正

阶梯效应（$m{=}4$ 减 $m{=}1$，正 = 阶梯更差）：

| 秩段 | 占比 | 扁平拓扑 | 树拓扑 |
|---|---|---|---|
| rank 1-4 | 10.5% | +0.234 | +0.313 |
| rank 5-16 | 20.4% | +0.242 | +0.362 |
| rank 17-64 | 30.4% | +0.260 | +0.371 |
| **rank 65+** | 38.8% | **+0.052** | **+0.096** |

**尾部代价比头部小 4–5 倍，方向与机制预期一致，但没有任何一段转正。**

### 10.2 遗忘集中在头部，不在尾部

各臂的再入 gap 按秩段分布（flat30-m1）：

| 秩段 | gap |
|---|---|
| rank 1-4 | **+2.156** |
| rank 5-16 | +1.612 |
| rank 17-64 | +0.897 |
| rank 65+ | **+0.153** |

域切换覆盖掉的是**旧域的高频内容**——学得最扎实、也最容易被顶掉的那部分；
稀有项本来就没学好，**没有东西可失去**。

> **保留问题的战场在头部，阶梯的作用域在尾部。它保护的地方不是出问题的地方。**

"尾部代价更小"因此救不了它：**尾部代价小，是因为尾部本来就没什么可争的。**

### 10.3 层次结构无贡献，EWC 在每一段全胜

节点数、参数、写入规则完全相同，只有拓扑不同：

| 臂 | 节点 | gap（末四次均值） | MACs/token |
|---|---|---|---|
| **flat30**（$D{=}1$，30 专家） | 31 | **0.578** | **32,768** |
| tree-m1（$D{=}4$ 二叉） | 31 | 0.644 | 81,920 |

**扁平混合体略胜深度 4 的树且便宜 2.5 倍。**此前"树赢 $D{=}1$（0.644 vs 1.127）"的优势
全部来自节点数 3→31，**不是来自层次**。

匹配参数后的 EWC（$d{=}992$，253,952 参数）：

| 秩段 | ewc | flat30-m1 | tree-m1 |
|---|---|---|---|
| rank 1-4 | **3.137** | 3.322 | 3.290 |
| rank 65+ | **7.191** | 7.609 | 7.644 |
| gap 均值 | **0.362** | 0.578 | 0.644 |

尾部并非不可学（均匀 8.0，EWC 取得 0.81 比特），**我们只取得它的一半**。

### 10.4 两个必须写明的混淆

**① 表示充分性没有匹配。**EWC 用 $d{=}992$，256 个随机码在 992 维里近乎正交——
它拿到的实际上是 one-hot，一张能精确表示任意条件分布的查表；
树的每个 node 用 $d{=}32$，秩严重不足。**参数匹配了，表示没匹配**，
这是同一个坑在本项目里的第三次。

**② 区间选错了。**主张是"有界内存下的持续学习"，而 992 维对 256 个符号根本不是有界——
一个模型装得下全部，**分区就没有任何东西可买**。树的前提（内存不够所以必须遗忘）
在这个配置里不成立。

判决实验应当搬进符号数远大于单模型容量的区间。**这不是移动球门——主张本来就只在那里有内容。**

---

## 11. 有界区间：阶梯的正结果，与一个统一的负结果解释（2026-08-16）

§10.4 指出两个混淆：表示充分性未匹配、区间不是"有界内存"。搬到 vocab 2048、
所有臂 $d{=}32$（$32 \ll 2048$，人人受限）之后重测。

### 11.1 阶梯效应随秩单调翻转，尾部转正

$m{=}4$ 减 $m{=}1$（正 = 阶梯更差）：

| 秩段 | 占比 | 树拓扑 | 扁平拓扑 |
|---|---|---|---|
| rank 1-7 | 12.3% | +0.900 | +0.601 |
| rank 8-45 | 22.1% | +0.980 | +0.642 |
| rank 46-304 | 29.2% | +0.528 | +0.342 |
| **rank 305+** | **36.4%** | **−0.152** | **−0.094** |

**单调下降并在尾部穿过零点，两个拓扑一致。**这是阶梯在新架构里的第一个正结果，
且它**只在有界区间出现**——vocab 256 / $d{=}992$ 时尾部代价虽小但从未转正。

它同时解释了 §15 的 BPE 失败：**尾部只占 1% 质量，收益乘以 1% 抵不过头部成本。**

**幅度要如实说**：尾部收益 0.09–0.15 比特，头部代价 0.60–0.98 比特，按质量加权整体仍是净负。

### 11.2 EWC 的惩罚是净成本，且与锚点尺度无关

第一版的 $\lambda \in \{0,1,10,100\}$ **全部形同不存在**：梯度是 $(\mathbf{1}_y-p)x$，
只有目标行是 $O(1)$，其余 $V-1$ 行是 $-p \sim 1/V$，所以 Fisher 对绝大多数权重是 $10^{-6}$，
$\lambda{=}100$ 的惩罚比梯度小四个数量级。**"$\lambda{=}0$ 胜出"当时是四条相同臂的噪声平局，不是发现。**

往上推又会发散（$\lambda{=}10^8$ 给出 NaN）：$F$ 在目标行 $\sim 0.1$、其余 $\sim 10^{-6}$，
**显式更新下不存在同时有效且稳定的 $\lambda$**。改成近端步

$$w \leftarrow \frac{w + \eta g + \eta\lambda\hat F w^*}{1 + \eta\lambda\hat F}, \qquad \hat F = F/\bar F$$

无条件稳定，$\lambda$ 语义统一（0 = 不锚定，$\infty$ = 钉死在锚点）。惩罚随即生效
（|惩罚|/|梯度| 达 0.54–0.94），而结论不变：

| $\lambda$ | 0 | 0.1 | 1 | 10 | 100 |
|---|---|---|---|---|---|
| 总比特 | **1,906,864** | 2,107,575 | 2,110,340 | 2,111,603 | 2,111,950 |

锚点跟随速率扫 **128 倍**（域内 3000 → 十六轮回 384k），各档差异 0.18%，$\lambda{=}0$ 每档胜出。
**时间尺度混淆已排除，这是干净的负结果。**

### 11.3 一个统一两个负结果的解释

EWC 与阶梯输给同一件事：**这条流上重学太便宜**。8 个域每 21k token 轮回一次，
末几次访问的 gap 只有 0.3–0.5 比特——**回来重学几乎不花钱，保护便不值它的成本**。
这也解释了 §10.2 的"遗忘集中在头部"：头部 token 频繁，几百个 token 就重学回来了。

> **在重访频繁、重学便宜的流上，任何巩固机制都必然是净成本。
> 这不是机制不行，是这个区间里没有需求。**

因此真正该扫的变量是**重访间隔**：保护的收益 vs 间隔长度，交叉点在哪。
"间隔多长时巩固开始划算"本身就是一条可画出来的曲线，也是"有界内存持续学习"
这个主张唯一有内容的地方。

### 11.4 层次结构仍无贡献

节点数、参数、规则全同，扁平 30 专家在几乎每一段不劣于深度 4 的树，
gap 均值 0.206 vs 0.275，算力只要 40%。**深度这一项至今一次未赢。**
